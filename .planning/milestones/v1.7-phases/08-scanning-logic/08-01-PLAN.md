---
phase: 08-scanning-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modes/test_tracker.py
autonomous: false
requirements:
  - SCAN-01
  - SCAN-02

must_haves:
  truths:
    - "Po wyjściu z TRACKING pan servo wznawia sinusoidę płynnie — bez widocznego skoku w pierwszej klatce SCANNING"
    - "Twarz widoczna w oknie TARGET_LOST wymaga pełnych 3 kolejnych detekcji zanim system wejdzie w TRACKING"
    - "Skrypt uruchamia się bez AttributeError lub ValueError przy starcie"
  artifacts:
    - path: "src/modes/test_tracker.py"
      provides: "Phase offset dla sinusoidy + streak reset przy wejściu w TARGET_LOST"
      contains: "_scan_phase_offset"
  key_links:
    - from: "MaszynaStanow._przejdz_do()"
      to: "MaszynaStanow._skanuj()"
      via: "self._scan_phase_offset — obliczony raz przy STATE_SCANNING entry, używany w każdym ticku"
      pattern: "_scan_phase_offset"
    - from: "TestTracker.uruchom()"
      to: "DetekcjaTwarzy.resetuj_streak()"
      via: "warunek STATE_TARGET_LOST entry"
      pattern: "STATE_TARGET_LOST and poprzedni_stan != STATE_TARGET_LOST"
---

<objective>
Dwie chirurgiczne poprawki w `src/modes/test_tracker.py` eliminujące skok serwa przy powrocie z TRACKING do SCANNING (SCAN-01) oraz przedwczesne wejście w TRACKING podczas okna TARGET_LOST (SCAN-02).

Purpose: Zamknięcie fazy 8 — ostatnie dwie znane bugi w test_tracker.py. Po tych poprawkach cykl SCANNING↔TRACKING jest czysty bez artefaktów ruchowych i bez przedwczesnych przejść stanu.
Output: Zaktualizowany `src/modes/test_tracker.py` + checkpoint weryfikacji hardware na RPi4.
</objective>

<execution_context>
@/Users/mattparol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattparol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scanning-logic/08-CONTEXT.md
@.planning/phases/08-scanning-logic/08-RESEARCH.md

<interfaces>
<!-- Kluczowe fragmenty kodu z test_tracker.py — wyciągnięte bezpośrednio. Executor nie musi eksplorować kodu. -->

<!-- MaszynaStanow.__init__ (linia 202-215): BRAK _scan_phase_offset — trzeba dodać -->
```python
def __init__(self):
    self.hardware = PanTiltSystem()
    self.stan = config.STATE_SCANNING
    self.pid_pan = PID(config.PID_PAN_P, config.PID_PAN_I, config.PID_PAN_D,
                       setpoint=0, sample_time=0.033)
    self.pid_pan.output_limits = (-PID_OUTPUT_LIMIT, PID_OUTPUT_LIMIT)
    self.pid_tilt = PID(config.PID_TILT_P, config.PID_TILT_I, config.PID_TILT_D,
                        setpoint=0, sample_time=0.033)
    self.pid_tilt.output_limits = (-PID_OUTPUT_LIMIT, PID_OUTPUT_LIMIT)
    self._czas_ostatniego_celu = time.time()
```

<!-- MaszynaStanow._skanuj() (linia 255-259): pan bez offsetu — to jest bug -->
```python
def _skanuj(self) -> None:
    t = time.time()
    pan = SCAN_AMPLITUDE * math.sin(2.0 * math.pi * SCAN_FREQUENCY * t)
    self.hardware.set_angles(pan, 0.0)
```

<!-- MaszynaStanow._przejdz_do() (linia 282-289): reset PID przy SCANNING, brak phase offset -->
```python
def _przejdz_do(self, nowy_stan: str) -> None:
    logger.info(f"Zmiana stanu: {self.stan} → {nowy_stan}")
    self.stan = nowy_stan
    self._czas_ostatniego_celu = time.time()
    if nowy_stan == config.STATE_SCANNING:
        self.pid_pan.reset()
        self.pid_tilt.reset()
```

<!-- TestTracker.uruchom() streak reset (linia 334-337): aktualny — BŁĘDNY warunek -->
```python
# Reset streak przy powrocie do SCANNING
if stan == config.STATE_SCANNING and poprzedni_stan != config.STATE_SCANNING:
    self.detekcja.resetuj_streak()
poprzedni_stan = stan
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCAN-01 phase offset + SCAN-02 streak reset</name>
  <files>src/modes/test_tracker.py</files>
  <action>
Trzy precyzyjne zmiany w `src/modes/test_tracker.py`:

**Zmiana 1 — MaszynaStanow.__init__: dodaj `_scan_phase_offset`**
Po linii `self._czas_ostatniego_celu = time.time()` (linia ~215) dodaj:
```python
self._scan_phase_offset: float = 0.0
```
Inicjalizacja do 0.0 zapewnia, że pierwsza sesja SCANNING startuje od 0° (gdzie `smooth_move_to(0,0)` zostawia serwo).

**Zmiana 2 — MaszynaStanow._przejdz_do(): oblicz offset przy wejściu w SCANNING**
Wewnątrz bloku `if nowy_stan == config.STATE_SCANNING:` (po istniejącym PID reset) dodaj:
```python
        raw = self.hardware.pan_angle / SCAN_AMPLITUDE
        self._scan_phase_offset = math.asin(max(-1.0, min(1.0, raw)))
```
Clamp do [-1.0, 1.0] przed arcsin — zapobiega ValueError gdy pan > SCAN_AMPLITUDE (np. 55°/45° = 1.22).

**Zmiana 3 — MaszynaStanow._skanuj(): użyj phase offset**
Zmień linię z sinusoidą:
```python
# PRZED:
pan = SCAN_AMPLITUDE * math.sin(2.0 * math.pi * SCAN_FREQUENCY * t)
# PO:
pan = SCAN_AMPLITUDE * math.sin(2.0 * math.pi * SCAN_FREQUENCY * t + self._scan_phase_offset)
```

**Zmiana 4 — TestTracker.uruchom(): streak reset przy TARGET_LOST entry, nie SCANNING entry**
Zmień warunek resetuj_streak (linia ~334):
```python
# PRZED:
if stan == config.STATE_SCANNING and poprzedni_stan != config.STATE_SCANNING:
    self.detekcja.resetuj_streak()
# PO:
if stan == STATE_TARGET_LOST and poprzedni_stan != STATE_TARGET_LOST:
    self.detekcja.resetuj_streak()
```
`STATE_TARGET_LOST` jest już zaimportowany na poziomie modułu (linia 38) — nie wymaga dodatkowych importów.

NIE zmieniać: PID gains, SCAN_AMPLITUDE, SCAN_FREQUENCY, korekta_tilt negacji, YUV420 konwersji, AwbEnable.
  </action>
  <verify>
    <automated>cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE && python3 -c "
import ast, sys
with open('src/modes/test_tracker.py') as f:
    src = f.read()
tree = ast.parse(src)
assert '_scan_phase_offset' in src, 'BRAK: _scan_phase_offset'
assert 'math.asin' in src, 'BRAK: math.asin (phase offset calculation)'
assert '+ self._scan_phase_offset' in src, 'BRAK: phase offset w _skanuj()'
assert 'STATE_TARGET_LOST and poprzedni_stan != STATE_TARGET_LOST' in src, 'BRAK: streak reset przy TARGET_LOST entry'
assert 'STATE_SCANNING and poprzedni_stan != config.STATE_SCANNING' not in src, 'STARY warunek streak reset wciaz obecny'
print('OK — wszystkie 4 zmiany potwierdzone przez AST/grep')
"
</automated>
  </verify>
  <done>
    Cztery zmiany wprowadzone w test_tracker.py:
    1. `_scan_phase_offset = 0.0` w __init__
    2. Phase offset obliczany w _przejdz_do() przy STATE_SCANNING
    3. Offset dodany do argumentu sin w _skanuj()
    4. Warunek streak reset zmieniony z STATE_SCANNING entry na STATE_TARGET_LOST entry
    Skrypt startuje bez wyjątków (python3 -c import).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Dwie poprawki zachowania test_tracker.py na RPi4:
    - SCAN-01: sinusoida kontynuuje od aktualnej pozycji pana po wyjściu z TRACKING
    - SCAN-02: twarz podczas TARGET_LOST window wymaga 3 klatek przed TRACKING
  </what-built>
  <how-to-verify>
    Uruchom na RPi4: `python3 run_test_tracker.py`

    **Test SCAN-01 (brak skoku serwa):**
    1. Pokaż twarz kamerze — poczekaj na TRACKING (HUD: "TRACKING")
    2. Przesuń twarz na bok (pan servo nie jest w centrum)
    3. Zakryj twarz na ~2s — poczekaj na TARGET_LOST → SCANNING
    4. Obserwuj pan servo w pierwszej klatce SCANNING
    Oczekiwany wynik: servo wznawia sinusoidę bez widocznego skoku/snap do nowej pozycji. Ruch powinien być ciągły.

    **Test SCAN-02 (streak wymagany przez TARGET_LOST):**
    1. Pokaż twarz kamerze — poczekaj na TRACKING
    2. Zakryj twarz — poczekaj na TARGET_LOST (HUD: "TARGET_LOST")
    3. Natychmiast pokaż twarz na chwilę (1-2 klatki), potem zakryj
    4. Sprawdź czy HUD wszedł w TRACKING
    Oczekiwany wynik: krótkie pokazanie twarzy podczas TARGET_LOST NIE powinno powodować TRACKING. Dopiero 3 kolejne klatki z twarzą wyzwalają TRACKING.
  </how-to-verify>
  <resume-signal>Wpisz "approved" gdy oba testy przejdą, lub opisz zaobserwowany problem</resume-signal>
</task>

</tasks>

<verification>
Po wykonaniu obu tasków:
- `python3 -c "import src.modes.test_tracker"` — zero błędów importu
- Skrypt startuje bez AttributeError ('_scan_phase_offset' must exist) i bez ValueError (arcsin domain)
- Checkpoint hardware RPi4 zatwierdzony przez użytkownika
</verification>

<success_criteria>
1. Pan servo wznawia sinusoidę bez widocznego skoku po TRACKING→SCANNING — kryterium SCAN-01
2. Twarz widoczna przez 1-2 klatki podczas TARGET_LOST okna nie powoduje wejścia w TRACKING — kryterium SCAN-02
3. Skrypt uruchamia się bez wyjątków na RPi4 — brak regresji
</success_criteria>

<output>
Po zakończeniu utwórz `.planning/phases/08-scanning-logic/08-01-SUMMARY.md`
</output>
