---
phase: 09-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware.py
  - src/modes/test_tracker.py
autonomous: true
requirements: [DIAG-02, DIAG-03, DIAG-04]

must_haves:
  truths:
    - "HUD wyswietla czerwony napis [MOCK] w prawym gornym rogu gdy pigpiod nie jest aktywny"
    - "Terminal loguje jednolinijkowy PID komponent w kazdym ticku stanu TRACKING dla obu osi"
    - "Terminal loguje rzeczywiste ColourGains z capture_metadata() po set_controls — widoczne niezerowe wartosci R i B"
  artifacts:
    - path: "src/hardware.py"
      provides: "property mock_mode eksponujace _mock_mode jako publiczny atrybut"
      contains: "def mock_mode"
    - path: "src/modes/test_tracker.py"
      provides: "[MOCK] overlay w _rysuj_hud(), PID logging w _sledz(), AWB re-read w Picamera2Stream.start()"
      contains: "[MOCK]"
  key_links:
    - from: "src/hardware.py"
      to: "src/modes/test_tracker.py (_rysuj_hud)"
      via: "self.maszyna.hardware.mock_mode"
      pattern: "mock_mode"
    - from: "src/modes/test_tracker.py (_sledz)"
      to: "terminal"
      via: "logger.debug PAN err= P= I= D= out="
      pattern: "pid_pan.components"
    - from: "src/modes/test_tracker.py (Picamera2Stream.start)"
      to: "terminal"
      via: "logger.info ColourGains potwierdzone"
      pattern: "capture_metadata.*ColourGains"
---

<objective>
Dodanie trzech diagnostycznych usprawnien widocznosci przed uruchomieniem na RPi4: indykator mock mode na HUD, logowanie komponentow PID per-tick w TRACKING, oraz weryfikacja ColourGains z re-read po AWB set_controls.

Zero zmian w logice sterowania — wylacznie obserwabilnosc.

Purpose: Operator musi widziec natychmiast czy serwa sa mock, jakie sa korekty PID, i czy AWB gains faktycznie zostaly zaakceptowane przez sensor.
Output: Zmodyfikowany src/hardware.py (mock_mode property) i src/modes/test_tracker.py ([MOCK] HUD + PID logging + AWB re-read).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-diagnostics/09-CONTEXT.md

<interfaces>
<!-- Kluczowe fragmenty kodu — executor nie musi przeszukiwac codebase. -->

Z src/hardware.py — _mock_mode (linie 25-39):
```python
self._mock_mode = not PIGPIO_AVAILABLE

if not self._mock_mode:
    try:
        factory = PiGPIOFactory()
        ...
    except Exception as e:
        ...
        self._mock_mode = True

if self._mock_mode:
    logger.info("Uruchamiono obsluge serw w srodowisku okrojonym MOCK (bez pigpio/win32).")
```
Brak publicznego property — trzeba dodac `mock_mode` (per D-02). Wstaw po bloku if self._mock_mode w __init__, przed def set_angles.

Z src/modes/test_tracker.py — _rysuj_hud koniec (linie 394-398):
```python
        # FPS (prawy-dolny rog, szary)
        fps_tekst = f"FPS:{self._fps_aktualny:.1f}"
        (tw, th), _ = cv2.getTextSize(fps_tekst, cv2.FONT_HERSHEY_SIMPLEX, 0.5, 1)
        cv2.putText(klatka, fps_tekst, (w - tw - 5, h - 10),
                    cv2.FONT_HERSHEY_SIMPLEX, 0.5, (150, 150, 150), 1)
        # <- tu koniec funkcji, dodac [MOCK] po FPS bloku
```
Dostep do hardware przez `self.maszyna.hardware` z poziomu TestTracker._rysuj_hud().

Z src/modes/test_tracker.py — _sledz (linie 274-281):
```python
        korekta_pan = -self.pid_pan(blad_pan)    # po tym pid_pan.components dostepne
        korekta_tilt = -self.pid_tilt(blad_tilt)  # po tym pid_tilt.components dostepne

        nowy_pan = self.hardware.pan_angle + korekta_pan
        nowy_tilt = self.hardware.tilt_angle + korekta_tilt
        self.hardware.set_angles(nowy_pan, nowy_tilt)
        # <- tu dodac logger.debug z komponentami PID
```
simple-pid >= 2.0: `pid.components` to tuple `(P, I, D)` dostepne PO wywolaniu `pid(error)`.

Z src/modes/test_tracker.py — AWB warm-up (linie 78-87):
```python
        logger.info("Czekam na stabilizację AWB (2s)...")
        time.sleep(2.0)
        metadata = self._picam2.capture_metadata()
        gains = metadata.get("ColourGains")
        if gains is None:
            logger.warning("ColourGains niedostępne, używam fallback (2.5, 1.9)")
            gains = AWB_FALLBACK_GAINS
        self._picam2.set_controls({"ColourGains": gains})
        r, b = gains
        logger.info(f"ColourGains zablokowane: (R={r:.2f}, B={b:.2f})")
        # <- po tej linii dodac re-read i log potwierdzenia (per D-06, D-07)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eksponuj mock_mode jako property w hardware.py</name>
  <files>src/hardware.py</files>
  <action>
Dodaj publiczny property `mock_mode` do klasy `PanTiltSystem`.

Wstaw bezposrednio po bloku `if self._mock_mode:` w `__init__` (po linii 39, przed def set_angles):

```python
    @property
    def mock_mode(self) -> bool:
        """Zwraca True gdy serwa sa w trybie symulacji (brak pigpiod)."""
        return self._mock_mode
```

Nie zmieniac zadnego innego kodu — tylko dodac property. Property jest potrzebne bo _rysuj_hud() w test_tracker.py musi wiedziec o stanie mock bez naruszania enkapsulacji (per D-02).
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "from src.hardware import PanTiltSystem; h = PanTiltSystem(); assert isinstance(h.mock_mode, bool); print('OK mock_mode:', h.mock_mode)"</automated>
  </verify>
  <done>h.mock_mode zwraca bool bez AttributeError. Na systemie bez pigpiod zwraca True.</done>
</task>

<task type="auto">
  <name>Task 2: [MOCK] overlay na HUD, PID component logging, AWB gains re-read</name>
  <files>src/modes/test_tracker.py</files>
  <action>
Trzy addytywne zmiany w test_tracker.py. Zero zmian w logice sterowania.

**Zmiana A — [MOCK] overlay w _rysuj_hud() (per D-01):**

Na koncu metody `_rysuj_hud` w klasie `TestTracker`, PO bloku FPS (po ostatnim cv2.putText z fps_tekst), dodaj:

```python
        # Indykator mock mode — czerwony [MOCK] w prawym gornym rogu (per D-01)
        if self.maszyna.hardware.mock_mode:
            mock_tekst = "[MOCK]"
            (mw, mh), _ = cv2.getTextSize(mock_tekst, cv2.FONT_HERSHEY_SIMPLEX, 0.6, 2)
            cv2.putText(klatka, mock_tekst, (w - mw - 5, mh + 5),
                        cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 0, 255), 2)
```

Kolor `(0, 0, 255)` = czysty czerwony BGR. Pozycja prawy-gorny rog: x = w - szerokosc_tekstu - 5, y = wysokosc_tekstu + 5.

**Zmiana B — PID component logging w _sledz() (per D-03, D-04, D-05):**

W metodzie `_sledz` klasy `MaszynaStanow`, PO linii `self.hardware.set_angles(nowy_pan, nowy_tilt)` (ostatnia linia metody), dodaj:

```python
        # PID diagnostyka per-tick (per D-03) — widoczne z logging.DEBUG
        p_pan, i_pan, d_pan = self.pid_pan.components
        p_tilt, i_tilt, d_tilt = self.pid_tilt.components
        logger.debug(
            "PAN err=%+.1f P=%.3f I=%.3f D=%.3f out=%+.1f | "
            "TILT err=%+.1f P=%.3f I=%.3f D=%.3f out=%+.1f",
            blad_pan, p_pan, i_pan, d_pan, korekta_pan,
            blad_tilt, p_tilt, i_tilt, d_tilt, korekta_tilt
        )
```

Uzyj logger.debug (nie info) — per-tick logi nie zasmiecaja normalnego INFO streamu. Uzytkownik uruchamia z `logging.basicConfig(level=logging.DEBUG)` w run_test_tracker.py gdy potrzebuje danych diagnostycznych.

**Zmiana C — AWB gains re-read po set_controls (per D-06, D-07):**

W metodzie `start` klasy `Picamera2Stream`, PO linii `logger.info(f"ColourGains zablokowane: (R={r:.2f}, B={b:.2f})")`, dodaj:

```python
        # Weryfikacja czy gains faktycznie sie ustawily — wykrywa silent failure (per D-06)
        meta_po = self._picam2.capture_metadata()
        gains_po = meta_po.get("ColourGains")
        if gains_po is not None:
            r_po, b_po = gains_po
            logger.info(f"ColourGains potwierdzone z sensora: (R={r_po:.2f}, B={b_po:.2f})")
            if abs(r_po - r) > 0.1 or abs(b_po - b) > 0.1:
                logger.warning(
                    f"AWB gains roznia sie od zadanych: zadane=(R={r:.2f}, B={b:.2f}), "
                    f"rzeczywiste=(R={r_po:.2f}, B={b_po:.2f})"
                )
        else:
            logger.warning("ColourGains niedostepne w re-read metadata po set_controls")
```

Tolerancja 0.1 na roznice gains — roznice ponizej tej wartosci sa szumem pomiaru, powyzej sugeruja silent failure AWB.
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "
import logging, io
from src.hardware import PanTiltSystem
from src.modes.test_tracker import MaszynaStanow

# Sprawdz [MOCK] property dostepne z hardware
h = PanTiltSystem()
assert h.mock_mode == True, 'mock_mode powinno byc True bez pigpiod'

# Sprawdz PID logging — wywolaj _sledz z syntetycznym bbox
m = MaszynaStanow()
buf = io.StringIO()
handler = logging.StreamHandler(buf)
handler.setLevel(logging.DEBUG)
logging.getLogger('src.modes.test_tracker').addHandler(handler)
logging.getLogger('src.modes.test_tracker').setLevel(logging.DEBUG)
m._sledz((100, 80, 40, 40), 320, 240)
output = buf.getvalue()
assert 'PAN err=' in output, f'Brak PID log PAN: repr={repr(output)}'
assert 'TILT err=' in output, f'Brak PID log TILT: repr={repr(output)}'
print('OK: mock_mode property + PID logging zweryfikowane')
print('LOG output:', output.strip())
"</automated>
  </verify>
  <done>
1. mock_mode property zwraca True w srodowisku bez pigpiod — brak AttributeError.
2. Wywolanie _sledz() generuje linie DEBUG z "PAN err=" i "TILT err=" — potwierdzone przez assert na output loggera.
3. Zmiany A, B, C sa w pliku — grep "[MOCK]", "pid_pan.components", "ColourGains potwierdzone" zwraca wyniki.
  </done>
</task>

</tasks>

<verification>
Weryfikacja calego planu po obu taskach:

```bash
cd /home/parolisko/ARIES-LITE

# 1. Sprawdz ze property istnieje w hardware.py
grep -n "def mock_mode" src/hardware.py

# 2. Sprawdz ze [MOCK] overlay jest w test_tracker.py
grep -n "\[MOCK\]" src/modes/test_tracker.py

# 3. Sprawdz ze PID components logging jest w _sledz
grep -n "pid_pan.components" src/modes/test_tracker.py

# 4. Sprawdz ze AWB re-read jest po set_controls
grep -n "ColourGains potwierdzone" src/modes/test_tracker.py

# 5. Import test — nie moze rzucac wyjatku
python3 -c "from src.hardware import PanTiltSystem; from src.modes.test_tracker import MaszynaStanow; print('Import OK')"
```
</verification>

<success_criteria>
1. DIAG-02: `grep "[MOCK]" src/modes/test_tracker.py` zwraca wyniki + `(0, 0, 255)` kolor — czerwony overlay na HUD
2. DIAG-03: `grep "pid_pan.components" src/modes/test_tracker.py` zwraca wyniki + assert na logger output w _sledz() przechodzi
3. DIAG-04: `grep "ColourGains potwierdzone" src/modes/test_tracker.py` zwraca wyniki — re-read po set_controls jest w kodzie
4. Zero regresji: `python3 -c "from src.modes.test_tracker import MaszynaStanow"` nie rzuca wyjatku
</success_criteria>

<output>
Po ukonczeniu utworz `.planning/phases/09-diagnostics/09-diagnostics-01-SUMMARY.md` z:
- Lista zmian (property mock_mode, [MOCK] overlay, PID logging, AWB re-read)
- Artefakty: src/hardware.py, src/modes/test_tracker.py
- Decyzje implementacyjne: logger.debug dla PID per-tick, tolerancja 0.1 dla AWB gains diff
- Status wymagan: DIAG-02 done, DIAG-03 done, DIAG-04 done
</output>
