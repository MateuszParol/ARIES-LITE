---
phase: 11-awb-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modes/test_tracker.py
autonomous: false
requirements:
  - AWB-01
  - AWB-02

must_haves:
  truths:
    - "Pierwsza klatka wideo nie ma blue tint — kolory neutralne od startu"
    - "Log zawiera 'ColourGains zablokowane' z niezerowymi wartosciami R i B"
    - "Aplikacja nie crasha gdy capture_metadata() zwraca None lub (0.0, 0.0)"
  artifacts:
    - path: "src/modes/test_tracker.py"
      provides: "AWB configure-time lock + post-start re-lock z fallback guard"
      contains: "AWB_FALLBACK_GAINS"
  key_links:
    - from: "src/modes/test_tracker.py"
      to: "Picamera2 ISP"
      via: "controls dict in create_video_configuration()"
      pattern: "create_video_configuration.*controls.*ColourGains"
    - from: "src/modes/test_tracker.py"
      to: "Picamera2 ISP"
      via: "set_controls post-start re-lock"
      pattern: "set_controls.*ColourGains.*float"
---

<objective>
Naprawic AWB w test_tracker.py — neutralne kolory od pierwszej klatki, brak crash przy None/0.0 gains.

Purpose: Eliminacja blue tint od frame 1 przez configure-time ColourGains lock + bezpieczny fallback guard w post-start re-lock.
Output: Zmodyfikowany src/modes/test_tracker.py z trzema zsynchronizowanymi zmianami w Picamera2Stream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-awb-fix/11-CONTEXT.md
@.planning/phases/11-awb-fix/11-RESEARCH.md

<interfaces>
<!-- Kluczowe linie w src/modes/test_tracker.py ktore executor musi znac -->

Linia 35 (stala — wykomentowana):
```python
#AWB_FALLBACK_GAINS = (1.0, 1.0)  # (Red, Blue) — neutralne, bez wzmocnienia
```

Linie 67-69 (create_video_configuration — bez controls):
```python
video_config = self._picam2.create_video_configuration(
    lores={"size": (self._width, self._height), "format": "YUV420"}
)
```

Linie 81-87 (post-start AWB block — guard wykomentowany, brak float cast):
```python
gains = metadata.get("ColourGains")
#if gains is None:
#    logger.warning("ColourGains niedostępne, używam fallback (2.5, 1.9)")
#    gains = AWB_FALLBACK_GAINS
self._picam2.set_controls({"ColourGains": gains})
r, b = gains
logger.info(f"ColourGains zablokowane: (R={r:.2f}, B={b:.2f})")
```

Linie 89-101 (re-read verification block z Phase 9 — BEZ ZMIAN):
```python
meta_po = self._picam2.capture_metadata()
gains_po = meta_po.get("ColourGains")
# ... verification logic ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AWB configure-time lock + fallback guard</name>
  <files>src/modes/test_tracker.py</files>
  <read_first>
    - src/modes/test_tracker.py (caly plik — obecny stan AWB block, linie 35, 67-69, 81-87)
    - .planning/phases/11-awb-fix/11-RESEARCH.md (sekcja "Zmiany wzgledem obecnego kodu (diff view)" — dokladny opis zmian)
  </read_first>
  <action>
Trzy zsynchronizowane zmiany w jednym pliku (per D-01 through D-07):

**Zmiana 1 — Linia 35: Odkomentowac AWB_FALLBACK_GAINS (per D-03, D-07)**
```python
# PRZED:
#AWB_FALLBACK_GAINS = (1.0, 1.0)  # (Red, Blue) — neutralne, bez wzmocnienia

# PO:
AWB_FALLBACK_GAINS = (1.0, 1.0)  # (Red, Blue) — neutralne, bez wzmocnienia
```
Usunac `#` na poczatku linii. Wartosc `(1.0, 1.0)` jest poprawna — NIE zmieniac na `(2.5, 1.9)` ani na `(0.0, 0.0)`.

**Zmiana 2 — Linie 67-69: Dodac controls do create_video_configuration() (per D-01, AWB-01)**
```python
# PRZED:
video_config = self._picam2.create_video_configuration(
    lores={"size": (self._width, self._height), "format": "YUV420"}
)

# PO:
video_config = self._picam2.create_video_configuration(
    lores={"size": (self._width, self._height), "format": "YUV420"},
    controls={"ColourGains": AWB_FALLBACK_GAINS}
)
```
Dodac przecinek po zamykajacym `}` parametru `lores`, nowa linia z `controls={"ColourGains": AWB_FALLBACK_GAINS}`.

**Zmiana 3 — Linie 81-87: Przywrocic i rozszerzyc fallback guard (per D-05, D-06, D-07)**
```python
# PRZED (linie 81-87):
gains = metadata.get("ColourGains")
#if gains is None:
#    logger.warning("ColourGains niedostępne, używam fallback (2.5, 1.9)")
#    gains = AWB_FALLBACK_GAINS
self._picam2.set_controls({"ColourGains": gains})
r, b = gains
logger.info(f"ColourGains zablokowane: (R={r:.2f}, B={b:.2f})")

# PO:
gains = metadata.get("ColourGains")
if gains is None or gains == (0.0, 0.0):
    logger.warning("AWB still running lub ColourGains niedostepne, uzywam fallback (1.0, 1.0)")
    gains = AWB_FALLBACK_GAINS
self._picam2.set_controls({"ColourGains": (float(gains[0]), float(gains[1]))})
r, b = gains
logger.info(f"ColourGains zablokowane: (R={r:.2f}, B={b:.2f})")
```
Kluczowe roznice: (1) guard odkomentowany i rozszerzony o `(0.0, 0.0)` check, (2) wartosc fallback zmieniona z `(2.5, 1.9)` na odwolanie do stalej `AWB_FALLBACK_GAINS`, (3) explicit `float()` cast w `set_controls()` — zapobiega TypeError na niektorych wersjach Picamera2.

**NIE modyfikowac** linii 89-101 (re-read verification block z Phase 9 — pozostaje bez zmian per D-02).
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "import ast; ast.parse(open('src/modes/test_tracker.py').read()); print('SYNTAX OK')" && grep -n "^AWB_FALLBACK_GAINS = (1.0, 1.0)" src/modes/test_tracker.py && grep -n "controls=.*ColourGains.*AWB_FALLBACK_GAINS" src/modes/test_tracker.py && grep -n "gains is None or gains == (0.0, 0.0)" src/modes/test_tracker.py && grep -n "float(gains\[0\]).*float(gains\[1\])" src/modes/test_tracker.py && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - src/modes/test_tracker.py zawiera `AWB_FALLBACK_GAINS = (1.0, 1.0)` (odkomentowane, linia ~35)
    - src/modes/test_tracker.py zawiera `controls={"ColourGains": AWB_FALLBACK_GAINS}` w create_video_configuration()
    - src/modes/test_tracker.py zawiera `if gains is None or gains == (0.0, 0.0):` (guard na oba przypadki)
    - src/modes/test_tracker.py zawiera `(float(gains[0]), float(gains[1]))` w set_controls() (explicit float cast)
    - src/modes/test_tracker.py zawiera `AWB_FALLBACK_GAINS` w linii fallback (nie hardcoded (2.5, 1.9))
    - Linie 89-101 (re-read block) nie sa zmodyfikowane
    - `python3 -c "import ast; ast.parse(open('src/modes/test_tracker.py').read())"` konczy sie sukcesem (brak bledow skladni)
  </acceptance_criteria>
  <done>AWB_FALLBACK_GAINS odkomentowane z wartoscia (1.0, 1.0), configure-time lock aktywny, fallback guard obsluguje None i (0.0, 0.0), set_controls uzywa explicit float cast</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Weryfikacja wizualna AWB na RPi4</name>
  <files>src/modes/test_tracker.py</files>
  <action>
Weryfikacja wizualna i logowa zmian AWB na rzeczywistym hardware RPi4. Executor uruchamia `python3 run_test_tracker.py` i prezentuje uzytkownikowi logi startowe do weryfikacji.
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && grep -n "ColourGains.*AWB_FALLBACK_GAINS" src/modes/test_tracker.py | wc -l | grep -q "[2-9]" && echo "CONFIGURE-TIME + FALLBACK BOTH PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - Log startowy zawiera `ColourGains zablokowane: (R=X.XX, B=X.XX)` z X.XX > 0.0
    - Brak blue tint od pierwszej klatki (wizualna weryfikacja uzytkownika)
    - Uzytkownik wpisal "approved"
  </acceptance_criteria>
  <done>Uzytkownik potwierdza naturalne kolory od pierwszej klatki i poprawne logi AWB</done>
</task>

</tasks>

<verification>
Weryfikacja calej fazy:
1. `grep -n "AWB_FALLBACK_GAINS = (1.0, 1.0)" src/modes/test_tracker.py` — stala odkomentowana
2. `grep -n "controls=.*ColourGains" src/modes/test_tracker.py` — configure-time lock
3. `grep -n "gains is None or gains == (0.0, 0.0)" src/modes/test_tracker.py` — guard na oba przypadki
4. `grep -n "float(gains" src/modes/test_tracker.py` — explicit float cast
5. Wizualnie: brak blue tint od pierwszej klatki na RPi4
</verification>

<success_criteria>
1. AWB-01: ColourGains ustawione w create_video_configuration() — neutralne kolory od frame 1
2. AWB-02: Brak blue tint — skora wyglada naturalnie w normalnym oswietleniu
3. Aplikacja nie crasha przy None lub (0.0, 0.0) z capture_metadata()
4. Re-read verification block (Phase 9) dziala bez zmian
</success_criteria>

<output>
After completion, create `.planning/phases/11-awb-fix/11-01-SUMMARY.md`
</output>
