---
phase: 18-srodowisko-protokol-migracja
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - requirements-v2.txt
autonomous: false
requirements: [ENV-01, ENV-02]
user_setup:
  - service: pyenv
    why: "Python 3.12 wymagany przez mediapipe — system ma tylko Python 3.13"
    env_vars: []
    dashboard_config:
      - task: "pyenv install 3.12.10 trwa ~15-20 minut na RPi4 — kompilacja ze zrodla"
        location: "Terminal RPi4"

must_haves:
  truths:
    - "import mediapipe dziala w Python 3.12 venv na RPi4 bez bledow"
    - "arduino-cli kompiluje szkielet firmware z QuickPID, Servo i LiquidCrystal bez bledow"
    - "FaceDetector (Tasks API) jest dostepny w zainstalowanym mediapipe"
  artifacts:
    - path: "venv/"
      provides: "Python 3.12 venv z mediapipe, pyserial, numpy"
      contains: "python3.12"
    - path: "requirements-v2.txt"
      provides: "Zaleznosci Python v2.0"
      contains: "mediapipe==0.10.18"
  key_links:
    - from: "venv/bin/python3"
      to: "mediapipe"
      via: "pip install w aktywnym venv"
      pattern: "import mediapipe"
    - from: "arduino-cli"
      to: "src/arduino/aries_controller/aries_controller.ino"
      via: "arduino-cli compile --fqbn arduino:avr:leonardo"
      pattern: "Sketch uses"
---

<objective>
Weryfikacja empiryczna srodowiska deweloperskiego — Python 3.12 z MediaPipe na RPi4 i arduino-cli z kompilacja szkieletu firmware.

Purpose: ENV-01 wymaga dzialajacego `import mediapipe` na RPi4 (empiryczny dowod). ENV-02 wymaga kompilacji szkieletu .ino z QuickPID/Servo/LiquidCrystal. Ten plan zamyka obie weryfikacje srodowiskowe.
Output: Dzialajacy venv Python 3.12 z mediapipe, zainstalowany arduino-cli z potwierdzona kompilacja, requirements-v2.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-srodowisko-protokol-migracja/18-CONTEXT.md
@.planning/phases/18-srodowisko-protokol-migracja/18-RESEARCH.md
@.planning/phases/18-srodowisko-protokol-migracja/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalacja Python 3.12 przez pyenv + venv z mediapipe</name>
  <files>requirements-v2.txt</files>
  <read_first>
    - .planning/phases/18-srodowisko-protokol-migracja/18-RESEARCH.md (sekcja "Instalacja — Python 3.12 przez pyenv", "Pitfall 1", "Pitfall 2", "Open Questions")
    - .planning/phases/18-srodowisko-protokol-migracja/18-CONTEXT.md (decyzje D-10, D-11, D-12)
  </read_first>
  <action>
Instalacja Python 3.12 przez pyenv i weryfikacja mediapipe (per D-10, D-11, D-12).

**WAZNE:** Ten system dziala na Debian Trixie z Python 3.13.5. MediaPipe NIE ma wheelu cp313 dla linux aarch64. Wymagany jest Python 3.12 przez pyenv.

Krok 1: Sprawdz czy pyenv jest juz zainstalowane:
```bash
pyenv --version 2>/dev/null || echo "PYENV_NOT_INSTALLED"
```

Krok 2: Jesli pyenv nie zainstalowane — zainstaluj:
```bash
curl https://pyenv.run | bash
```
Dodaj do `~/.bashrc` (jesli nie ma):
```bash
export PYENV_ROOT="$HOME/.pyenv"
[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"
```
Przeladuj: `source ~/.bashrc`

Krok 3: Zainstaluj zaleznosci kompilacji Python (jesli brakuje):
```bash
sudo apt install -y libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev \
  libncursesw5-dev xz-utils tk-dev libxml2-dev libxmlsec1-dev libffi-dev liblzma-dev
```

Krok 4: Skompiluj Python 3.12 (~15-20 minut na RPi4):
```bash
pyenv install 3.12.10
```
Jesli 3.12.10 nie jest dostepne, uzyj najnowszego 3.12.x: `pyenv install --list | grep "3.12"` i wybierz najwyzsza wersje.

Krok 5: Usun stary venv jesli istnieje, stworz nowy z Python 3.12 (per D-11):
```bash
cd /home/parolisko/ARIES-LITE
rm -rf venv
$HOME/.pyenv/versions/3.12.*/bin/python3.12 -m venv venv --system-site-packages
```

Krok 6: Zainstaluj zaleznosci v2.0 (per D-12):
```bash
source venv/bin/activate
pip install mediapipe==0.10.18 pyserial numpy
```
Jesli `mediapipe==0.10.18` fail — sprobuj `pip install mediapipe` (najnowsza dostepna wersja z cp312 wheel).

Krok 7: Weryfikacja empiryczna ENV-01:
```bash
source venv/bin/activate
python3 -c "import mediapipe as mp; print('mediapipe', mp.__version__)"
python3 -c "from mediapipe.tasks.python.vision import FaceDetector; print('FaceDetector dostepny — ENV-01 PASS')"
python3 -c "import serial; print('pyserial OK')"
python3 -c "import numpy; print('numpy', numpy.__version__)"
```

Krok 8: Sprawdz picamera2 (Open Question z RESEARCH.md):
```bash
source venv/bin/activate
python3 -c "import picamera2; print('picamera2 OK')" 2>&1
```
Jesli picamera2 NIE importuje sie pod Python 3.12 — zaloguj wynik i kontynuuj. Picamera2 jest potrzebna dopiero w Phase 21; rozwiazanie tego problemu jest odlozone.

Krok 9: Utworz `requirements-v2.txt` w root:
```
# ARIES-LITE v2.0 — Python 3.12 na RPi4
# Instalacja: pip install -r requirements-v2.txt
mediapipe==0.10.18
pyserial>=3.5
numpy>=1.26
# picamera2 — zainstalowane systemowo przez apt (python3-picamera2)
# opencv-python-headless — zainstalowane systemowo lub pip install jesli brakuje
```

Commit: `feat(18): Python 3.12 venv with mediapipe verified + requirements-v2.txt`
  </action>
  <verify>
    <automated>source /home/parolisko/ARIES-LITE/venv/bin/activate && python3 -c "import mediapipe; print(mediapipe.__version__)" && python3 -c "from mediapipe.tasks.python.vision import FaceDetector; print('ENV-01 PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `venv/bin/python3 --version` zwraca Python 3.12.x (nie 3.13)
    - `source venv/bin/activate && python3 -c "import mediapipe"` konczy sie bez bledu
    - `source venv/bin/activate && python3 -c "from mediapipe.tasks.python.vision import FaceDetector"` konczy sie bez bledu
    - `requirements-v2.txt` istnieje w root i zawiera `mediapipe==0.10.18`
    - `requirements-v2.txt` zawiera `pyserial>=3.5`
  </acceptance_criteria>
  <done>Python 3.12 venv z mediapipe 0.10.18, pyserial i numpy zainstalowane i zweryfikowane empirycznie. FaceDetector (Tasks API) importowalny. requirements-v2.txt zapisany.</done>
</task>

<task type="auto">
  <name>Task 2: Instalacja arduino-cli + weryfikacja kompilacji szkieletu</name>
  <files><!-- Brak plikow repo — narzedzie systemowe. Tworzy ~/.arduino15/ i instaluje ~/.local/share/arduino-cli/ --></files>
  <read_first>
    - .planning/phases/18-srodowisko-protokol-migracja/18-RESEARCH.md (sekcja "Instalacja — arduino-cli", "Pitfall 4", "Pitfall 5", "Pattern 2: Szkielet .ino")
    - .planning/phases/18-srodowisko-protokol-migracja/18-CONTEXT.md (decyzje D-13, D-14, D-15)
    - src/arduino/aries_controller/aries_controller.ino (szkielet utworzony w Plan 01 Task 3)
  </read_first>
  <action>
Instalacja arduino-cli i weryfikacja kompilacji szkieletu firmware (per D-13, D-15).

Krok 1: Sprawdz czy arduino-cli jest juz zainstalowane:
```bash
arduino-cli version 2>/dev/null || echo "ARDUINO_CLI_NOT_INSTALLED"
```

Krok 2: Jesli nie zainstalowane — zainstaluj binarnie (per D-13, Pitfall 4):
```bash
curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | BINDIR=/usr/local/bin sudo sh
```
UWAGA: Uzyj `sudo` dla BINDIR=/usr/local/bin (wymaga uprawnien zapisu).

Krok 3: Zainicjalizuj konfiguracje i zainstaluj rdzen AVR (per Pitfall 5 — kolejnosc ma znaczenie):
```bash
arduino-cli config init
arduino-cli core update-index
arduino-cli core install arduino:avr
```

Krok 4: Zainstaluj biblioteke QuickPID (per D-15):
```bash
arduino-cli lib update-index
arduino-cli lib install "QuickPID"
```
Servo i LiquidCrystal sa wbudowane w arduino:avr — nie wymagaja osobnej instalacji.

Krok 5: Weryfikacja kompilacji szkieletu (per ENV-02):
```bash
arduino-cli compile \
  --fqbn arduino:avr:leonardo \
  src/arduino/aries_controller/aries_controller.ino
```
Oczekiwany wynik: `Sketch uses X bytes (Y%)...` bez bledow kompilacji.

Krok 6: Weryfikacja zainstalowanych bibliotek:
```bash
arduino-cli lib list | grep -E "QuickPID|Servo|LiquidCrystal"
```
Oczekiwany wynik: QuickPID 3.1.x widoczna na liscie.

NIE uploaduj firmware na Arduino (moze nie byc podlaczone). ENV-02 wymaga tylko kompilacji.

Commit: `feat(18): arduino-cli installed, firmware skeleton compiles`
  </action>
  <verify>
    <automated>arduino-cli compile --fqbn arduino:avr:leonardo /home/parolisko/ARIES-LITE/src/arduino/aries_controller/aries_controller.ino 2>&1 | grep -c "Sketch uses"</automated>
  </verify>
  <acceptance_criteria>
    - `arduino-cli version` zwraca wersje (v1.4.x lub nowsza)
    - `arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller/aries_controller.ino` konczy sie sukcesem (exit code 0)
    - Output kompilacji zawiera "Sketch uses" (potwierdzenie udanej kompilacji)
    - `arduino-cli lib list` zawiera "QuickPID"
    - Brak bledow `#include` w outputcie kompilacji
  </acceptance_criteria>
  <done>arduino-cli zainstalowane, rdzen arduino:avr i QuickPID zainstalowane. Szkielet aries_controller.ino kompiluje sie bez bledow dla Arduino Leonardo (fqbn arduino:avr:leonardo).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Weryfikacja srodowiska deweloperskiego przez uzytkownika</name>
  <files></files>
  <action>
Checkpoint: uzytkownik weryfikuje ze srodowisko deweloperskie v2.0 dziala poprawnie na RPi4.

Zbudowane:
1. Python 3.12 venv z mediapipe 0.10.18 (import + FaceDetector zweryfikowane)
2. arduino-cli z kompilacja szkieletu firmware (QuickPID + Servo + LiquidCrystal)
3. requirements-v2.txt z nowymi zaleznosciami

Jak zweryfikowac:
1. Uruchom w terminalu RPi4:
   ```bash
   cd /home/parolisko/ARIES-LITE
   source venv/bin/activate
   python3 --version  # Powinno byc 3.12.x
   python3 -c "import mediapipe; print(mediapipe.__version__)"  # Powinno byc 0.10.18
   ```
2. Sprawdz kompilacje Arduino:
   ```bash
   arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller/aries_controller.ino
   ```
   Powinno wyswietlic "Sketch uses X bytes" bez bledow.
3. Sprawdz picamera2 (opcjonalnie):
   ```bash
   source venv/bin/activate
   python3 -c "import picamera2; print('OK')"
   ```
   Jesli fail — to znany problem (Python 3.12 vs systemowe 3.13 picamera2) — rozwiazanie w Phase 21.

Wpisz "approved" jesli mediapipe importuje sie poprawnie i arduino-cli kompiluje szkielet. Jesli cos nie dziala — opisz blad.
  </action>
  <verify>Uzytkownik potwierdza "approved"</verify>
  <done>Uzytkownik zweryfikowal dzialanie mediapipe i arduino-cli na RPi4</done>
</task>

</tasks>

<verification>
- `source venv/bin/activate && python3 -c "import mediapipe"` — ENV-01 pass
- `arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller/aries_controller.ino` — ENV-02 pass
- `cat requirements-v2.txt | grep mediapipe` — plik zaleznosci istnieje
- `venv/bin/python3 --version` — Python 3.12.x (nie 3.13)
</verification>

<success_criteria>
1. `import mediapipe` dziala na RPi4 w Python 3.12 venv bez bledow (ENV-01)
2. `FaceDetector` z mediapipe Tasks API jest importowalny (ENV-01)
3. arduino-cli kompiluje szkielet firmware z QuickPID, Servo, LiquidCrystal bez bledow (ENV-02)
4. requirements-v2.txt zapisany z nowymi zaleznosciami v2.0
</success_criteria>

<output>
After completion, create `.planning/phases/18-srodowisko-protokol-migracja/18-02-SUMMARY.md`
</output>
