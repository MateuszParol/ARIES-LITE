---
phase: 15-tilt-servo-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: false
requirements:
  - TRK-01
  - TRK-02
  - TRK-03

must_haves:
  truths:
    - "Po wejsciu w TRACKING serwa nie docieraja do limitow katowych (±60 pan / ±30 tilt) w ciagu pierwszych 2 sekund"
    - "Twarz zostaje wycentrowana w obu osiach w ciagu 1-3 sekund od wejscia w TRACKING — error_x i error_y maleja do <0.15"
    - "Brak ciaglych ostrzezen CLAMP w logach terminala po wejsciu w TRACKING"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Zredukowany OUTPUT_LIMIT 3.0 stopni/tick"
      contains: "#define OUTPUT_LIMIT    3.0f"
  key_links:
    - from: "aries_controller.ino:OUTPUT_LIMIT"
      to: "QuickPID SetOutputLimits"
      via: "_inicjalizuj_pid_parametry() linie 348-349"
      pattern: "SetOutputLimits.*OUTPUT_LIMIT"
---

<objective>
Redukcja agresji PID przy wejsciu w SLEDZENIE — zmiana OUTPUT_LIMIT z 5.0 na 3.0 stopni/tick + weryfikacja sprzetowa na Uno R4 WiFi z analiza logow SD.

Purpose: Serwa nie uciekaja do limitow katowych po rozpoczeciu sledzenia twarzy — PID konwerguje plynnie zamiast szarpac.
Output: Firmware z OUTPUT_LIMIT=3.0f zflashowany na Uno R4 WiFi, potwierdzona konwergencja PID przez logi CSV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tilt-servo-fix/15-CONTEXT.md

@src/arduino/aries_controller/aries_controller.ino

<interfaces>
<!-- Jedyny punkt zmiany: linia 25 -->
#define OUTPUT_LIMIT    5.0f      // +/-5 stopni/tick — D-02

<!-- Uzycie w _inicjalizuj_pid_parametry(): -->
_pid_pan.SetOutputLimits(-OUTPUT_LIMIT, OUTPUT_LIMIT);    // linia 348
_pid_tilt.SetOutputLimits(-OUTPUT_LIMIT, OUTPUT_LIMIT);   // linia 349

<!-- pid_reset() juz wywolywany przy SLEDZENIE (linia 705): -->
_serwa.pid_reset();           // reset PID przy wejsciu w SLEDZENIE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zmiana OUTPUT_LIMIT 5.0f na 3.0f</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (linia 25: aktualny OUTPUT_LIMIT, linie 348-349: SetOutputLimits)
  </read_first>
  <action>
Zmien jedna linie w src/arduino/aries_controller/aries_controller.ino:

Linia 25 — PRZED:
```
#define OUTPUT_LIMIT    5.0f      // +/-5 stopni/tick — D-02
```

Linia 25 — PO (per D-01):
```
#define OUTPUT_LIMIT    3.0f      // +/-3 stopni/tick — redukcja agresji PID (D-01, TRK-01)
```

To jedyna zmiana w kodzie. Reszta (pid_reset w _przejdz_do, gainy KP=2.0/KI=0.1/KD=0.5) pozostaje bez zmian per D-02.

Po edycji zweryfikuj kompilacje:
```bash
arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/
```

Jesli kompilacja przejdzie — upload na plytke:
```bash
arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/
```
  </action>
  <verify>
    <automated>grep -n "OUTPUT_LIMIT" src/arduino/aries_controller/aries_controller.ino | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Linia 25 zawiera: `#define OUTPUT_LIMIT    3.0f` (grep potwierdza)
    - Brak innych zmian w pliku (git diff pokazuje tylko 1 linie zmieniona)
    - arduino-cli compile konczy sie z kodem 0
    - arduino-cli upload konczy sie z kodem 0 (firmware na plycie Uno R4 WiFi)
  </acceptance_criteria>
  <done>Firmware z OUTPUT_LIMIT=3.0f skompilowany i zflashowany na Uno R4 WiFi bez bledow</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Weryfikacja sprzetowa — tracking PID na Uno R4 WiFi</name>
  <files>n/a</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (potwierdzenie OUTPUT_LIMIT=3.0f)
  </read_first>
  <action>
Checkpoint — uzytkownik weryfikuje zachowanie sprzetowe po flashowaniu firmware z OUTPUT_LIMIT=3.0f. Claude nie wykonuje kodu — czeka na wynik testu.

Procedura weryfikacji opisana w how-to-verify ponizej. Trzy testy:
1. TRK-01: Serwa nie uciekaja do limitow ±60/±30 w ciagu 2s od SLEDZENIE
2. TRK-02: Twarz centrowana w 1-3s (error maleje)
3. TRK-03: Brak ciaglego CLAMP w Serial Monitor

Jesli FAIL — iteracja per D-04: OUTPUT_LIMIT=2.0f lub korekta gainow.
  </action>
  <what-built>Firmware z OUTPUT_LIMIT=3.0f — redukcja maksymalnej korekty PID z 5 na 3 stopni/tick. Reset PID juz dzialal (Phase 14). Cel: plynna konwergencja sledzenia bez ucieczki serw do limitow.</what-built>
  <how-to-verify>
**Przygotowanie:**
1. Upewnij sie ze `sudo pigpiod` dziala na RPi (jesli wymagany)
2. Uruchom pi_brain.py na RPi: `python3 run_pi_brain.py`
3. Poczekaj az system wejdzie w SKANOWANIE

**Test TRK-01 — brak ucieczki serw:**
4. Postan przed kamera tak zeby system wykryl twarz i przeszedl w SLEDZENIE
5. OBSERWUJ: Czy serwa szarpna gwaltownie do limitow (±60 pan lub ±30 tilt) w ciagu pierwszych 2 sekund?
   - PASS: Serwa reaguja lagodnie, bez gwaltownego szarpniecia
   - FAIL: Serwa wyraznie uciekaja do limitow katowych

**Test TRK-02 — konwergencja PID:**
6. Stoj nieruchomo przed kamera przez 3 sekundy
7. OBSERWUJ: Czy kamera centruje twarz w kadrze w ciagu 1-3 sekund?
   - PASS: Twarz widocznie centrowana, ruch serw maleje
   - FAIL: Serwa oscyluja bez konwergencji lub nie sledza twarzy

**Test TRK-03 — brak CLAMP w logach:**
8. Sprawdz logi Serial Monitor Arduino: czy sa cigle ostrzezenia CLAMP po wejsciu w SLEDZENIE?
   - PASS: Brak ciaglych CLAMP (pojedyncze przy wejsciu dopuszczalne)
   - FAIL: Ciagly stream ostrzezen CLAMP

**Analiza logow SD (per D-03):**
9. Po 30-sekundowym tescie wyjmij karte SD
10. Sprawdz CSV: czy wartosci pan/tilt nie docieraja do limitow ±60/±30 w ciagu 2s od TRACKING?
11. Sprawdz CSV: czy error_x/error_y maleja do bliskich zera w ciagu 1-3 sekund?

**Jesli FAIL (per D-04 — iteracyjne strojenie):**
- Pierwsza iteracja: proba OUTPUT_LIMIT=2.0f
- Druga iteracja: ew. korekta gainow (KP nizszy) jako fallback
- Opisz obserwacje — Claude dostosuje wartosc w kolejnej iteracji
  </how-to-verify>
  <verify>
    <automated>grep -n "OUTPUT_LIMIT" src/arduino/aries_controller/aries_controller.ino</automated>
  </verify>
  <acceptance_criteria>
    - TRK-01: Serwa nie docieraja do limitow ±60/±30 w ciagu 2s od wejscia w SLEDZENIE
    - TRK-02: error_x i error_y w CSV maleja do mniej niz 0.15 w ciagu 3s od wejscia w SLEDZENIE
    - TRK-03: Brak ciaglego streamu CLAMP w Serial Monitor (pojedyncze przy wejsciu OK)
  </acceptance_criteria>
  <done>Wszystkie 3 testy (TRK-01, TRK-02, TRK-03) PASS — uzytkownik potwierdza "approved"</done>
  <resume-signal>Wpisz "approved" jesli wszystkie 3 testy PASS, lub opisz ktore FAIL z obserwacjami</resume-signal>
</task>

</tasks>

<verification>
- grep OUTPUT_LIMIT aries_controller.ino potwierdza wartosc 3.0f
- git diff pokazuje dokladnie 1 zmieniona linie
- Kompilacja arduino-cli bez bledow
- Weryfikacja sprzetowa: 3 testy (TRK-01, TRK-02, TRK-03) PASS
</verification>

<success_criteria>
1. OUTPUT_LIMIT=3.0f w firmware — jedyna zmiana kodu
2. Firmware skompilowany i zflashowany na Uno R4 WiFi
3. Serwa nie uciekaja do limitow po wejsciu w TRACKING (TRK-01)
4. Twarz centrowana w 1-3s — konwergencja PID widoczna (TRK-02)
5. Brak ciaglych CLAMP w logach (TRK-03)
</success_criteria>

<output>
After completion, create `.planning/phases/15-tilt-servo-fix/15-01-SUMMARY.md`
</output>
