---
phase: 07-pid-sign-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modes/test_tracker.py
  - requirements.txt
autonomous: true
requirements: [PID-01, PID-02, PID-03]

must_haves:
  truths:
    - "Linia 275 w _sledz() zawiera negację: korekta_tilt = -self.pid_tilt(blad_tilt)"
    - "Linia 274 w _sledz() zachowuje negację: korekta_pan = -self.pid_pan(blad_pan) — bez zmian"
    - "simple-pid >= 2.0.1 jest zainstalowane w venv na urządzeniu"
    - "python3 run_test_tracker.py startuje bez błędów importu i bez crashu"
  artifacts:
    - path: "src/modes/test_tracker.py"
      provides: "MaszynaStanow._sledz() z poprawnym znakiem tilt"
      contains: "korekta_tilt = -self.pid_tilt(blad_tilt)"
    - path: "requirements.txt"
      provides: "Pin wersji simple-pid"
      contains: "simple-pid>=2.0.1"
  key_links:
    - from: "src/modes/test_tracker.py:275"
      to: "src/hardware.py:set_angles()"
      via: "nowy_tilt = self.hardware.tilt_angle + korekta_tilt"
      pattern: "korekta_tilt = -self\\.pid_tilt"
---

<objective>
Nanieść jednolinijkową poprawkę znaku tilt w MaszynaStanow._sledz() i upewnić się, że simple-pid >= 2.0.1 jest zainstalowane — warunki konieczne do weryfikacji sprzętowej w Wave 2.

Purpose: Oś tilt bez negacji powoduje rozbieżność: twarz poniżej centrum → kamera jedzie dalej w dół → snap do soft-limitu w 2-3 klatkach. Negacja odwraca korektę, kamera wraca ku twarzy — zbieżność.

Output: Jeden zmieniony znak w test_tracker.py + zaaktualizowany requirements.txt + potwierdzenie importu.
</objective>

<execution_context>
@/Users/mattparol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattparol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/modes/test_tracker.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and upgrade simple-pid to >= 2.0.1</name>
  <files>requirements.txt</files>
  <action>
1. Sprawdź aktualnie zainstalowaną wersję:
   ```
   pip show simple-pid
   ```
   Oczekiwana: Version: 2.0.1 lub wyższa.

2. Jeśli wersja < 2.0.1, uruchom:
   ```
   pip install --upgrade "simple-pid>=2.0.1"
   ```

3. Otwórz requirements.txt i zaktualizuj lub dodaj pin dla simple-pid:
   - Znajdź istniejący wpis `simple-pid` (jeśli istnieje) i zmień na `simple-pid>=2.0.1`
   - Jeśli wpisu nie ma, dodaj `simple-pid>=2.0.1`

Uzasadnienie wersji: simple-pid 2.0.1 (2024-07-21) naprawia błąd, gdzie `_last_error` nie był czyszczony przez `reset()`. Na wersjach < 2.0.1 po wejściu w TRACKING po SCANNING pochodna ma stary `_last_error` z poprzedniej sesji śledzenia — powoduje skok serwa na pierwszej klatce (PID-03).
  </action>
  <verify>
    <automated>cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE && source venv/bin/activate && pip show simple-pid | grep -E "^Version:" </automated>
  </verify>
  <done>Wyjście pip show simple-pid pokazuje Version: 2.0.1 lub wyżej. requirements.txt zawiera simple-pid>=2.0.1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply tilt sign fix in MaszynaStanow._sledz()</name>
  <files>src/modes/test_tracker.py</files>
  <action>
Otwórz src/modes/test_tracker.py. Znajdź metodę MaszynaStanow._sledz() — linia 275 (okolice bloku korekta_pan/korekta_tilt).

Obecny kod (linia 275):
```python
korekta_tilt = self.pid_tilt(blad_tilt)
```

Zmień na:
```python
korekta_tilt = -self.pid_tilt(blad_tilt)  # negacja — oś tilt działa jak pan
```

WAŻNE — co NIE zmienić:
- Linia 274: `korekta_pan = -self.pid_pan(blad_pan)` — zostawić bez zmian (PID-02: pan działa od v1.6)
- Żadne inne linie w tej metodzie
- Wartości Kp, Ki, Kd w config.py — nie dotykać
- Komentarz "# Pan negowany (jak w tracker.py:77)" — można zachować lub zaktualizować do "# Obie korekty negowane — inwersja osi pixel→servo"

Uzasadnienie (z research):
- blad_tilt = srodek_y - ramka_cy — dodatni gdy twarz PONIŻEJ centrum
- pid_tilt(blad_tilt > 0) zwraca wartość dodatnią (dąży do zmniejszenia błędu)
- nowy_tilt = current + korekta_tilt — bez negacji: tilt rośnie, kamera jedzie dalej w dół (od twarzy)
- Z negacją: korekta ujemna → tilt maleje → kamera podnosi się ku twarzy → zbieżność
- Hardware STATE.md: "tilt+ = dół" i "snap do soft-limitu" potwierdza rozbieżność bez negacji
  </action>
  <verify>
    <automated>cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE && source venv/bin/activate && python3 -c "import ast, sys; ast.parse(open('src/modes/test_tracker.py').read()); print('syntax OK')" && grep -n "korekta_tilt" src/modes/test_tracker.py</automated>
  </verify>
  <done>grep pokazuje dwie linie: jedna z `korekta_tilt = -self.pid_tilt(blad_tilt)` (zmieniona) i jedna z `nowy_tilt = self.hardware.tilt_angle + korekta_tilt` (nienaruszona). Składnia Pythona poprawna (ast.parse przechodzi bez błędu). Linia korekta_pan nadal zawiera `-self.pid_pan(blad_pan)` — niezmieniona.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test — import and startup check</name>
  <files></files>
  <action>
Wykonaj weryfikację importu i składni całego modułu przed checkpointem sprzętowym.

Uruchom:
```bash
cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE
source venv/bin/activate
python3 -c "
from src.modes.test_tracker import MaszynaStanow, TestTracker, Picamera2Stream
import inspect, src.modes.test_tracker as m
src_lines = inspect.getsource(m.MaszynaStanow._sledz).splitlines()
for i, line in enumerate(src_lines):
    if 'korekta_tilt' in line or 'korekta_pan' in line:
        print(f'  {line.strip()}')
print('import OK')
"
```

Oczekiwany wynik — w wyjściu muszą pojawić się:
```
  korekta_pan = -self.pid_pan(blad_pan)
  korekta_tilt = -self.pid_tilt(blad_tilt)
  import OK
```

Jeśli pojawi się `korekta_tilt = self.pid_tilt(blad_tilt)` (bez minusa) — poprawka nie została zapisana, wróć do Task 2.

Uwaga: na non-RPi import `Picamera2Stream` może rzucić `ImportError` (brak picamera2). W takim przypadku:
```bash
python3 -c "
import ast
src = open('src/modes/test_tracker.py').read()
ast.parse(src)
lines = src.splitlines()
for i, line in enumerate(lines, 1):
    if 'korekta_tilt' in line or 'korekta_pan' in line:
        print(f'{i}: {line}')
print('parse OK')
"
```
  </action>
  <verify>
    <automated>cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE && python3 -c "import ast; src=open('src/modes/test_tracker.py').read(); ast.parse(src); lines=src.splitlines(); [print(f'{i+1}: {l}') for i,l in enumerate(lines) if 'korekta_tilt' in l or 'korekta_pan' in l]; print('parse OK')"</automated>
  </verify>
  <done>Wyjście zawiera `korekta_pan = -self.pid_pan(blad_pan)` i `korekta_tilt = -self.pid_tilt(blad_tilt)` — obie z negacją. Nie ma linii z `korekta_tilt = self.pid_tilt` (bez minusa). Kończy się "parse OK".</done>
</task>

</tasks>

<verification>
Po ukończeniu wszystkich trzech zadań:

1. `grep -n "korekta_tilt\|korekta_pan" src/modes/test_tracker.py` — obie linie z negacją (`-self.pid_pan`, `-self.pid_tilt`)
2. `pip show simple-pid` — Version >= 2.0.1
3. `grep "simple-pid" requirements.txt` — zawiera `simple-pid>=2.0.1`
4. `python3 -c "import ast; ast.parse(open('src/modes/test_tracker.py').read()); print('OK')"` — parse OK

Weryfikacja funkcjonalna (zbieżność tilt, poprawność pan, brak skoku integratora) — w planie 07-02 (checkpoint sprzętowy).
</verification>

<success_criteria>
- Linia 275: `korekta_tilt = -self.pid_tilt(blad_tilt)` — negacja dodana
- Linia 274: `korekta_pan = -self.pid_pan(blad_pan)` — negacja zachowana, bez zmian
- requirements.txt: `simple-pid>=2.0.1`
- Składnia modułu poprawna (ast.parse przechodzi)
- Gotowość do weryfikacji sprzętowej w Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/07-pid-sign-correctness/07-01-SUMMARY.md`
</output>
