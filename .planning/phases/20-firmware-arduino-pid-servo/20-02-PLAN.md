---
phase: 20-firmware-arduino-pid-servo
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: true
requirements:
  - ARD-01
  - ARD-03
  - ARD-05
  - ARD-06
must_haves:
  truths:
    - "Petla PID wykonuje sie co 10ms — deterministyczny timing via millis()"
    - "Przy braku ramek przez >500ms Arduino przechodzi autonomicznie do SCAN"
    - "Maszyna stanow przechodzi IDLE → SCAN → TRACK w odpowiedzi na ramki z RPi"
    - "Skan sinusoidalny Lissajous 2D pokrywa obie osie jednoczesnie"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Pelna logika sterowania: dispatch, PID tick, skan Lissajous, watchdog, loop()"
      contains: "dispatch_ramke"
  key_links:
    - from: "przetwarzaj_bajt()"
      to: "dispatch_ramke()"
      via: "Wywolanie po poprawnej checksumie zamiast echo"
      pattern: "dispatch_ramke\\(\\)"
    - from: "dispatch_ramke()"
      to: "przejdz_do()"
      via: "Zmiana stanu systemu na podstawie mode z ramki"
      pattern: "przejdz_do"
    - from: "pid_tick()"
      to: "ustaw_serwa()"
      via: "Po obliczeniu PID lub skan tick"
      pattern: "ustaw_serwa\\(\\)"
    - from: "loop()"
      to: "pid_tick()"
      via: "Wywolanie w kazdej iteracji, millis() throttle wewnatrz"
      pattern: "pid_tick\\(\\)"
---

<objective>
Dodanie logiki sterowania do firmware: dispatch ramek (maszyna stanow IDLE/SCAN/TRACK), petla PID 100 Hz z normalizacja bledu, skan Lissajous 2D, watchdog millis() 500ms, i rozbudowa loop() o cala logike.

Purpose: Po tym planie firmware jest w pelni funkcjonalny — odbiera ramki z RPi, steruje serwami przez PID w trybie TRACK, skanuje autonomicznie w trybie SCAN, wraca do SCAN przy utracie komunikacji.
Output: Kompletny firmware gotowy do uploadu i testow empirycznych na hardware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/protocol/PROTOCOL_SPEC.md
@.planning/phases/20-firmware-arduino-pid-servo/20-CONTEXT.md
@.planning/phases/20-firmware-arduino-pid-servo/20-RESEARCH.md
@.planning/phases/20-firmware-arduino-pid-servo/20-01-SUMMARY.md

<interfaces>
<!-- Po Plan 01 firmware zawiera: -->
<!-- #define: KP, KI, KD, OUTPUT_LIMIT, PID_INTERVAL_MS, PAN_PIN, TILT_PIN -->
<!--          PAN_MIN/MAX, TILT_MIN/MAX, PAN_INVERT, TILT_INVERT, HALF_FRAME_W -->
<!--          WATCHDOG_TIMEOUT_MS, SCAN_FREQ_PAN/TILT, SCAN_AMP_PAN/TILT -->
<!-- enum StanSystemu { IDLE, SCAN, TRACK }; -->
<!-- Servo serwo_pan, serwo_tilt; -->
<!-- QuickPID pidPan, pidTilt; -->
<!-- float pan_wej, pan_wyj, pan_sp, tilt_wej, tilt_wyj, tilt_sp; -->
<!-- StanSystemu stan_systemu; -->
<!-- float kat_pan, kat_tilt; -->
<!-- int16_t ostatni_blad_x, ostatni_blad_y; -->
<!-- unsigned long czas_ostatniej_ramki, czas_ostatniego_pid, czas_startowy_skanu; -->
<!-- void safe_startup(); void init_pid(); void ustaw_serwa(); -->
<!-- void przetwarzaj_bajt(uint8_t bajt); — parser z Phase 19 (echo na poprawna ramke) -->

Plan 02 MODYFIKUJE:
- przetwarzaj_bajt(): zamien Serial.write(echo) na dispatch_ramke()
- loop(): dodaj watchdog check + pid_tick()

Plan 02 DODAJE:
- dispatch_ramke(): ekstrakcja pol ramki, reset watchdog, zmiana stanu
- przejdz_do(): przejscie stanow z resetem PID
- pid_tick(): petla PID 100 Hz z millis() throttle
- skan_tick(): skan Lissajous 2D
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Funkcje dispatch_ramke(), przejdz_do(), pid_tick(), skan_tick()</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino
    - .planning/phases/20-firmware-arduino-pid-servo/20-RESEARCH.md (Pattern 3, 4, 5, 6)
    - .planning/protocol/PROTOCOL_SPEC.md
  </read_first>
  <action>
Dodaj cztery nowe funkcje do aries_controller.ino. Umiejscowienie: PONIZEJ przetwarzaj_bajt(), POWYZEJ safe_startup().

**1. przejdz_do() — per Research Pattern 5:**
```cpp
// Przejscie do nowego stanu z resetem PID i scan timer
void przejdz_do(StanSystemu nowy_stan) {
    stan_systemu = nowy_stan;
    if (nowy_stan == SCAN) {
        czas_startowy_skanu = millis();  // reset czasu skanu
        pidPan.Reset();                   // reset integratora PID
        pidTilt.Reset();
    } else if (nowy_stan == TRACK) {
        pidPan.Reset();                   // reset PID przy wejsciu w TRACK
        pidTilt.Reset();
    }
    // IDLE: nic dodatkowego
}
```
WAZNE: Reset PID przy TRACK zapobiega ucieczce serw z narosnieta wartoscia integratora z poprzedniego trybu. Reset PID przy SCAN zapobiega resztkowemu PID output.

**2. dispatch_ramke() — per Research Pattern 6, D-08 (bezposredni dispatcher):**
```cpp
// Ekstrakcja pol z ramki i dispatch do maszyny stanow (D-08: bezposredni)
void dispatch_ramke() {
    uint8_t tryb     = ramka_buf[1];
    int16_t blad_x   = (int16_t)(ramka_buf[2] | (ramka_buf[3] << 8));
    int16_t blad_y   = (int16_t)(ramka_buf[4] | (ramka_buf[5] << 8));
    // ramka_buf[6] = face_size — zarezerwowane na Phase 23

    czas_ostatniej_ramki = millis();  // reset watchdog (D-07, Pitfall 5)

    ostatni_blad_x = blad_x;
    ostatni_blad_y = blad_y;

    // Bezposredni dispatch — mode z ramki ustawia stan (D-08)
    if (tryb <= 2) {  // walidacja zakresu mode: 0=IDLE, 1=SCAN, 2=TRACK
        StanSystemu nowy = (StanSystemu)tryb;
        if (nowy != stan_systemu) {
            przejdz_do(nowy);
        }
    }
}
```
WAZNE: `czas_ostatniej_ramki = millis()` MUSI byc w dispatch_ramke() (po udanej checksumie), NIE w przetwarzaj_bajt() — Pitfall 5.
WAZNE: Walidacja `tryb <= 2` chroni przed nieprawidlowym mode.

**3. skan_tick() — per Research Pattern 4, D-09, D-10, D-11:**
```cpp
// Skan sinusoidalny Lissajous 2D — autonomiczne skanowanie (D-09)
// f_pan=0.05 Hz, f_tilt=0.073 Hz — irracjonalny stosunek (D-11)
// PAN=70 deg, TILT=25 deg (D-10)
void skan_tick(unsigned long teraz) {
    float t = (teraz - czas_startowy_skanu) / 1000.0f;  // sekundy
    kat_pan  = SCAN_AMP_PAN  * sin(2.0f * M_PI * SCAN_FREQ_PAN  * t);
    kat_tilt = SCAN_AMP_TILT * sin(2.0f * M_PI * SCAN_FREQ_TILT * t);
    // Clamp dla bezpieczenstwa (powinien byc zbedny przy AMP < MAX, ale defense-in-depth)
    kat_pan  = constrain(kat_pan,  PAN_MIN,  PAN_MAX);
    kat_tilt = constrain(kat_tilt, TILT_MIN, TILT_MAX);
    ustaw_serwa();
}
```
WAZNE: Uzyj `M_PI` (dostepny w avr-libc math.h). `2.0f * M_PI` NIE `2 * M_PI` (float literal).

**4. pid_tick() — per Research Pattern 3, D-01, D-03:**
```cpp
// Petla PID 100 Hz z millis() throttle (D-03)
// Normalizacja bledu error_px / 160.0f → -1.0..+1.0 (D-01)
void pid_tick() {
    unsigned long teraz = millis();
    if (teraz - czas_ostatniego_pid < PID_INTERVAL_MS) return;
    czas_ostatniego_pid = teraz;

    if (stan_systemu == TRACK) {
        // D-01: normalizacja bledu — piksel / polowa_ramki
        pan_wej  = (float)ostatni_blad_x / HALF_FRAME_W;
        tilt_wej = (float)ostatni_blad_y / HALF_FRAME_W;

        pidPan.Compute();
        pidTilt.Compute();

        // Zastosuj kierunek (D-12: PAN_INVERT / TILT_INVERT) + clamp
        kat_pan  = constrain(kat_pan  + PAN_INVERT  * pan_wyj,  PAN_MIN,  PAN_MAX);
        kat_tilt = constrain(kat_tilt + TILT_INVERT * tilt_wyj, TILT_MIN, TILT_MAX);
        ustaw_serwa();
    } else if (stan_systemu == SCAN) {
        skan_tick(teraz);
    }
    // IDLE: nic nie rob — serwa nieruchome
}
```
WAZNE: `(float)ostatni_blad_x / HALF_FRAME_W` — HALF_FRAME_W zdefiniowany jako 160.0f, wiec dzielenie daje float (Pitfall 2). PAN_INVERT / TILT_INVERT jako mnoznik kierunku.

Komentarze po polsku. 4-spacje indentacja.
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "void dispatch_ramke" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "void przejdz_do" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "void pid_tick" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "void skan_tick" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "HALF_FRAME_W" src/arduino/aries_controller/aries_controller.ino returns >= 2 (define + usage)
    - grep -c "PAN_INVERT.*pan_wyj" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "TILT_INVERT.*tilt_wyj" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "czas_ostatniej_ramki = millis" src/arduino/aries_controller/aries_controller.ino returns >= 1 (in dispatch_ramke)
    - grep -c "pidPan.Reset" src/arduino/aries_controller/aries_controller.ino returns >= 2 (in przejdz_do SCAN + TRACK)
    - grep "SCAN_FREQ_PAN" src/arduino/aries_controller/aries_controller.ino | grep -c "sin" returns 1
    - arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller exits 0
  </acceptance_criteria>
  <done>Cztery funkcje sterowania kompiluja sie: dispatch_ramke() (ekstrakcja + watchdog reset + bezposredni dispatch), przejdz_do() (zmiana stanu + PID reset), pid_tick() (100 Hz z normalizacja + INVERT), skan_tick() (Lissajous 2D f_pan=0.05, f_tilt=0.073, AMP 70/25).</done>
</task>

<task type="auto">
  <name>Task 2: Modyfikacja przetwarzaj_bajt() i loop() — pelna integracja</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino
    - .planning/phases/20-firmware-arduino-pid-servo/20-RESEARCH.md (Pattern 5: loop())
  </read_first>
  <action>
Dwie modyfikacje w istniejacym kodzie:

**1. Modyfikacja przetwarzaj_bajt() — zamien echo na dispatch:**

W funkcji przetwarzaj_bajt(), w bloku `if (obliczona == ramka_buf[7])`:
- USUN: `Serial.write(ramka_buf, FRAME_SIZE);` (echo z Phase 19)
- DODAJ: `dispatch_ramke();` (dispatch do maszyny stanow)

Nic innego w przetwarzaj_bajt() nie zmieniaj. Komentarz nad nowa linia: `// Checksum poprawna — dispatch do maszyny stanow (Phase 20)`

**2. Modyfikacja loop() — pelna petla sterowania:**

Zastap cala funkcje loop() nowa wersja per Research Pattern 5:
```cpp
void loop() {
    // --- Parser serial (zachowany z Phase 19) ---
    while (Serial.available() > 0) {
        uint8_t bajt = (uint8_t)Serial.read();
        przetwarzaj_bajt(bajt);
    }

    // --- Watchdog millis() (D-07) ---
    // Po 500ms bez poprawnej ramki → autonomiczny powrot do SCAN
    if (stan_systemu != IDLE && stan_systemu != SCAN) {
        if (millis() - czas_ostatniej_ramki > WATCHDOG_TIMEOUT_MS) {
            przejdz_do(SCAN);
        }
    }

    // --- PID / skan tick (D-03) ---
    pid_tick();
}
```

WAZNE logika watchdog:
- Watchdog odpala sie TYLKO gdy system jest w TRACK (nie w IDLE ani SCAN) — bo IDLE to stan startowy (nie chcemy od razu SCAN), a SCAN juz skanuje.
- Target watchdog = SCAN (nie IDLE) — per rekomendacja research: system autonomiczny przy utracie komunikacji.
- `millis() - czas_ostatniej_ramki` — bezpieczne przy przepelnieniu unsigned long (Pitfall 6).
- czas_ostatniej_ramki resetowane w dispatch_ramke() (Pitfall 5).

WAZNE: Zachowaj parser while(Serial.available()) z Phase 19 BEZ ZMIAN (tylko zmiana w przetwarzaj_bajt wewnatrz).
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "dispatch_ramke()" src/arduino/aries_controller/aries_controller.ino returns >= 2 (definition + call in przetwarzaj_bajt)
    - grep -c "Serial.write" src/arduino/aries_controller/aries_controller.ino returns 0 (echo usuniety)
    - grep -c "pid_tick()" src/arduino/aries_controller/aries_controller.ino returns >= 2 (definition + call in loop)
    - grep -c "WATCHDOG_TIMEOUT_MS" src/arduino/aries_controller/aries_controller.ino returns >= 2 (define + usage in loop)
    - grep -c "przejdz_do(SCAN)" src/arduino/aries_controller/aries_controller.ino returns >= 1 (watchdog in loop)
    - grep "Serial.available" src/arduino/aries_controller/aries_controller.ino | grep -c "while" returns 1 (parser zachowany)
    - arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller exits 0
  </acceptance_criteria>
  <done>Firmware w pelni zintegrowany: przetwarzaj_bajt() dispatchuje do maszyny stanow (zamiast echo), loop() zawiera parser + watchdog 500ms→SCAN + pid_tick(). Echo usuniety. Kompilacja bez bledow.</done>
</task>

</tasks>

<verification>
- `arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller` — kompilacja bez bledow
- Brak `Serial.write` (echo usuniety)
- dispatch_ramke() wywolywany z przetwarzaj_bajt() po poprawnej checksumie
- Watchdog w loop() sprawdza timeout 500ms i przechodzi do SCAN
- pid_tick() wywolywany w loop(), zawiera PID Compute + normalizacje + INVERT
- skan_tick() generuje Lissajous z f_pan=0.05, f_tilt=0.073
- przejdz_do() resetuje PID przy zmianie stanu
</verification>

<success_criteria>
1. Firmware kompiluje sie bez bledow (arduino-cli compile exit 0)
2. Parser zachowany z Phase 19, echo zastapiony dispatch_ramke()
3. Maszyna stanow: IDLE (startowy) → SCAN/TRACK sterowane ramkami, watchdog→SCAN
4. PID tick co 10ms z normalizacja bledu / 160.0f i PAN_INVERT/TILT_INVERT
5. Skan Lissajous 2D: PAN 70 deg @ 0.05 Hz, TILT 25 deg @ 0.073 Hz
6. Watchdog 500ms bez ramek → przejscie do SCAN (nie IDLE)
7. PID resetowany przy kazdej zmianie stanu (SCAN i TRACK)
</success_criteria>

<output>
After completion, create `.planning/phases/20-firmware-arduino-pid-servo/20-02-SUMMARY.md`
</output>
