---
phase: 20-firmware-arduino-pid-servo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: true
requirements:
  - ARD-02
  - ARD-04
  - ARD-05
must_haves:
  truths:
    - "Serwa plynnie docieraja do pozycji 90/90 przy starcie — brak skoku pradu"
    - "Zmiana PAN_INVERT / TILT_INVERT w #define odwraca kierunek serwa"
    - "Servo.attach() na D9 i D10, PID zmienne zadeklarowane i skonfigurowane"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Defines, globals, safe_startup(), init_pid(), ustaw_serwa(), rozbudowany setup()"
      contains: "safe_startup"
  key_links:
    - from: "setup()"
      to: "serwo_pan.attach(9)"
      via: "Servo.attach PRZED safe_startup()"
      pattern: "serwo_pan\\.attach\\(9\\)"
    - from: "safe_startup()"
      to: "serwo_pan.writeMicroseconds"
      via: "rampa 500→1500us w 1000ms"
      pattern: "writeMicroseconds"
---

<objective>
Rozbudowa firmware aries_controller.ino o fundament sterowania: stale konfiguracyjne (#define), zmienne globalne (PID, Servo, stan systemu), funkcje safe_startup(), init_pid(), ustaw_serwa(), oraz pelny setup() z poprawna kolejnoscia inicjalizacji.

Purpose: Fundament dla logiki sterowania w Plan 02. Bez tego Plan 02 nie ma zmiennych, serw ani PID do uzycia.
Output: Kompilujacy sie firmware z safe startup i zainicjowanym PID, gotowy na dodanie logiki sterowania.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/protocol/PROTOCOL_SPEC.md
@.planning/phases/20-firmware-arduino-pid-servo/20-CONTEXT.md
@.planning/phases/20-firmware-arduino-pid-servo/20-RESEARCH.md

<interfaces>
<!-- Istniejacy firmware z Phase 19 — parser state-machine + echo -->
<!-- Plan 01 ZACHOWUJE caly parser (przetwarzaj_bajt, enum StanParsera, zmienne parsera) -->
<!-- Plan 01 DODAJE: defines, globals, safe_startup(), init_pid(), ustaw_serwa(), nowy setup() -->

From src/arduino/aries_controller/aries_controller.ino (Phase 19):
```cpp
// Istniejace — zachowac:
#include <QuickPID.h>
#include <Servo.h>
#include <LiquidCrystal.h>
#define FRAME_SIZE 8
#define START_MARKER 0xAA
enum StanParsera { WAIT_START, READ_PAYLOAD };
uint8_t ramka_buf[FRAME_SIZE];
uint8_t ramka_idx = 0;
StanParsera stan_parsera = WAIT_START;
void przetwarzaj_bajt(uint8_t bajt) { ... }  // zachowac BEZ ZMIAN
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale konfiguracyjne i zmienne globalne</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino
    - .planning/phases/20-firmware-arduino-pid-servo/20-RESEARCH.md
    - .planning/phases/20-firmware-arduino-pid-servo/20-CONTEXT.md
  </read_first>
  <action>
Rozbuduj aries_controller.ino o stale konfiguracyjne i zmienne globalne. Zachowaj CALY istniejacy kod parsera (przetwarzaj_bajt, enum StanParsera, zmienne parsera). Dodaj PONIZEJ istniejacych #define i POWYZEJ enum StanParsera:

**Nowe #define (per D-02, D-04, D-10, D-12, research Pattern 2-6):**
```cpp
// --- Konfiguracja PID (D-02) ---
#define KP              2.0f      // Proporcjonalny gain
#define KI              0.1f      // Calkowitowy gain
#define KD              0.5f      // Roznicowy gain
#define OUTPUT_LIMIT    5.0f      // +/-5 stopni/tick — D-02
#define PID_INTERVAL_MS 10        // 100 Hz — D-03

// --- Konfiguracja serw (D-05, D-10) ---
#define PAN_PIN         9         // D9 = TIMER1A
#define TILT_PIN        10        // D10 = TIMER1B
#define PAN_MIN         (-60.0f)  // limit katowy pan
#define PAN_MAX         (60.0f)
#define TILT_MIN        (-30.0f)  // limit katowy tilt
#define TILT_MAX        (30.0f)

// --- Kierunek serw — empiryczna kalibracja (D-04, D-12) ---
#define PAN_INVERT      (1)       // +1 lub -1 — zmien empirycznie
#define TILT_INVERT     (-1)      // -1 potwierdzony w v1.7 legacy

// --- Normalizacja bledu (D-01) ---
#define HALF_FRAME_W    160.0f    // polowa szerokosci klatki 320px

// --- Watchdog (D-07) ---
#define WATCHDOG_TIMEOUT_MS 500   // 500ms bez ramek → SCAN

// --- Skan Lissajous (D-09, D-10, D-11) ---
#define SCAN_FREQ_PAN   0.05f    // Hz — czestotliwosc pan
#define SCAN_FREQ_TILT  0.073f   // Hz — irracjonalny stosunek (D-11)
#define SCAN_AMP_PAN    70.0f    // stopnie — D-10
#define SCAN_AMP_TILT   25.0f    // stopnie — D-10
```

**Nowy enum StanSystemu (PONIZEJ enum StanParsera):**
```cpp
// --- Stan systemu (D-06) ---
enum StanSystemu { IDLE, SCAN, TRACK };
```

**Nowe zmienne globalne (PONIZEJ zmiennych parsera):**
```cpp
// --- Instancje Servo ---
Servo serwo_pan;
Servo serwo_tilt;

// --- Zmienne PID (QuickPID wymaga float*) ---
float pan_wej = 0.0f, pan_wyj = 0.0f, pan_sp = 0.0f;
float tilt_wej = 0.0f, tilt_wyj = 0.0f, tilt_sp = 0.0f;
QuickPID pidPan(&pan_wej, &pan_wyj, &pan_sp, KP, KI, KD, QuickPID::Action::direct);
QuickPID pidTilt(&tilt_wej, &tilt_wyj, &tilt_sp, KP, KI, KD, QuickPID::Action::direct);

// --- Stan systemu ---
StanSystemu stan_systemu = IDLE;   // D-06: startowy IDLE
float kat_pan  = 0.0f;             // aktualny kat pan (-60..+60)
float kat_tilt = 0.0f;             // aktualny kat tilt (-30..+30)
int16_t ostatni_blad_x = 0;        // ostatni blad X z ramki
int16_t ostatni_blad_y = 0;        // ostatni blad Y z ramki

// --- Timing ---
unsigned long czas_ostatniej_ramki = 0;  // watchdog timer
unsigned long czas_ostatniego_pid  = 0;  // PID tick timer
unsigned long czas_startowy_skanu  = 0;  // czas wejscia w SCAN
```

WAZNE: NIE zmieniaj przetwarzaj_bajt() ani istniejacych zmiennych parsera. Zachowaj komentarze w jezyku polskim.
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "PAN_INVERT" src/arduino/aries_controller/aries_controller.ino returns >= 1
    - grep -c "TILT_INVERT" src/arduino/aries_controller/aries_controller.ino returns >= 1
    - grep -c "QuickPID pidPan" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "QuickPID pidTilt" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "Servo serwo_pan" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "enum StanSystemu" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "HALF_FRAME_W" src/arduino/aries_controller/aries_controller.ino returns >= 1
    - grep -c "SCAN_FREQ_TILT.*0.073" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "OUTPUT_LIMIT.*5.0" src/arduino/aries_controller/aries_controller.ino returns 1
    - arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller exits 0
  </acceptance_criteria>
  <done>Plik .ino zawiera wszystkie #define (PID, servo, scan, watchdog, INVERT), enum StanSystemu, instancje QuickPID i Servo, zmienne stanu — kompiluje sie bez bledow.</done>
</task>

<task type="auto">
  <name>Task 2: Funkcje safe_startup(), init_pid(), ustaw_serwa() i nowy setup()</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino
    - .planning/phases/20-firmware-arduino-pid-servo/20-RESEARCH.md (Pattern 1, Pattern 2, Pattern 7)
  </read_first>
  <action>
Dodaj trzy nowe funkcje i zmodyfikuj setup(). Umiejscowienie: PONIZEJ przetwarzaj_bajt(), POWYZEJ setup().

**Funkcja safe_startup() — per D-04, Research Pattern 1:**
```cpp
// Bezpieczny startup — rampa writeMicroseconds() 500→1500us w 1000ms
// Minimalne obciazenie zasilacza 6V, brak skoku pradu (D-04)
void safe_startup() {
    const int US_MIN    = 500;    // minimalny impuls
    const int US_CENTER = 1500;   // centrum = 90 stopni
    const int KROKI     = 50;     // 1000ms / 20ms = 50 krokow
    const int OPOZNIENIE_MS = 20;

    for (int i = 0; i <= KROKI; i++) {
        int us = US_MIN + (int)((long)(US_CENTER - US_MIN) * i / KROKI);
        serwo_pan.writeMicroseconds(us);
        serwo_tilt.writeMicroseconds(us);
        delay(OPOZNIENIE_MS);
    }
    kat_pan  = 0.0f;
    kat_tilt = 0.0f;
}
```

**Funkcja init_pid() — per D-02, D-03, Research Pattern 2:**
```cpp
// Konfiguracja QuickPID dual-axis (D-02, D-03)
void init_pid() {
    pidPan.SetSampleTimeUs(10000);   // 100 Hz
    pidTilt.SetSampleTimeUs(10000);
    pidPan.SetOutputLimits(-OUTPUT_LIMIT, OUTPUT_LIMIT);
    pidTilt.SetOutputLimits(-OUTPUT_LIMIT, OUTPUT_LIMIT);
    pidPan.SetAntiWindupMode(QuickPID::iAwCondition);
    pidTilt.SetAntiWindupMode(QuickPID::iAwCondition);
    pidPan.SetProportionalMode(QuickPID::pOnError);
    pidTilt.SetProportionalMode(QuickPID::pOnError);
    pidPan.SetDerivativeMode(QuickPID::dOnMeas);
    pidTilt.SetDerivativeMode(QuickPID::dOnMeas);
    pidPan.SetMode(QuickPID::Control::automatic);
    pidTilt.SetMode(QuickPID::Control::automatic);
}
```

**Funkcja ustaw_serwa() — per Research Pattern 7:**
```cpp
// Konwersja kat (-60..+60 / -30..+30) na Servo.write(0-180)
// Centrum serwa = 90 stopni = pozycja 0 w naszym ukladzie
void ustaw_serwa() {
    kat_pan  = constrain(kat_pan,  PAN_MIN,  PAN_MAX);
    kat_tilt = constrain(kat_tilt, TILT_MIN, TILT_MAX);
    serwo_pan.write((int)(kat_pan  + 90.0f));
    serwo_tilt.write((int)(kat_tilt + 90.0f));
}
```

**Zmodyfikuj setup() — per Research "Pelna sekwencja setup()":**
Zachowaj Serial.begin(115200) i while(!Serial) timeout. Dodaj:
1. `serwo_pan.attach(PAN_PIN);` i `serwo_tilt.attach(TILT_PIN);` — D-05, PRZED safe_startup (Pitfall 1)
2. `safe_startup();` — D-04
3. `init_pid();` — D-03
4. `czas_ostatniej_ramki = millis();` i `czas_startowy_skanu = millis();` — inicjalizacja timerow
5. `stan_systemu = IDLE;` — D-06 (redundancja z globalem, ale jawna)

WAZNE: Kolejnosc w setup() jest KRYTYCZNA — attach() MUSI byc przed safe_startup(), safe_startup() przed init_pid(). Komentarze po polsku.
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "void safe_startup" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "void init_pid" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "void ustaw_serwa" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "serwo_pan.attach" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "serwo_tilt.attach" src/arduino/aries_controller/aries_controller.ino returns 1
    - grep -c "writeMicroseconds" src/arduino/aries_controller/aries_controller.ino returns >= 2
    - grep -c "SetAntiWindupMode" src/arduino/aries_controller/aries_controller.ino returns 2
    - grep -c "SetDerivativeMode.*dOnMeas" src/arduino/aries_controller/aries_controller.ino returns 2
    - grep -c "constrain.*PAN_MIN.*PAN_MAX" src/arduino/aries_controller/aries_controller.ino returns 1
    - arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller exits 0
  </acceptance_criteria>
  <done>Firmware kompiluje sie z safe_startup() (rampa 500→1500us), init_pid() (QuickPID dual-axis z anti-windup, dOnMeas, 100 Hz), ustaw_serwa() (clamp + write), setup() z poprawna kolejnoscia: attach → safe_startup → init_pid.</done>
</task>

</tasks>

<verification>
- `arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller` — kompilacja bez bledow
- grep potwierdza obecnosc: PAN_INVERT, TILT_INVERT, QuickPID pidPan/pidTilt, Servo serwo_pan/serwo_tilt, safe_startup, init_pid, ustaw_serwa, enum StanSystemu
- Istniejacy parser przetwarzaj_bajt() niezmieniony (nadal zawiera echo Serial.write)
</verification>

<success_criteria>
1. Firmware kompiluje sie bez bledow i ostrzezen (arduino-cli compile exit 0)
2. Wszystkie #define obecne: KP=2.0f, KI=0.1f, KD=0.5f, OUTPUT_LIMIT=5.0f, PAN_INVERT, TILT_INVERT, SCAN_FREQ_TILT=0.073f, SCAN_AMP_PAN=70.0f
3. safe_startup() implementuje rampe writeMicroseconds 500→1500us
4. init_pid() konfiguruje QuickPID z iAwCondition, dOnMeas, 100 Hz
5. setup() ma poprawna kolejnosc: Serial → attach → safe_startup → init_pid → timery
</success_criteria>

<output>
After completion, create `.planning/phases/20-firmware-arduino-pid-servo/20-01-SUMMARY.md`
</output>
