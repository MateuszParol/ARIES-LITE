---
phase: 12-pid-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - run_test_tracker.py
autonomous: false
requirements:
  - PID-04
  - PID-05
  - PID-06
must_haves:
  truths:
    - "run_test_tracker.py --debug ustawia log level na DEBUG i PID per-tick logi sa widoczne w terminalu"
    - "Logi PID pokazuja blad_tilt != 0 i korekta_tilt != 0 gdy twarz jest poza centrum — software path PID oblicza poprawnie (PID-04)"
    - "Zadna os nie ucieka do limitu natychmiastowo — korekty sa proporcjonalne do bledu, brak runaway (PID-05)"
    - "Po 10+ klatkach TRACKING blad pan i tilt maleje w kierunku zera — PID konwerguje (PID-06)"
  artifacts:
    - path: "run_test_tracker.py"
      provides: "Flaga --debug ustawiajaca log level na DEBUG"
      contains: "argparse"
  key_links:
    - from: "run_test_tracker.py"
      to: "logging.basicConfig"
      via: "argparse --debug flag sets level=logging.DEBUG"
      pattern: "level=logging\\.DEBUG"
    - from: "src/modes/test_tracker.py"
      to: "src/hardware.py"
      via: "set_angles() w _sledz() z korekta_pan i korekta_tilt"
      pattern: "set_angles.*nowy_pan.*nowy_tilt"
---

<objective>
Dodanie flagi --debug do run_test_tracker.py i empiryczna walidacja PID na RPi4 — potwierdzenie ze oba kontrolery PID obliczaja poprawne korekty, nie uciekaja, i konwerguja do centrum.

Purpose: Phase 12 waliduje software path PID po naprawach detekcji (Phase 10) i AWB (Phase 11). Istniejace logi PID per-tick z Phase 9 sa w logger.debug — potrzebna flaga CLI zeby je zobaczyc.
Output: Zmodyfikowany run_test_tracker.py z --debug, raport walidacji PID z logow RPi4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pid-validation/12-CONTEXT.md

@run_test_tracker.py
@src/modes/test_tracker.py
@src/config.py
@src/hardware.py

<interfaces>
<!-- Istniejace interfejsy z ktorymi plan operuje -->

From src/modes/test_tracker.py (linie 298-305):
```python
# PID diagnostyka per-tick — logger.debug
logger.debug(
    "PAN err=%+.1f P=%.3f I=%.3f D=%.3f out=%+.1f | "
    "TILT err=%+.1f P=%.3f I=%.3f D=%.3f out=%+.1f",
    blad_pan, p_pan, i_pan, d_pan, korekta_pan,
    blad_tilt, p_tilt, i_tilt, d_tilt, korekta_tilt
)
```

From src/hardware.py:
```python
def set_angles(self, pan: float, tilt: float) -> None:
    # Clamp + WARNING logging
    self.pan_angle = pan_clamped
    self.tilt_angle = tilt_clamped
```

From run_test_tracker.py (obecny stan):
```python
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    datefmt="%H:%M:%S",
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dodaj flage --debug do run_test_tracker.py</name>
  <files>run_test_tracker.py</files>
  <read_first>
    - run_test_tracker.py (obecny stan — 55 linii, logging.basicConfig na liniach 19-24)
    - src/modes/test_tracker.py (linie 298-305 — PID logger.debug ktory chcemy odslonic)
  </read_first>
  <action>
    Zmodyfikuj run_test_tracker.py per D-03:

    1. Dodaj `import argparse` na gorze (po istniejacych importach: sys, signal, logging).

    2. W funkcji main(), PRZED logging.basicConfig(), dodaj parsowanie argumentow:
    ```python
    parser = argparse.ArgumentParser(description="ARIES-LITE Test Tracker")
    parser.add_argument("--debug", action="store_true", help="Ustaw log level na DEBUG (PID per-tick logi)")
    args = parser.parse_args()
    ```

    3. Przeniesc logging.basicConfig() DO WNETRZA main(), po argparse. Zmienic hardcoded `level=logging.INFO` na warunkowy:
    ```python
    log_level = logging.DEBUG if args.debug else logging.INFO
    logging.basicConfig(
        level=log_level,
        format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
        datefmt="%H:%M:%S",
    )
    ```

    4. Dodac log informujacy o trybie debug:
    ```python
    if args.debug:
        logger.info("Tryb DEBUG aktywny — PID per-tick logi widoczne")
    ```

    5. Zaktualizowac docstring modulu — dodac `python3 run_test_tracker.py --debug` do sekcji Uzycie.

    NIE zmieniaj niczego w src/modes/test_tracker.py — istniejace logger.debug() z Phase 9 jest wystarczajace (per D-02).
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "import run_test_tracker; print('import OK')" && python3 run_test_tracker.py --help | grep -q "\-\-debug" && echo "PASS: --debug flag exists"</automated>
  </verify>
  <acceptance_criteria>
    - run_test_tracker.py zawiera `import argparse`
    - run_test_tracker.py zawiera `parser.add_argument("--debug"`
    - run_test_tracker.py zawiera `level=log_level` (nie hardcoded INFO)
    - run_test_tracker.py zawiera `logging.DEBUG if args.debug else logging.INFO`
    - `python3 run_test_tracker.py --help` wyswietla opcje --debug
    - Docstring zawiera `python3 run_test_tracker.py --debug`
  </acceptance_criteria>
  <done>run_test_tracker.py akceptuje --debug, ustawia log level na DEBUG, import przechodzi bez bledu</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Empiryczna walidacja PID na RPi4</name>
  <files>run_test_tracker.py</files>
  <read_first>
    - run_test_tracker.py (po Task 1 — z flaga --debug)
    - src/modes/test_tracker.py (linie 277-306 — _sledz() i PID diagnostyka)
    - src/config.py (PID gains, limity serw)
    - .planning/phases/12-pid-validation/12-CONTEXT.md (kryteria D-08: blad_tilt != 0 i korekta_tilt != 0)
  </read_first>
  <action>
    Weryfikacja empiryczna PID na RPi4. Uzytkownik uruchamia `sudo pigpiod && python3 run_test_tracker.py --debug`,
    stoi przed kamera i obserwuje logi PID per-tick w terminalu. Sprawdza trzy kryteria:
    (1) TILT err i out sa niezerowe gdy twarz poza centrum (PID-04 per D-08),
    (2) brak natychmiastowego skoku do limitow serw (PID-05),
    (3) err maleje po 10+ klatkach stabilnej detekcji (PID-06).
    Jesli PID nie dziala — uzytkownik wkleja logi i nastepuje diagnoza + fix per D-04, D-05.
  </action>
  <verify>
    <automated>MISSING — wymaga fizycznego RPi4 z kamera i serwami. Weryfikacja manualna przez uzytkownika.</automated>
  </verify>
  <acceptance_criteria>
    - Logi PID per-tick sa widoczne w terminalu z --debug
    - TILT err != 0 i TILT out != 0 gdy twarz jest poza centrum kadru (PID-04)
    - Brak natychmiastowego skoku do limitow serw po wejsciu w TRACKING (PID-05)
    - Po 10+ klatkach stabilnej detekcji, err_pan i err_tilt maleja ponizej ±15 px (PID-06)
  </acceptance_criteria>
  <what-built>
    Flaga --debug w run_test_tracker.py odslania PID per-tick logi z Phase 9.
    Caly software path PID: error calculation -> PID controller -> negacja -> set_angles().
  </what-built>
  <how-to-verify>
    Na RPi4 z podlaczonymi serwami i kamera:

    1. Uruchom: `sudo pigpiod && python3 run_test_tracker.py --debug`
    2. Stoj ok. 60-80 cm przed kamera — poczekaj na zielony prostokat (TRACKING)
    3. Obserwuj terminal — powinny pojawiac sie linie:
       `PAN err=+XX.X P=... I=... D=... out=+YY.Y | TILT err=+XX.X P=... I=... D=... out=+YY.Y`

    **Weryfikacja PID-04 (tilt reaguje w software):**
    - Przesun glowe w gore lub dol
    - Sprawdz: `TILT err=` powinno byc != 0 (roznica od centrum)
    - Sprawdz: `out=` przy TILT powinno byc != 0 (korekta PID)
    - Per D-08: jesli err i out sa niezerowe, PID-04 przechodzi

    **Weryfikacja PID-05 (brak runaway):**
    - Po wejsciu w TRACKING, obserwuj wartosci `out=` dla obu osi
    - Nie powinny natychmiast skakac do limitu (+-5.0)
    - Korekty powinny byc proporcjonalne do bledu
    - Brak WARNING "Clamp pan/tilt" w normalnym trackingu

    **Weryfikacja PID-06 (konwergencja):**
    - Stoj nieruchomo przez 5+ sekund w TRACKING
    - Wartosci `err=` powinny malesiec w kierunku 0 (+-10 px to OK)
    - Kamera powinna centrowac twarz (prostokat blisko srodka kadru)
    - HUD wartosc Pan/Tilt powinna sie ustabilizowac

    **Jesli PID nie dziala poprawnie:**
    - Zapisz ~20 linii logow PID per-tick
    - Opisz obserwowany problem (runaway, brak reakcji, oscylacje)
    - Phase 12 obejmuje naprawe bledow znaku/wiring per D-04, D-05
  </how-to-verify>
  <done>Uzytkownik potwierdza "approved" — PID dziala poprawnie na obu osiach, lub dostarcza logi do diagnozy</done>
  <resume-signal>
    Wpisz "approved" jesli PID dziala poprawnie na obu osiach.
    Jesli jest problem — wklej 20+ linii logow PID i opis obserwacji. Nastapi diagnoza i fix per D-04.
  </resume-signal>
</task>

</tasks>

<verification>
1. `python3 run_test_tracker.py --help` pokazuje flage --debug
2. `python3 run_test_tracker.py --debug` na RPi4 wyswietla PID per-tick logi
3. Logi potwierdzaja: blad_tilt != 0, korekta_tilt != 0 (PID-04)
4. Brak runaway — korekty proporcjonalne do bledu (PID-05)
5. Konwergencja — blad maleje po 10+ klatkach (PID-06)
</verification>

<success_criteria>
- run_test_tracker.py akceptuje --debug i odslania PID logi
- Empiryczne potwierdzenie PID-04: tilt software path oblicza poprawne korekty
- Empiryczne potwierdzenie PID-05: brak runaway na zadnej osi
- Empiryczne potwierdzenie PID-06: PID konwerguje do centrum po stabilnej detekcji
</success_criteria>

<output>
After completion, create `.planning/phases/12-pid-validation/12-01-SUMMARY.md`
</output>
