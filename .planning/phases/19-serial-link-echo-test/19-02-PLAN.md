---
phase: 19-serial-link-echo-test
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - scripts/echo_test.py
autonomous: false
requirements:
  - SER-05

must_haves:
  truths:
    - "echo_test.py wysyla referencyjny TRACK frame i porownuje echo bajt po bajcie"
    - "echo_test.py zwraca exit 0 gdy echo == sent (PASS)"
    - "echo_test.py zwraca exit 1 gdy echo != sent lub timeout (FAIL)"
    - "echo_test.py drukuje sent=[hex], recv=[hex], PASS/FAIL"
  artifacts:
    - path: "scripts/echo_test.py"
      provides: "Skrypt weryfikacji end-to-end serial link"
      contains: "from src.vision.serial_interface import SerialInterface"
  key_links:
    - from: "scripts/echo_test.py"
      to: "src/vision/serial_interface.py"
      via: "import SerialInterface, wywolanie send_frame()"
      pattern: "from src\\.vision\\.serial_interface import SerialInterface"
    - from: "scripts/echo_test.py"
      to: "src/arduino/aries_controller/aries_controller.ino"
      via: "echo response przez USB CDC /dev/ttyACM0"
      pattern: "iface\\._ser\\.read"
---

<objective>
Skrypt echo_test.py weryfikujacy caly lancuch serial end-to-end: RPi wysyla referencyjny TRACK frame przez SerialInterface, Arduino parsuje i odsyla echo, RPi porownuje bajt po bajcie i raportuje PASS/FAIL.

Purpose: Jedyny dowod ze warstwa serial dziala poprawnie — bez tego nie ma sensu budowac PID ani wizji.
Output: scripts/echo_test.py + wynik PASS na fizycznym hardware
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/protocol/PROTOCOL_SPEC.md
@.planning/phases/19-serial-link-echo-test/19-CONTEXT.md
@.planning/phases/19-serial-link-echo-test/19-RESEARCH.md
@.planning/phases/19-serial-link-echo-test/19-01-SUMMARY.md

<interfaces>
<!-- From src/vision/serial_interface.py (created in Plan 01): -->
```python
class SerialInterface:
    BAUDRATE = 115200
    START_MARKER = 0xAA
    FRAME_SIZE = 8

    def __init__(self, port: str = "/dev/ttyACM0", timeout: float = 1.0): ...
    def open(self) -> None: ...  # dtr=False, low_latency, logger.info
    def send_frame(self, mode: int, error_x: int, error_y: int, face_size: int) -> None: ...
    def send_heartbeat(self) -> None: ...  # alias send_frame(0,0,0,0)
    def close(self) -> None: ...
    @staticmethod
    def _buduj_ramke(mode: int, error_x: int, error_y: int, face_size: int) -> bytes: ...
    # self._ser: serial.Serial | None  — dostep do surowego portu
```

<!-- Referencyjny TRACK frame (PROTOCOL_SPEC.md): -->
<!-- mode=2, error_x=45, error_y=-12, face_size=128 -->
<!-- Oczekiwana ramka: [0xAA, 0x02, 0x2D, 0x00, 0xF4, 0xFF, 0x80, 0xA4] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: echo_test.py — skrypt weryfikacji end-to-end</name>
  <files>scripts/echo_test.py</files>
  <read_first>
    - src/vision/serial_interface.py (interfejs SerialInterface — open/send_frame/close + _ser atrybut)
    - .planning/protocol/PROTOCOL_SPEC.md (referencyjny przyklad: mode=2, error_x=45, error_y=-12, face_size=128 -> [0xAA,0x02,0x2D,0x00,0xF4,0xFF,0x80,0xA4])
    - .planning/phases/19-serial-link-echo-test/19-CONTEXT.md (D-07, D-08, D-09)
    - .planning/phases/19-serial-link-echo-test/19-RESEARCH.md (Pattern 3: echo_test.py, Pitfall 3: echo buffer desync)
  </read_first>
  <action>
Utworz katalog `scripts/` jesli nie istnieje. Utworz `scripts/echo_test.py` per D-07.

Shebang: `#!/usr/bin/env python3`
Docstring po polsku: "Skrypt weryfikacji echo serial link RPi-Arduino. Wysyla referencyjny TRACK frame, porownuje echo bajt po bajcie."

Importy: `import sys`, `import time`, `import serial` (pyserial), `pathlib.Path`.
Dodaj sciezke projektu do sys.path: `sys.path.insert(0, str(Path(__file__).parent.parent))`.
Import: `from src.vision.serial_interface import SerialInterface`.

Stale:
```python
PORT = "/dev/ttyACM0"
TIMEOUT = 2.0
FRAME_SIZE = 8
# Referencyjny TRACK frame z PROTOCOL_SPEC.md (D-08)
OCZEKIWANA = bytes([0xAA, 0x02, 0x2D, 0x00, 0xF4, 0xFF, 0x80, 0xA4])
```

Funkcja `main() -> int`:
1. Tworzy `iface = SerialInterface(port=PORT, timeout=TIMEOUT)`
2. W try/finally (finally: `iface.close()`):
   a. `iface.open()`
   b. `time.sleep(2.0)` — czekaj na Arduino USB CDC (Leonardo boot delay per Research Pitfall 1)
   c. `iface._ser.reset_input_buffer()` — wyczysc bufor przed testem (Pitfall 3)
   d. `iface.send_frame(mode=2, error_x=45, error_y=-12, face_size=128)` — referencyjny TRACK frame per D-08
   e. Drukuj: `print(f"sent=[{' '.join(f'{b:02X}' for b in OCZEKIWANA)}]")`
   f. `odebrano = iface._ser.read(FRAME_SIZE)` — czytaj echo z timeoutem
   g. Formatuj hex odebrane: `hex_recv = ' '.join(f'{b:02X}' for b in odebrano) if odebrano else '(brak odpowiedzi)'`
   h. Drukuj: `print(f"recv=[{hex_recv}]")`
   i. Jesli `odebrano == OCZEKIWANA`: drukuj "PASS", return 0. Inaczej: drukuj "FAIL", return 1.
3. Except `serial.SerialException as e`: drukuj `f"BLAD PORTU: {e}"`, return 1.

Na koncu: `if __name__ == "__main__": sys.exit(main())`

Komentarze po polsku. Type hints. 4-space indent.
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && source venv/bin/activate && python3 -c "
import ast, sys
with open('scripts/echo_test.py') as f:
    tree = ast.parse(f.read())
# Sprawdz ze plik jest poprawnym Python
names = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'main' in names, f'Brak funkcji main(), znalezione: {names}'
print('SYNTAX OK, main() present')
"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/echo_test.py exists and is valid Python (ast.parse succeeds)
    - scripts/echo_test.py contains `from src.vision.serial_interface import SerialInterface`
    - scripts/echo_test.py contains `def main() -> int:`
    - scripts/echo_test.py contains `OCZEKIWANA = bytes([0xAA, 0x02, 0x2D, 0x00, 0xF4, 0xFF, 0x80, 0xA4])`
    - scripts/echo_test.py contains `iface.send_frame(mode=2, error_x=45, error_y=-12, face_size=128)`
    - scripts/echo_test.py contains `time.sleep(2.0)` (Leonardo boot delay)
    - scripts/echo_test.py contains `reset_input_buffer()` (buffer flush)
    - scripts/echo_test.py contains `sys.exit(main())`
    - scripts/echo_test.py contains `PASS` and `FAIL` string literals
    - scripts/echo_test.py contains `sent=[` and `recv=[` format strings (per D-09)
  </acceptance_criteria>
  <done>echo_test.py importuje SerialInterface, wysyla referencyjny TRACK frame, czyta echo, porownuje bajt po bajcie, raportuje PASS/FAIL + hex dump, zwraca exit 0/1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Weryfikacja end-to-end na fizycznym hardware</name>
  <files>scripts/echo_test.py</files>
  <action>
Upload firmware na Arduino i uruchom echo_test.py. Checkpoint — uzytkownik weryfikuje wynik na fizycznym hardware.

1. `arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:avr:leonardo src/arduino/aries_controller`
2. `cd /home/parolisko/ARIES-LITE && source venv/bin/activate && python3 scripts/echo_test.py`
  </action>
  <what-built>
Caly serial link RPi-Arduino: SerialInterface (nadajnik), parser state-machine (odbiornik), echo_test.py (weryfikacja). Plan 01 stworzyl obie strony, Plan 02 Task 1 stworzyl skrypt testowy.
  </what-built>
  <how-to-verify>
Wymagania: Arduino Leonardo podlaczone do RPi przez USB.

1. Upload firmware na Arduino:
   ```bash
   arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:avr:leonardo src/arduino/aries_controller
   ```
   Oczekiwanie: upload OK, brak bledow.

2. Uruchom echo test:
   ```bash
   cd /home/parolisko/ARIES-LITE
   source venv/bin/activate
   python3 scripts/echo_test.py
   ```
   Oczekiwanie:
   ```
   sent=[AA 02 2D 00 F4 FF 80 A4]
   recv=[AA 02 2D 00 F4 FF 80 A4]
   PASS
   ```
   Exit code: 0 (`echo $?` po uruchomieniu).

3. (Opcjonalnie) Odlacz USB i podlacz ponownie, uruchom echo_test.py jeszcze raz — powinien znowu zwrocic PASS (resync test).
  </how-to-verify>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && source venv/bin/activate && python3 scripts/echo_test.py</automated>
  </verify>
  <done>echo_test.py zwraca PASS (exit 0) na fizycznym hardware — serial link dziala end-to-end.</done>
  <resume-signal>Wpisz "PASS" jesli echo_test.py zwrocil PASS. Jesli FAIL — opisz output (sent/recv hex dump).</resume-signal>
</task>

</tasks>

<verification>
1. `python3 -c "import ast; ast.parse(open('scripts/echo_test.py').read()); print('SYNTAX OK')"` — SYNTAX OK
2. `grep -c 'SerialInterface' scripts/echo_test.py` — >= 2 (import + uzycie)
3. `python3 scripts/echo_test.py` z Arduino podlaczonym — exit 0, output PASS
</verification>

<success_criteria>
- echo_test.py istnieje, jest poprawnym Python, importuje SerialInterface
- Na fizycznym hardware: echo_test.py wysyla TRACK frame, Arduino echoes, wynik PASS
- Exit code 0 oznacza sukces, exit code 1 oznacza failure
- Output zawiera sent=[hex] recv=[hex] PASS/FAIL per D-09
</success_criteria>

<output>
After completion, create `.planning/phases/19-serial-link-echo-test/19-02-SUMMARY.md`
</output>
