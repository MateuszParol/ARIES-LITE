---
phase: 16-camera-color-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
  - tools/analiza_skoku_csv.py
autonomous: true
requirements: [SCN-01, SCN-02, SCN-03]

must_haves:
  truths:
    - "skan_krok() uzywa _t_offset_pan i _t_offset_tilt w obliczeniu sinusoidy"
    - "resetuj_czas_skanu() oblicza t_offset przez asin() z aktualnej pozycji serwa"
    - "_przejdz_do(SKANOWANIE) przekazuje kat_pan i kat_tilt do resetuj_czas_skanu()"
    - "Firmware kompiluje sie bez bledow na Arduino Uno R4 WiFi"
    - "Skrypt Python analizuje logi CSV pod katem skoku przy przejsciach stanow"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Phase-offset continuity w skan_krok() i resetuj_czas_skanu()"
      contains: "_t_offset_pan"
    - path: "tools/analiza_skoku_csv.py"
      provides: "Skrypt weryfikacji SCN-03 z logow CSV"
      contains: "delta_pan"
  key_links:
    - from: "MaszynaStanow::_przejdz_do()"
      to: "ServoPID::resetuj_czas_skanu()"
      via: "przekazanie kat_pan, kat_tilt jako argumenty"
      pattern: "resetuj_czas_skanu\\(_serwa\\.kat_pan.*_serwa\\.kat_tilt\\)"
    - from: "ServoPID::skan_krok()"
      to: "_t_offset_pan, _t_offset_tilt"
      via: "addytywny offset w argumencie sin()"
      pattern: "t \\+ _t_offset_pan"
---

<objective>
Implementacja phase-offset continuity w firmware Arduino — eliminacja skoku serw przy przejsciu SLEDZENIE/BEZCZYNNOSC do SKANOWANIE + skrypt weryfikacji CSV.

Purpose: SCN-03 wymaga plynnej kontynuacji skanu z aktualnej pozycji serw. Obecna implementacja resetuje t=0, co powoduje skok do pozycji 0 stopni niezaleznie od aktualnej pozycji. Rozwiazanie: obliczenie t_offset przez arcsin z aktualnej pozycji serwa, osobno dla kazdej osi (per D-05, D-06).

Output: Zmodyfikowany firmware z phase-offset continuity + skrypt Python do analizy logow CSV
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-camera-color-fix/16-CONTEXT.md
@.planning/phases/16-camera-color-fix/16-RESEARCH.md

<interfaces>
<!-- Key types and contracts from firmware. Executor should use these directly. -->

From src/arduino/aries_controller/aries_controller.ino (class ServoPID):
```cpp
// Publiczne pola (linie ~225-230):
float kat_pan;           // aktualny kat pan (-60..+60)
float kat_tilt;          // aktualny kat tilt (-30..+30)
float ostatni_blad_x;    // ostatni blad X z ramki
float ostatni_blad_y;    // ostatni blad Y z ramki

// Metody publiczne:
void skan_krok(unsigned long teraz);     // linia 282 — sinusoida Lissajous
void resetuj_czas_skanu();               // linia 307 — reset czasu (DO MODYFIKACJI)
void pid_krok(unsigned long teraz, ...); // linia 248 — PID tick
void ustaw_serwa();                      // linia 294 — konwersja kat→Servo.write
void pid_reset();                        // linia 302

// Prywatne pola (do rozszerzenia):
unsigned long _czas_startowy_skanu;      // linia 324
// DODAC: float _t_offset_pan = 0.0f;
// DODAC: float _t_offset_tilt = 0.0f;
```

From src/arduino/aries_controller/aries_controller.ino (class MaszynaStanow):
```cpp
// _przejdz_do() — linia 694-708:
void _przejdz_do(StanSystemu nowy) {
    if (nowy == _stan_systemu) return;
    StanSystemu stary = _stan_systemu;
    _stan_systemu = nowy;
    _logger.loguj_zmiane_stanu(stary, nowy, _serwa.kat_pan, _serwa.kat_tilt);
    if (nowy == SKANOWANIE) {
        _serwa.resetuj_czas_skanu();  // ← ZMIENIC na resetuj_czas_skanu(_serwa.kat_pan, _serwa.kat_tilt)
        _serwa.pid_reset();
    } else if (nowy == SLEDZENIE) {
        _hmi.buzzer_beep();
        _serwa.pid_reset();
    }
}
```

Stale scan (linie 46-50):
```cpp
#define SCAN_FREQ_PAN   0.05f    // Hz
#define SCAN_FREQ_TILT  0.073f   // Hz
#define SCAN_AMP_PAN    70.0f    // stopnie
#define SCAN_AMP_TILT   25.0f    // stopnie
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-offset continuity w ServoPID + wywolanie w MaszynaStanow</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (caly plik — executor musi zobaczyc aktualny stan klasy ServoPID i MaszynaStanow)
  </read_first>
  <action>
Zmodyfikowac firmware w trzech miejscach (per D-05, D-06, D-07):

**1. Nowe prywatne pola w klasie ServoPID** (sekcja private, po linii `unsigned long _czas_startowy_skanu;`):
```cpp
float _t_offset_pan;    // sekundy — offset fazowy pan (phase-offset continuity)
float _t_offset_tilt;   // sekundy — offset fazowy tilt (phase-offset continuity)
```
Zainicjalizowac oba na `0.0f` w konstruktorze ServoPID (lub inline initialization `= 0.0f`).

**2. Zmodyfikowac `skan_krok()`** (linia ~282) — dodac offsety do argumentu sin():
```cpp
void skan_krok(unsigned long teraz) {
    float t = (teraz - _czas_startowy_skanu) / 1000.0f;  // sekundy
    kat_pan  = SCAN_AMP_PAN  * sin(2.0f * (float)M_PI * SCAN_FREQ_PAN  * (t + _t_offset_pan));
    kat_tilt = SCAN_AMP_TILT * sin(2.0f * (float)M_PI * SCAN_FREQ_TILT * (t + _t_offset_tilt));
    kat_pan  = constrain(kat_pan,  PAN_MIN,  PAN_MAX);
    kat_tilt = constrain(kat_tilt, TILT_MIN, TILT_MAX);
    ustaw_serwa();
}
```

**3. Zmodyfikowac `resetuj_czas_skanu()`** (linia ~307) — nowa sygnatura z obliczeniem arcsin:
```cpp
// Phase-offset continuity: obliczenie t_offset z aktualnej pozycji serwa (D-05, D-06)
// asin() zwraca [-pi/2, +pi/2] — pozycyjnie poprawne, moze odwrocic kierunek predkosci
// constrain ratio do [-1,1] zapobiega NaN (Pitfall 1 z RESEARCH.md)
void resetuj_czas_skanu(float aktualny_pan, float aktualny_tilt) {
    _czas_startowy_skanu = millis();
    float ratio_pan  = constrain(aktualny_pan  / SCAN_AMP_PAN,  -1.0f, 1.0f);
    float ratio_tilt = constrain(aktualny_tilt / SCAN_AMP_TILT, -1.0f, 1.0f);
    _t_offset_pan  = asin(ratio_pan)  / (2.0f * (float)M_PI * SCAN_FREQ_PAN);
    _t_offset_tilt = asin(ratio_tilt) / (2.0f * (float)M_PI * SCAN_FREQ_TILT);
}
```

**4. Zmodyfikowac wywolanie w `_przejdz_do()`** w klasie MaszynaStanow (linia ~700):
Zamienic:
```cpp
_serwa.resetuj_czas_skanu();
```
Na:
```cpp
_serwa.resetuj_czas_skanu(_serwa.kat_pan, _serwa.kat_tilt);
```

**5. Dodac `#include <math.h>`** na gorze pliku jesli jeszcze nie ma (dla asin(), M_PI). Na ARM Renesas math.h jest dostepny w stdlib — sprawdzic czy juz jest dolaczony przez inne naglowki.

UWAGA: Nie zmieniac stalych SCAN_AMP_PAN, SCAN_AMP_TILT, SCAN_FREQ_PAN, SCAN_FREQ_TILT (per D-08 — punkt startowy, strojenie w Plan 02 jesli potrzebne). Nie zmieniac `pid_krok()`, `ustaw_serwa()`, DataLogger, HMI, ani zadnych innych czesci firmware.

Komentarze po polsku. Zachowac istniejacy styl kodu (4-space indent, komentarze z `//`).
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && grep -n "_t_offset_pan" src/arduino/aries_controller/aries_controller.ino && grep -n "asin" src/arduino/aries_controller/aries_controller.ino && grep -n "resetuj_czas_skanu(_serwa" src/arduino/aries_controller/aries_controller.ino</automated>
  </verify>
  <acceptance_criteria>
    - aries_controller.ino contains `float _t_offset_pan` in ServoPID private section
    - aries_controller.ino contains `float _t_offset_tilt` in ServoPID private section
    - aries_controller.ino contains `asin(ratio_pan)` in resetuj_czas_skanu()
    - aries_controller.ino contains `asin(ratio_tilt)` in resetuj_czas_skanu()
    - aries_controller.ino contains `(t + _t_offset_pan)` in skan_krok()
    - aries_controller.ino contains `(t + _t_offset_tilt)` in skan_krok()
    - aries_controller.ino contains `resetuj_czas_skanu(_serwa.kat_pan, _serwa.kat_tilt)` in _przejdz_do()
    - aries_controller.ino contains `constrain(aktualny_pan / SCAN_AMP_PAN, -1.0f, 1.0f)` (Pitfall 1 protection)
    - No changes to SCAN_AMP_*, SCAN_FREQ_*, pid_krok(), DataLogger, HMI sections
  </acceptance_criteria>
  <done>Phase-offset continuity zaimplementowana: skan_krok() uzywa addytywnych offsetow, resetuj_czas_skanu() oblicza je przez arcsin, _przejdz_do(SKANOWANIE) przekazuje aktualna pozycje serw</done>
</task>

<task type="auto">
  <name>Task 2: Skrypt analizy CSV + weryfikacja kompilacji firmware</name>
  <files>tools/analiza_skoku_csv.py, src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (po zmianach z Task 1 — potwierdzic ze kompilacja sie uda)
    - .planning/phases/16-camera-color-fix/16-RESEARCH.md (sekcja Code Examples — skrypt CSV, linie 333-352)
  </read_first>
  <action>
**1. Utworzyc `tools/analiza_skoku_csv.py`** — skrypt do analizy logow CSV z karty SD pod katem SCN-03:

```python
#!/usr/bin/env python3
"""Analiza skoku serw przy przejsciu SLEDZENIE→SKANOWANIE z logow CSV DataLogger.

Uzycie:
    python3 tools/analiza_skoku_csv.py LYYMMDD.CSV

Sprawdza czy skok pan i tilt przy kazdym przejsciu SLEDZENIE→SKANOWANIE
nie przekracza progu 5 stopni (per D-11).

Kolumny CSV: timestamp, stan, pan, tilt, error_x, error_y, face_size, latency_ms
"""
import csv
import sys

PROG_SKOKU = 5.0  # stopnie — max akceptowalny skok (D-11)

def analizuj(sciezka_csv: str) -> bool:
    with open(sciezka_csv) as f:
        wiersze = list(csv.DictReader(f))

    przejscia = []
    for i in range(1, len(wiersze)):
        poprz, akt = wiersze[i - 1], wiersze[i]
        if poprz['stan'] == 'SLEDZENIE' and akt['stan'] == 'SKANOWANIE':
            delta_pan = abs(float(akt['pan']) - float(poprz['pan']))
            delta_tilt = abs(float(akt['tilt']) - float(poprz['tilt']))
            przejscia.append({
                'wiersz': i + 1,
                'delta_pan': delta_pan,
                'delta_tilt': delta_tilt,
            })

    if not przejscia:
        print("BRAK przejsc SLEDZENIE→SKANOWANIE w logu.")
        return True

    wszystkie_ok = True
    for p in przejscia:
        ok = p['delta_pan'] <= PROG_SKOKU and p['delta_tilt'] <= PROG_SKOKU
        status = 'PASS' if ok else 'FAIL'
        if not ok:
            wszystkie_ok = False
        print(f"Wiersz {p['wiersz']}: skok_pan={p['delta_pan']:.1f}°, "
              f"skok_tilt={p['delta_tilt']:.1f}° — {status}")

    print(f"\nWynik: {'PASS' if wszystkie_ok else 'FAIL'} "
          f"({len(przejscia)} przejsc, prog={PROG_SKOKU}°)")
    return wszystkie_ok


if __name__ == '__main__':
    if len(sys.argv) != 2:
        print(f"Uzycie: {sys.argv[0]} <sciezka_do_csv>")
        sys.exit(1)
    ok = analizuj(sys.argv[1])
    sys.exit(0 if ok else 1)
```

**2. Sprawdzic kompilacje firmware** (jesli arduino-cli jest dostepne):
```bash
arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino
```
Jesli arduino-cli nie jest zainstalowane na RPi, zweryfikowac skladnie recznie: brak brakujacych nawiasow, poprawne typy, brak niezadeklarowanych zmiennych. Kompilacja zostanie potwierdzona w Plan 02 na maszynie z Arduino IDE.

**3. Utworzyc katalog `tools/` jesli nie istnieje.**
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "import tools.analiza_skoku_csv" 2>/dev/null; test -f tools/analiza_skoku_csv.py && python3 tools/analiza_skoku_csv.py --help 2>&1 || python3 -c "exec(open('tools/analiza_skoku_csv.py').read().split(\"if __name__\")[0]); print('OK: modul importowalny')"</automated>
  </verify>
  <acceptance_criteria>
    - tools/analiza_skoku_csv.py exists and is executable Python
    - tools/analiza_skoku_csv.py contains `PROG_SKOKU = 5.0`
    - tools/analiza_skoku_csv.py contains `def analizuj(sciezka_csv: str) -> bool:`
    - tools/analiza_skoku_csv.py contains `SLEDZENIE` and `SKANOWANIE` string comparisons
    - tools/analiza_skoku_csv.py contains `delta_pan` and `delta_tilt` calculations
    - tools/analiza_skoku_csv.py returns exit code 1 on FAIL, 0 on PASS
    - tools/__init__.py exists (or script works standalone without it)
    - Firmware .ino file has no obvious syntax errors (matching braces, semicolons)
  </acceptance_criteria>
  <done>Skrypt analiza_skoku_csv.py gotowy do uzycia z logami SD, firmware skompilowany lub zweryfikowany skladniowo</done>
</task>

</tasks>

<verification>
1. `grep "_t_offset_pan" src/arduino/aries_controller/aries_controller.ino` zwraca 3+ trafien (deklaracja, uzycie w skan_krok, obliczenie w resetuj_czas_skanu)
2. `grep "resetuj_czas_skanu(_serwa" src/arduino/aries_controller/aries_controller.ino` zwraca 1 trafienie w _przejdz_do()
3. `python3 tools/analiza_skoku_csv.py` uruchamia sie bez bledow importu
4. Brak zmian w stalych SCAN_*, pid_krok(), DataLogger, HMI
</verification>

<success_criteria>
- Phase-offset continuity zaimplementowana w firmware: skan_krok() z offsetami, resetuj_czas_skanu() z arcsin, _przejdz_do() z przekazaniem pozycji
- Skrypt CSV gotowy do analizy logow po weryfikacji sprzetowej
- Firmware kompiluje sie lub jest skladniowo poprawny
- Gotowe do weryfikacji sprzetowej w Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/16-camera-color-fix/16-01-SUMMARY.md`
</output>
