---
phase: 16-camera-color-fix
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: false
requirements: [SCN-01, SCN-02, SCN-03]

must_haves:
  truths:
    - "Serwo tilt fizycznie oscyluje podczas stanu SKANOWANIE (SCN-01)"
    - "Pan i tilt poruszaja sie z roznymi czestotliwosciami tworzac wzorzec Lissajous (SCN-02)"
    - "Przejscie SLEDZENIE/TARGET_LOST do SKANOWANIE nie powoduje skoku >5 stopni (SCN-03)"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Firmware z phase-offset continuity zweryfikowany na hardware"
  key_links:
    - from: "Upload firmware"
      to: "Weryfikacja SC na hardware"
      via: "arduino-cli upload + obserwacja wizualna + logi CSV"
---

<objective>
Weryfikacja empiryczna firmware na Arduino Uno R4 WiFi — potwierdzenie SC #1 (tilt oscyluje), SC #2 (Lissajous 2D), SC #3 (brak skoku przy przejsciu do SKANOWANIE). Opcjonalne strojenie parametrow Lissajous jesli pokrycie FOV nieoptymalne.

Purpose: Kod z Plan 01 wymaga weryfikacji na fizycznym hardware — firmware Arduino nie ma unit testow, weryfikacja jest empiryczna per CLAUDE.md. SC #1 i #2 moga byc spelnione juz przez istniejacy kod (per D-01), SC #3 wymaga potwierdzenia nowej implementacji phase-offset.

Output: Firmware zweryfikowany na hardware, logi CSV potwierdzajace SC #1/#2/#3
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-camera-color-fix/16-CONTEXT.md
@.planning/phases/16-camera-color-fix/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kompilacja i upload firmware na Uno R4 WiFi</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (potwierdzic zmiany z Plan 01 sa na miejscu)
    - .planning/phases/16-camera-color-fix/16-01-SUMMARY.md (co dokladnie zmieniono)
  </read_first>
  <action>
**1. Kompilacja firmware:**
```bash
arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino
```
Jesli kompilacja sie nie uda — naprawic bledy (brakujacy `#include <math.h>`, literowki, brakujace sredniki). Typowe problemy:
- `asin` nie znaleziony — dodac `#include <math.h>` na gorze pliku
- `M_PI` nie zdefiniowany — dodac `#ifndef M_PI` / `#define M_PI 3.14159265358979323846f` / `#endif`
- Niezgodnosc typow — sprawdzic ze ratio_pan/tilt sa `float`, nie `double`

**2. Upload na Uno R4 WiFi:**
```bash
arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino
```
Jesli port inny niz /dev/ttyACM0, uzyc `arduino-cli board list` aby znalezc poprawny port.

**3. Po uploading:** Poczekac na bootscreen LCD (2s) i potwierdzic ze system startuje normalnie. Sprawdzic Serial Monitor (115200 baud) ze nie ma bledow inicjalizacji.
  </action>
  <verify>
    <automated>arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi /home/parolisko/ARIES-LITE/src/arduino/aries_controller/aries_controller.ino 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - arduino-cli compile exits with code 0
    - Output contains "Sketch uses" (memory usage line = successful compilation)
    - No "error:" lines in compilation output
    - Upload succeeds (or is deferred to checkpoint if hardware not connected)
  </acceptance_criteria>
  <done>Firmware skompilowany i wgrany na Uno R4 WiFi, system bootuje normalnie</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Weryfikacja empiryczna SC #1, #2, #3 na hardware</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <action>Checkpoint: uzytkownik weryfikuje firmware na fizycznym hardware. Testy SC #1 (tilt oscyluje), SC #2 (Lissajous 2D), SC #3 (skok leq 5 stopni). Opcjonalne strojenie parametrow Lissajous jesli pokrycie FOV nieoptymalne (per D-04, max 2-3 iteracje).</action>
  <verify>
    <automated>echo "checkpoint:human-verify — wymaga fizycznego hardware i obserwacji wizualnej"</automated>
  </verify>
  <what-built>
Phase-offset continuity w firmware Arduino — skan Lissajous 2D z plynna kontynuacja z aktualnej pozycji serw przy powrocie do SKANOWANIE. Zmiany: skan_krok() uzywa addytywnych _t_offset_pan/_t_offset_tilt, resetuj_czas_skanu() oblicza offsety przez arcsin().
  </what-built>
  <read_first>
    - .planning/phases/16-camera-color-fix/16-CONTEXT.md (D-10, D-11, D-12 — kryteria weryfikacji)
    - tools/analiza_skoku_csv.py (skrypt do analizy logow CSV)
  </read_first>
  <how-to-verify>
**Przygotowanie:**
- Uruchomic `sudo pigpiod` na RPi (jesli potrzebne)
- Podlaczyc Uno R4 WiFi przez USB do RPi
- Wlozyc karte SD do DataLogger Shield

**Test SC #1 — Tilt oscyluje w SKANOWANIE:**
1. Uruchomic system w trybie SKANOWANIE (lub poczekac na watchdog 500ms)
2. Obserwowac serwo tilt — musi fizycznie oscylowac gora-dol
3. Po 30s wyjac karte SD i sprawdzic kolumne tilt w CSV: `grep "SKANOWANIE" LYYMMDD.CSV | awk -F',' '{print $4}'` — wartosci musza zmieniac sie (nie stale 0.0)
4. PASS: tilt zmienia sie w zakresie ~-25..+25 stopni

**Test SC #2 — Wzorzec Lissajous:**
1. Podczas skanowania obserwowac ruch kamery — musi pokrywac 2D (nie tylko linia prosta)
2. W logach CSV: pan i tilt nie osiagaja szczytow w tym samym momencie (rozne czestotliwosci)
3. PASS: kamera porusza sie w obu osiach jednoczesnie z widocznym wzorcem 2D

**Test SC #3 — Brak skoku przy przejsciu (per D-11: max 5 stopni):**
1. Uruchomic pi_brain.py na RPi: `python3 run_pi_brain.py`
2. Pokazac twarz kamerze — system przejdzie w SLEDZENIE
3. Zakryc twarz — po histerezji (12 klatek) system wroci do SKANOWANIE
4. Obserwowac czy serwa NIE szarpna przy przejsciu — plynna kontynuacja
5. Powtorzyc 3-5 razy
6. Wyjac karte SD i uruchomic: `python3 tools/analiza_skoku_csv.py LYYMMDD.CSV`
7. PASS: wszystkie przejscia maja skok leq 5 stopni na obu osiach

**Opcjonalnie — strojenie Lissajous (per D-04, D-08, D-09):**
Jesli pokrycie FOV jest nieoptymalne (np. SCAN_AMP_PAN=70 clampuje za czesto na +/-60):
- Zmniejszyc SCAN_AMP_PAN do 55-58 stopni
- Ewentualnie zwiekszyc SCAN_AMP_TILT do 28-30 stopni (limit TILT_MAX=30)
- Przebudowac i przetestowac ponownie (max 2-3 iteracje per D-04)
  </how-to-verify>
  <resume-signal>
Wpisz "approved" jesli SC #1, #2, #3 PASS.
Opisz problemy jesli cos nie dziala (np. "tilt nie oscyluje", "skok >5 stopni", "clamp zbyt czesty").
Jesli strojono parametry — podaj nowe wartosci SCAN_AMP_PAN, SCAN_AMP_TILT.
  </resume-signal>
  <acceptance_criteria>
    - SC #1 PASS: tilt zmienia sie w zakresie ~-25..+25 w logach CSV podczas SKANOWANIE
    - SC #2 PASS: pan i tilt nie sa w fazie — ruch 2D widoczny wizualnie
    - SC #3 PASS: python3 tools/analiza_skoku_csv.py zwraca "PASS" dla wszystkich przejsc
    - Uzytkownik wpisal "approved" lub opisal problemy do naprawy
  </acceptance_criteria>
  <done>Wszystkie trzy SC potwierdzone empirycznie na fizycznym hardware. Phase-offset continuity dziala — brak skoku serw przy przejsciu do SKANOWANIE.</done>
</task>

</tasks>

<verification>
1. Firmware kompiluje sie i wgrywa na Uno R4 WiFi bez bledow
2. SC #1: tilt oscyluje w SKANOWANIE (wizualnie + CSV)
3. SC #2: wzorzec Lissajous 2D (wizualnie + CSV pan!=tilt frequency)
4. SC #3: skok leq 5 stopni przy SLEDZENIE to SKANOWANIE (analiza_skoku_csv.py PASS)
</verification>

<success_criteria>
- Firmware zweryfikowany na hardware — 3 SC confirmed
- Logi CSV dostepne jako dowod empiryczny
- Skrypt analiza_skoku_csv.py zwraca PASS
- Opcjonalne strojenie Lissajous wykonane jesli potrzebne
</success_criteria>

<output>
After completion, create `.planning/phases/16-camera-color-fix/16-02-SUMMARY.md`
</output>
