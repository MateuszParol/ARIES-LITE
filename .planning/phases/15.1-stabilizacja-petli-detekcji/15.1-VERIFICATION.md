---
phase: 15.1-stabilizacja-petli-detekcji
verified: 2026-04-04T14:00:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
human_verification:
  - test: "Uruchom python3 run_pi_brain.py na RPi4 — obserwuj okno HUD"
    expected: "Okno imshow ma rozmiar 640x480 (widocznie 2x wieksze niz przed zmiana)"
    why_human: "cv2.imshow rozmiar okna nie daje sie zweryfikowac statycznie — wymaga wyswietlacza"
  - test: "Uruchom python3 run_pi_brain.py na RPi4 — zaslon twarz na ~1s, odslonij"
    expected: "Brak szybkiego przelaczania SLEDZENIE->SKANOWANIE->SLEDZENIE w logach [LAT] TX; bufor 12 klatek trzyma SLEDZENIE przez chwilowe zasloniecia"
    why_human: "Zachowanie histerezy w czasie rzeczywistym wymaga obserwacji logow na zywym sprzecie"
  - test: "Uruchom python3 run_pi_brain.py na RPi4 — porownaj FPS z wersja przed faza 15.1"
    expected: "FPS w HUD wyzszy (DNN co 5 klatek zamiast co klatke), serwa poruszaja sie plynnie bez zamrozenia"
    why_human: "Poprawa FPS i plynnosc serw wymagaja empirycznej obserwacji na RPi4 (juz zatwierdzone przez uzytkownika)"
---

# Phase 15.1: Stabilizacja Petli Detekcji — Verification Report

**Phase Goal:** Petla detekcji DNN dziala stabilnie — skip-frames zmniejsza obciazenie CPU, histereza stanow zapobiega migotaniu SKANOWANIE/SLEDZENIE, okno imshow ma rozsadny rozmiar
**Verified:** 2026-04-04T14:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | DNN inference nie wykonuje sie co klatke — skip-frames redukuje wywolania DNN do co 5-tej klatki | VERIFIED | `DNN_SKIP_EVERY: int = 5` na linii 25 `detector.py`; logika `_klatka_licznik % DNN_SKIP_EVERY != 0` na linii 89; `return self._ostatnie_twarze` na linii 90 |
| 2 | Przejscia stanow SKANOWANIE/SLEDZENIE nie migocza — histereza wymaga 12 kolejnych klatek bez detekcji przed zmiana na SKANOWANIE | VERIFIED | `LOST_THRESHOLD: int = 12` na linii 14 `stabilizator.py`; `_klatki_bez_twarzy >= self._prog_utraty` na linii 66; wejscie w SLEDZENIE natychmiastowe (linijka 63) |
| 3 | Okno imshow ma rozmiar 640x480 (2x powiekszone z 320x240) | VERIFIED | `HUD_SCALE: int = 2` na linii 34 `brain.py`; `cv2.resize(..., (klatka.shape[1] * HUD_SCALE, klatka.shape[0] * HUD_SCALE), interpolation=cv2.INTER_NEAREST)` na linii 322-325; wymagana empiryczna weryfikacja rozmiaru okna na RPi4 — uzytkownik zatwierdzil poprawne dzialanie HUD |
| 4 | Miedzy DNN forward passami petla trzyma ostatni bbox (hold-last) — Arduino widzi ciagly ruch | VERIFIED | `self._ostatnie_twarze` zainicjalizowane na linii 64; ustawiane po kazdym udanym forward pass (linia 119); zwracane na klatkach skip (linia 90); resetowane przy wyjatku (linia 124) |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/vision/stabilizator.py` | StabilizatorStanow z LOST_THRESHOLD=12 | VERIFIED | 78 linii; klasa `StabilizatorStanow`; stala `LOST_THRESHOLD: int = 12`; metody `aktualizuj()`, `resetuj()`, property `stan`; klasy `TRYB_SKANOWANIE`, `TRYB_SLEDZENIE` |
| `src/vision/detector.py` | DNN skip-frames z hold-last bbox | VERIFIED | 196 linii; `DNN_SKIP_EVERY: int = 5`; `_klatka_licznik: int = 0`; `_ostatnie_twarze: List[...]`; skip logika w `wykryj()` na linii 88-90; hold-last na linii 119, 124 |
| `src/vision/brain.py` | Integracja stabilizatora + HUD resize | VERIFIED | 376 linii; import `StabilizatorStanow`; `HUD_SCALE: int = 2`; `self._stabilizator = StabilizatorStanow()`; `stabilizator.aktualizuj()` na linii 173; `cv2.resize` z `INTER_NEAREST` na linii 322-325 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `src/vision/brain.py` | `src/vision/stabilizator.py` | `from .stabilizator import StabilizatorStanow`, wywolanie `aktualizuj()` | WIRED | Import na linii 24; `self._stabilizator.aktualizuj(bbox_wykryty=...)` na linii 173; wynik `stan` uzyty na linii 176 |
| `src/vision/brain.py` | `src/vision/detector.py` | `self._detektor.wykryj()` z wbudowanym skip-frames | WIRED | `self._detektor = WykrywaczTwarzy()` na linii 97; `twarze = self._detektor.wykryj(klatka, timestamp_ms)` na linii 167 |
| `src/vision/brain.py` | `cv2.resize` | HUD_SCALE resize przed imshow | WIRED | `klatka_hud = cv2.resize(klatka, (...* HUD_SCALE, ...* HUD_SCALE), interpolation=cv2.INTER_NEAREST)` na linii 322-325; `cv2.imshow(... klatka_hud)` na linii 327 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `brain.py` — TX tryb | `stan` z `stabilizator.aktualizuj()` | `StabilizatorStanow._klatki_bez_twarzy` licznik aktualizowany co klatke | Tak — wartosc pochodzi z rzeczywistej historii detekcji (`bbox is not None`) | FLOWING |
| `detector.py` — `wykryj()` | `_ostatnie_twarze` | DNN forward pass przez `cv2.dnn.readNetFromCaffe` / `self._net.forward()` | Tak — wynik prawdziwej sieci neuronowej; hold-last poprawnie aktualizuje po kazdym pelnym passie | FLOWING |
| `brain.py` — HUD resize | `klatka_hud` | `cv2.resize(klatka, ...*HUD_SCALE)` | Tak — transformacja na rzeczywistej klatce z kamery, nie na statycznym danych | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED — system wymaga Picamera2 i modeli DNN dostepnych tylko na RPi4. Empiryczna weryfikacja wykonana przez uzytkownika (zatwierdzona w Task 3 SUMMARY):

| Behavior | Method | Result | Status |
|----------|--------|--------|--------|
| FPS w HUD wyzszy po skip-frames | Uzytkownik obserwuje HUD na RPi4 | FPS wyraznie plynniejszy | PASS (zatwierdzone przez uzytkownika) |
| Plynne serwa (hold-last bbox) | Uzytkownik obserwuje ruch serw | Plynne poruszanie bez zamrozenia | PASS (zatwierdzone przez uzytkownika) |
| HUD okno czytelny rozmiar | Uzytkownik obserwuje wyswietlacz | Brak skarg na rozmiar okna | PASS (zatwierdzone przez uzytkownika) |
| Brak migotania stanow | Obserwacja logow TX | Brak raportu migotania | PASS (zatwierdzone przez uzytkownika) |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|------------|-------------|-------------|--------|----------|
| DET-01 | 15.1-01-PLAN.md | DNN skip-frames — redukcja wywolan DNN do co N-tej klatki | SATISFIED | `DNN_SKIP_EVERY=5` w `detector.py` linia 25; skip logika linie 88-90 |
| DET-02 | 15.1-01-PLAN.md | Hold-last bbox miedzy forward passami | SATISFIED | `_ostatnie_twarze` aktualizowane na linii 119, zwracane na linii 90 |
| DET-03 | 15.1-01-PLAN.md | Histereza stanow SKANOWANIE/SLEDZENIE | SATISFIED | `StabilizatorStanow` z `LOST_THRESHOLD=12` w `stabilizator.py`; integracja w `brain.py` linia 173 |

**Uwaga — ORPHANED requirements:** Identyfikatory DET-01, DET-02, DET-03 sa przywolane w PLAN frontmatter i ROADMAP.md (Phase 15.1), ale NIE ISTNIEJA w `.planning/REQUIREMENTS.md`. Plik REQUIREMENTS.md zawiera tylko prefixy: MIG, RTC, LOG, INT, SER, ARD, VIS, WEB, M5S, TOF. Brak sekcji "DET" (Detekcja). Wymagania sa w pracy udokumentowane w dokumencie badawczym 15.1-RESEARCH.md (D-01 przez D-08) i ROADMAP.md Success Criteria, ale nie zostaly formalnie dodane do REQUIREMENTS.md. Jest to luka w dokumentacji, nie w implementacji.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|---------|--------|
| Brak | - | Brak TODO/FIXME/placeholder | - | Czyste |

Przeskanowane pliki:
- `src/vision/stabilizator.py` — brak anti-patternow; implementacja kompletna
- `src/vision/detector.py` — brak anti-patternow; `return self._ostatnie_twarze` jest prawidlowym hold-last (nie stubem — `_ostatnie_twarze` jest ustawiane po kazdym DNN forward pass)
- `src/vision/brain.py` — brak anti-patternow; `if bbox is not None` na linii 289 jest w `_rysuj_hud()` dla rysowania prostokata HUD, nie w logice TX — TX jest sterowane przez `stabilizator.aktualizuj()` na linii 173

---

### Human Verification Required

#### 1. Rozmiar okna HUD (640x480)

**Test:** Uruchom `python3 run_pi_brain.py` na RPi4 z podlaczonym wyswietlaczem. Obserwuj rozmiar okna imshow.
**Expected:** Okno imshow wyswietla sie jako 640x480 pikseli (widocznie 2x wieksze niz stare 320x240).
**Why human:** Rozmiar okna cv2.imshow nie jest weryfikowalny statycznie — wymaga aktywnego wyswietlacza na RPi4.

**STATUS: Zatwierdzone przez uzytkownika** (Task 3 SUMMARY "Uzytkownik zatwierdzil poprawne dzialanie: brak migotania stanow, wyzszy FPS, czytelny HUD 640x480, plynne serwa.")

#### 2. Brak migotania stanow SKANOWANIE/SLEDZENIE

**Test:** Uruchom `python3 run_pi_brain.py` na RPi4. Pokaz twarz, zaslonij na ~0.5s, odslonij. Obserwuj logi `[LAT] TX`.
**Expected:** System trzyma stan SLEDZENIE przez krotkie zasloniecia. Brak szybkiego przelaczania SLEDZENIE->SKANOWANIE->SLEDZENIE w logach.
**Why human:** Zachowanie histerezy w czasie rzeczywistym wymaga obserwacji logow na zywym sprzecie z Picamera2.

**STATUS: Zatwierdzone przez uzytkownika** (Task 3 SUMMARY — "brak migotania stanow" zatwierdzone).

#### 3. Plynne serwa + wyzszy FPS (skip-frames)

**Test:** Uruchom `python3 run_pi_brain.py` na RPi4. Obserwuj FPS w HUD i ruch serw przy sledzeniu twarzy.
**Expected:** FPS wyzszy niz przed faza 15.1 (DNN co 5 klatek zamiast co klatke). Serwa poruszaja sie plynnie bez widocznego zamrozenia na 4 klatkach skip.
**Why human:** Poprawa FPS wymaga porownania przed/po i obserwacji serw na zywym sprzecie.

**STATUS: Zatwierdzone przez uzytkownika** (Task 3 SUMMARY — "wyzszy FPS" i "plynne serwa" zatwierdzone).

---

### Commits

| Commit | Opis | Status |
|--------|------|--------|
| `bef3563` | feat(15.1-01): StabilizatorStanow + DNN skip-frames w WykrywaczTwarzy | Zweryfikowany — istnieje w repo |
| `06adc51` | feat(15.1-01): integracja StabilizatorStanow i HUD resize w brain.py | Zweryfikowany — istnieje w repo |

---

### Gaps Summary

Brak gap — wszystkie 4 must-haves zweryfikowane. Wszystkie artifakty istnieja, sa substantive (nie stuby), i sa prawidlowo podlaczone. Przeplyw danych potwierdzony (Level 4). Empiryczna weryfikacja na RPi4 wykonana i zatwierdzona przez uzytkownika.

**Jedyna luka to dokumentacyjna:** DET-01, DET-02, DET-03 sa wymienione w PLAN i ROADMAP ale nie zostaly dodane do `.planning/REQUIREMENTS.md`. Nie blokuje to phase goal — implementacja jest kompletna i dziala.

---

_Verified: 2026-04-04T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
