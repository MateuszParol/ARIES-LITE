---
phase: 15.1-stabilizacja-petli-detekcji
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vision/stabilizator.py
  - src/vision/detector.py
  - src/vision/brain.py
autonomous: false
requirements: [DET-01, DET-02, DET-03]

must_haves:
  truths:
    - "DNN inference nie wykonuje sie co klatke — skip-frames redukuje wywolania DNN do co 5-tej klatki"
    - "Przejscia stanow SKANOWANIE/SLEDZENIE nie migocza — histereza wymaga 12 kolejnych klatek bez detekcji przed zmiana na SKANOWANIE"
    - "Okno imshow ma rozmiar 640x480 (2x powiekszone z 320x240)"
    - "Miedzy DNN forward passami petla trzyma ostatni bbox (hold-last) — Arduino widzi ciagly ruch"
  artifacts:
    - path: "src/vision/stabilizator.py"
      provides: "StabilizatorStanow — histereza stanow SKANOWANIE/SLEDZENIE"
      exports: ["StabilizatorStanow"]
      min_lines: 30
    - path: "src/vision/detector.py"
      provides: "DNN skip-frames z hold-last bbox"
      contains: "_klatka_licznik"
    - path: "src/vision/brain.py"
      provides: "Integracja stabilizatora + HUD resize"
      contains: "HUD_SCALE"
  key_links:
    - from: "src/vision/brain.py"
      to: "src/vision/stabilizator.py"
      via: "import StabilizatorStanow, wywolanie aktualizuj()"
      pattern: "stabilizator\\.aktualizuj"
    - from: "src/vision/brain.py"
      to: "src/vision/detector.py"
      via: "wykryj() z wbudowanym skip-frames hold-last"
      pattern: "self\\._detektor\\.wykryj"
    - from: "src/vision/brain.py"
      to: "cv2.resize"
      via: "HUD_SCALE resize przed imshow"
      pattern: "cv2\\.resize.*HUD_SCALE"
---

<objective>
Stabilizacja petli detekcji w MozgRPi: (1) DNN skip-frames redukuje obciazenie CPU przez wykonywanie forward pass co 5-ta klatke z hold-last bbox miedzy nimi, (2) histereza stanow SKANOWANIE/SLEDZENIE przez nowa klase StabilizatorStanow zapobiega migotaniu trybu TX, (3) resize okna imshow z 320x240 do 640x480 dla czytelnego debugowania.

Purpose: Petla glowna brain.py aktualnie wykonuje DNN co klatke (zbedne obciazenie CPU) i natychmiast przelacza tryb TX na kazda klatke bez/z detekcja (migotanie stanow). Okno imshow 320x240 jest za male na czytelne debugowanie.

Output: Nowy plik stabilizator.py, zmodyfikowany detector.py (skip-frames), zmodyfikowany brain.py (integracja + resize).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-stabilizacja-petli-detekcji/15.1-CONTEXT.md
@.planning/phases/15.1-stabilizacja-petli-detekcji/15.1-RESEARCH.md
@src/vision/brain.py
@src/vision/detector.py

<interfaces>
<!-- Istniejace interfejsy z detector.py uzywane przez brain.py -->

From src/vision/detector.py:
```python
class WykrywaczTwarzy:
    def wykryj(self, klatka_bgr: np.ndarray, timestamp_ms: int = 0) -> List[Tuple[int, int, int, int]]: ...
    def wybierz_twarz(self, twarze: List[Tuple[int, int, int, int]]) -> Optional[Tuple[int, int, int, int]]: ...
    def zamknij(self) -> None: ...
```

From src/vision/brain.py:
```python
# Stale modulowe uzywane w TX:
TRYB_SKANOWANIE: int = 1
TRYB_SLEDZENIE: int = 2

class MozgRPi:
    def _petla_glowna(self) -> None: ...    # linia 141-209 — glowna petla do modyfikacji
    def _rysuj_hud(self, klatka, bbox, error_x, error_y, tryb) -> None: ...  # linia 255-321 — HUD do modyfikacji
```

From src/vision/serial_interface.py:
```python
class SerialInterface:
    def send_frame(self, mode: int, error_x: int, error_y: int, face_size: int) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stworz StabilizatorStanow + dodaj DNN skip-frames do WykrywaczTwarzy</name>
  <files>src/vision/stabilizator.py, src/vision/detector.py</files>

  <read_first>
    - src/vision/detector.py (aktualny kod WykrywaczTwarzy — metoda wykryj() do modyfikacji)
    - src/vision/brain.py (petla glowna — zrozumienie jak wykryj() jest wywolywany)
    - legacy/src/modes/test_tracker.py (linijki 24-36: stale DNN_SKIP_EVERY, STREAK_REQUIRED — wzorzec referencyjny)
    - .planning/phases/15.1-stabilizacja-petli-detekcji/15.1-RESEARCH.md (Pattern 1 i Pattern 2 — wzorce implementacji)
  </read_first>

  <action>
  **Plik 1: src/vision/stabilizator.py (NOWY)**

  Stworz nowy plik z klasa StabilizatorStanow implementujaca histereze stanow SKANOWANIE/SLEDZENIE (per D-08 — osobny plik w src/vision/).

  Zawartosc pliku:
  - Docstring modulu po polsku: "Stabilizator stanow SKANOWANIE/SLEDZENIE z histereza dla ARIES-LITE v2.1."
  - Import: `logging`, `from typing import Optional`
  - Stala modulowa: `LOST_THRESHOLD: int = 12` (per D-04: 10-15 klatek, 12 jako kompromis)
  - Klasa `StabilizatorStanow`:
    - Stale klasowe: `TRYB_SKANOWANIE = "SKANOWANIE"`, `TRYB_SLEDZENIE = "SLEDZENIE"`
    - `__init__(self, prog_utraty: int = LOST_THRESHOLD) -> None`:
      - `self._stan: str = self.TRYB_SKANOWANIE` (start w skanowaniu)
      - `self._prog_utraty: int = prog_utraty`
      - `self._klatki_bez_twarzy: int = 0`
    - Property `stan` -> str: zwraca `self._stan`
    - `aktualizuj(self, bbox_wykryty: bool) -> str`:
      - Jesli `bbox_wykryty`: reset `_klatki_bez_twarzy = 0`, ustaw `_stan = TRYB_SLEDZENIE` (wejscie natychmiastowe — per Research: brak streak filtra, DNN jest wystarczajaco precyzyjny per D-07)
      - Jesli nie `bbox_wykryty`: inkrementuj `_klatki_bez_twarzy += 1`; zmiana na SKANOWANIE TYLKO gdy `_klatki_bez_twarzy >= self._prog_utraty`
      - Return: aktualny `self._stan`
    - `resetuj(self) -> None`: reset stanu do SKANOWANIE i wyzeruj licznik (do uzytku przy restart)

  **Plik 2: src/vision/detector.py (MODYFIKACJA)**

  Dodaj DNN skip-frames z hold-last bbox do klasy WykrywaczTwarzy (per D-01, D-02).

  Zmiany:
  1. Dodaj stala modulowa na gorze pliku (po istniejacych stalych):
     `DNN_SKIP_EVERY: int = 5  # co N-ta klatka pelny DNN forward pass (D-02, konfigurowalne)`
  2. W `__init__()` dodaj po `self._sticky_bbox`:
     - `self._klatka_licznik: int = 0`
     - `self._ostatnie_twarze: List[Tuple[int, int, int, int]] = []`
  3. W `wykryj()` — na poczatku metody (przed try), dodaj logike skip:
     ```python
     self._klatka_licznik += 1
     if self._klatka_licznik % DNN_SKIP_EVERY != 0:
         return self._ostatnie_twarze  # hold last — zwroc poprzedni wynik
     ```
  4. Na koncu metody `wykryj()`, tuz przed `return twarze`, dodaj:
     `self._ostatnie_twarze = twarze`
  5. W bloku except (linia 108-110), dodaj `self._ostatnie_twarze = []` przed `return []`
  6. Zaktualizuj docstring metody `wykryj()` — dodaj informacje o skip-frames: "Wykonuje pelny DNN forward pass co DNN_SKIP_EVERY klatek. Miedzy nimi zwraca ostatni wynik (hold-last bbox)."
  </action>

  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "
from src.vision.stabilizator import StabilizatorStanow, LOST_THRESHOLD
s = StabilizatorStanow()
# Test 1: start w SKANOWANIE
assert s.stan == 'SKANOWANIE', f'Start: {s.stan}'
# Test 2: natychmiastowe wejscie w SLEDZENIE
assert s.aktualizuj(True) == 'SLEDZENIE'
# Test 3: 11 klatek bez twarzy — nadal SLEDZENIE (prog 12)
for _ in range(11):
    s.aktualizuj(False)
assert s.stan == 'SLEDZENIE', f'Po 11: {s.stan}'
# Test 4: 12-ta klatka — zmiana na SKANOWANIE
assert s.aktualizuj(False) == 'SKANOWANIE'
# Test 5: DNN_SKIP_EVERY w detector
from src.vision.detector import DNN_SKIP_EVERY
assert DNN_SKIP_EVERY == 5, f'DNN_SKIP_EVERY={DNN_SKIP_EVERY}'
# Test 6: LOST_THRESHOLD
assert LOST_THRESHOLD == 12
print('ALL TESTS PASSED')
"</automated>
  </verify>

  <acceptance_criteria>
    - src/vision/stabilizator.py istnieje i zawiera `class StabilizatorStanow`
    - src/vision/stabilizator.py zawiera `LOST_THRESHOLD: int = 12`
    - src/vision/stabilizator.py zawiera metody `aktualizuj`, `resetuj`, property `stan`
    - src/vision/detector.py zawiera `DNN_SKIP_EVERY: int = 5`
    - src/vision/detector.py zawiera `self._klatka_licznik: int = 0`
    - src/vision/detector.py zawiera `self._ostatnie_twarze`
    - src/vision/detector.py: metoda wykryj() zawiera `self._klatka_licznik % DNN_SKIP_EVERY != 0`
    - python3 -c "from src.vision.stabilizator import StabilizatorStanow" exits 0
    - python3 -c "from src.vision.detector import DNN_SKIP_EVERY" exits 0
  </acceptance_criteria>

  <done>StabilizatorStanow klasa gotowa z histereza (LOST_THRESHOLD=12, wejscie natychmiastowe, wyjscie po 12 klatkach). WykrywaczTwarzy ma DNN skip-frames (co 5 klatek forward pass, hold-last miedzy nimi). Oba moduly importowalne bez bledow.</done>
</task>

<task type="auto">
  <name>Task 2: Integracja stabilizatora i HUD resize w brain.py</name>
  <files>src/vision/brain.py</files>

  <read_first>
    - src/vision/brain.py (caly plik — petla glowna linia 141-209, HUD linia 255-321)
    - src/vision/stabilizator.py (nowo stworzony — interfejs StabilizatorStanow)
    - src/vision/detector.py (zmodyfikowany — potwierdzic skip-frames w wykryj())
    - .planning/phases/15.1-stabilizacja-petli-detekcji/15.1-RESEARCH.md (Code Examples — integracja w _petla_glowna i _rysuj_hud)
  </read_first>

  <action>
  **Modyfikacja src/vision/brain.py:**

  1. **Import** — dodaj na gorze (po istniejacych importach z .):
     `from .stabilizator import StabilizatorStanow`

  2. **Stala HUD_SCALE** (per D-06) — dodaj po istniejacych stalych modulowych (po linii TRYB_SLEDZENIE):
     `HUD_SCALE: int = 2  # mnoznik rozdzielczosci imshow: 320*2=640, 240*2=480 (D-06)`

  3. **__init__()** — dodaj po `self._czas_ostatniej_tx`:
     `self._stabilizator = StabilizatorStanow()  # histereza SKANOWANIE/SLEDZENIE (D-08)`

  4. **_petla_glowna()** — zastap blok if/else (linie 166-202) nowa logika z stabilizatorem:

     Stara logika (usunac linie 166-202):
     ```python
     if bbox is not None:
         # ... TX SLEDZENIE ...
     else:
         # ... TX SKANOWANIE ...
     ```

     Nowa logika (wstawic w to samo miejsce):
     ```python
     # Aktualizuj stabilizator — histereza stanow (D-04: prog 12 klatek)
     stan = self._stabilizator.aktualizuj(bbox_wykryty=(bbox is not None))

     # Ustal tryb TX na podstawie stabilizatora
     if stan == StabilizatorStanow.TRYB_SLEDZENIE and bbox is not None:
         tryb_int = TRYB_SLEDZENIE
         tryb = "SLEDZENIE"
         error_x, error_y, face_size = self._oblicz_error(bbox, klatka.shape)
     else:
         tryb_int = TRYB_SKANOWANIE
         tryb = "SKANOWANIE"
         error_x, error_y, face_size = 0, 0, 0

     # Wyslij ramke TX do Arduino
     try:
         czas_przed_tx = time.monotonic_ns() // 1_000_000
         self._serial.send_frame(
             mode=tryb_int,
             error_x=error_x,
             error_y=error_y,
             face_size=face_size,
         )
         czas_po_tx = time.monotonic_ns() // 1_000_000
         self._czas_ostatniej_tx[0] = time.time()

         if tryb_int == TRYB_SLEDZENIE:
             logger.info(
                 f"[LAT] TX SLEDZENIE: {czas_po_tx - czas_przed_tx}ms "
                 f"err_x={error_x} err_y={error_y} ts={czas_po_tx}"
             )
         else:
             self._licznik_scan_log += 1
             if self._licznik_scan_log % 50 == 0:
                 logger.info(f"[LAT] TX SKANOWANIE: heartbeat ok ts={czas_po_tx}")
     except Exception as e:
         logger.error(f"TX ({tryb}) blad: {e}")
     ```

  5. **_rysuj_hud()** — dodaj cv2.resize przed cv2.imshow (linia ~317). Zastap blok imshow:

     Stary kod:
     ```python
     if not self._headless:
         try:
             cv2.imshow("ARIES-LITE pi_brain", klatka)
             cv2.waitKey(1)
         except cv2.error:
             ...
     ```

     Nowy kod:
     ```python
     if not self._headless:
         try:
             klatka_hud = cv2.resize(
                 klatka,
                 (klatka.shape[1] * HUD_SCALE, klatka.shape[0] * HUD_SCALE),
                 interpolation=cv2.INTER_NEAREST,
             )
             cv2.imshow("ARIES-LITE pi_brain", klatka_hud)
             cv2.waitKey(1)
         except cv2.error:
             self._headless = True
             logger.info("Headless mode — brak wyswietlacza, cv2.imshow wylaczony")
     ```

  6. **Docstring modulu** — zaktualizuj wersje z "v2.0" na "v2.1" i dodaj informacje o stabilizatorze.
  </action>

  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && python3 -c "
from src.vision.brain import MozgRPi, HUD_SCALE, TRYB_SKANOWANIE, TRYB_SLEDZENIE
from src.vision.stabilizator import StabilizatorStanow
# Test 1: HUD_SCALE = 2
assert HUD_SCALE == 2, f'HUD_SCALE={HUD_SCALE}'
# Test 2: MozgRPi importuje sie bez bledu
m = MozgRPi.__new__(MozgRPi)
# Test 3: TRYB constants
assert TRYB_SKANOWANIE == 1
assert TRYB_SLEDZENIE == 2
print('ALL IMPORT TESTS PASSED')
" && grep -c 'stabilizator.aktualizuj' src/vision/brain.py && grep -c 'HUD_SCALE' src/vision/brain.py && grep -c 'cv2.resize' src/vision/brain.py && grep -c 'INTER_NEAREST' src/vision/brain.py</automated>
  </verify>

  <acceptance_criteria>
    - src/vision/brain.py zawiera `from .stabilizator import StabilizatorStanow`
    - src/vision/brain.py zawiera `HUD_SCALE: int = 2`
    - src/vision/brain.py zawiera `self._stabilizator = StabilizatorStanow()`
    - src/vision/brain.py: _petla_glowna() zawiera `self._stabilizator.aktualizuj(bbox_wykryty=`
    - src/vision/brain.py: _petla_glowna() zawiera `StabilizatorStanow.TRYB_SLEDZENIE`
    - src/vision/brain.py: _rysuj_hud() zawiera `cv2.resize(` z `HUD_SCALE` i `INTER_NEAREST`
    - src/vision/brain.py NIE zawiera starego bloku `if bbox is not None:` / `else:` z bezposrednim TX (zastapiony stabilizatorem)
    - python3 -c "from src.vision.brain import MozgRPi, HUD_SCALE" exits 0
    - grep 'stabilizator.aktualizuj' src/vision/brain.py zwraca >= 1 wynik
    - grep 'cv2.resize' src/vision/brain.py zwraca >= 1 wynik
  </acceptance_criteria>

  <done>brain.py zintegrowany ze StabilizatorStanow — tryb TX okreslany przez histereze (nie bezposredni if/else). HUD skalowany 2x przez cv2.resize z INTER_NEAREST. Stara logika bezposredniego przelaczania zastapiona wywolaniem stabilizator.aktualizuj().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Weryfikacja empiryczna na RPi4</name>
  <files>src/vision/brain.py, src/vision/detector.py, src/vision/stabilizator.py</files>
  <action>
  Weryfikacja empiryczna zmian na RPi4. Uzytkownik uruchamia system i obserwuje zachowanie:

  **Co zbudowano:**
  1. DNN skip-frames (co 5 klatek forward pass, hold-last bbox miedzy nimi)
  2. Histereza stanow (StabilizatorStanow — prog 12 klatek bez detekcji przed SKANOWANIE)
  3. HUD resize (640x480 zamiast 320x240)

  **Kroki weryfikacji:**
  1. Na RPi4 uruchom: `cd /home/parolisko/ARIES-LITE && source venv/bin/activate && python3 run_pi_brain.py`
  2. **HUD resize:** Sprawdz rozmiar okna imshow — powinno byc 640x480 (2x wieksze niz poprzednio 320x240). Tekst HUD (Tryb, eX/eY, FPS) czytelny.
  3. **DNN skip-frames:** Obserwuj FPS w HUD — powinien byc wyzszy niz poprzednio (DNN co 5 klatek zamiast co klatke).
  4. **Histereza stanow:** Pokaz twarz kamerze, potem zaslonij reka na ~0.5s i odslonij. W logach terminala NIE powinno byc szybkiego przelaczania SLEDZENIE->SKANOWANIE->SLEDZENIE. System powinien trzymac SLEDZENIE przez krotkie zasloniecia (bufor 12 klatek ~0.8s).
  5. **Hold-last bbox:** Podczas sledzenia twarzy serwa powinny poruszac sie plynnie — bez zamrozenia na 4 klatki (hold bbox utrzymuje ciaglosc TX).
  6. Zatrzymaj Ctrl+C — sprawdz czy czyste wyjscie bez bledow.
  </action>
  <verify>Uzytkownik potwierdza "approved" lub opisuje problemy</verify>
  <done>Uzytkownik zatwierdzil poprawne dzialanie: brak migotania stanow, wyzszy FPS, czytelny HUD 640x480, plynne serwa.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from src.vision.stabilizator import StabilizatorStanow"` — import bez bledu
2. `python3 -c "from src.vision.detector import DNN_SKIP_EVERY; assert DNN_SKIP_EVERY == 5"` — stala skip
3. `python3 -c "from src.vision.brain import HUD_SCALE; assert HUD_SCALE == 2"` — stala HUD
4. `grep -c 'stabilizator.aktualizuj' src/vision/brain.py` — >= 1 (integracja)
5. `grep -c 'cv2.resize' src/vision/brain.py` — >= 1 (HUD resize)
6. `grep -c '_klatka_licznik' src/vision/detector.py` — >= 2 (skip logika)
</verification>

<success_criteria>
- StabilizatorStanow klasa w osobnym pliku stabilizator.py z histereza (LOST_THRESHOLD=12)
- DNN skip-frames w detector.py (DNN_SKIP_EVERY=5, hold-last bbox)
- brain.py integruje stabilizator zamiast bezposredniego if/else na bbox
- HUD resize 2x w brain.py (HUD_SCALE=2, INTER_NEAREST)
- Weryfikacja empiryczna na RPi4 potwierdza: brak migotania stanow, wyzszy FPS, czytelny HUD
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-stabilizacja-petli-detekcji/15.1-01-SUMMARY.md`
</output>
