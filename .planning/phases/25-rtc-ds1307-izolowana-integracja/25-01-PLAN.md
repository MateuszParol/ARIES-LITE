---
phase: 25-rtc-ds1307-izolowana-integracja
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arduino/i2c_scanner/i2c_scanner.ino
autonomous: false
requirements: [RTC-01, INT-07]
user_setup: []

must_haves:
  truths:
    - "arduino-cli lib list pokazuje RTClib 2.1.x i Adafruit BusIO zainstalowane"
    - "I2C scanner sketch kompiluje sie na target arduino:renesas_uno:unor4wifi"
    - "Serial Monitor po flashu na Uno R4 WiFi wypisuje 'I2C znaleziony: 0x68'"
  artifacts:
    - path: "src/arduino/i2c_scanner/i2c_scanner.ino"
      provides: "Diagnostyczny sketch I2C scanner"
      contains: "Wire.beginTransmission"
  key_links:
    - from: "i2c_scanner.ino"
      to: "DS1307 na DataLogger Shield"
      via: "Wire I2C master na A4/A5"
      pattern: "0x68"
---

<objective>
Zainstalowac biblioteki RTClib + BusIO, stworzyc diagnostyczny I2C scanner sketch i zweryfikowac fizyczne polaczenie I2C z DS1307 na DataLogger Shield.

Purpose: Per D-02 — potwierdzenie fizycznego polaczenia I2C PRZED jakimkolwiek kodem RTClib. Eliminacja ryzyka Pitfall 1 (styki shielda).
Output: Biblioteki zainstalowane, sketch I2C scanner, potwierdzenie 0x68 na Serial Monitor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rtc-ds1307-izolowana-integracja/25-CONTEXT.md
@.planning/phases/25-rtc-ds1307-izolowana-integracja/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalacja RTClib + BusIO i stworzenie I2C scanner sketch</name>
  <files>src/arduino/i2c_scanner/i2c_scanner.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (istniejacy firmware — wzorzec komentarzy i formatowania)
    - .planning/phases/25-rtc-ds1307-izolowana-integracja/25-RESEARCH.md (sekcja "Instalacja Wave 0 prerequisite" i "Pattern 4: I2C Scanner")
  </read_first>
  <action>
1. Zainstaluj biblioteki arduino-cli (Wave 0 prerequisite):
   ```bash
   arduino-cli lib install "RTClib"
   arduino-cli lib install "Adafruit BusIO"
   ```
   Zweryfikuj instalacje:
   ```bash
   arduino-cli lib list | grep -i "rtclib\|busio"
   ```
   Oczekiwany wynik: RTClib 2.1.x i Adafruit BusIO w liscie.

2. Stworz katalog `src/arduino/i2c_scanner/` i plik `i2c_scanner.ino` — diagnostyczny sketch per D-02.

3. Tresc sketcha (kompletna — executor NIE musi szukac wzorca):
   ```cpp
   // i2c_scanner.ino — Diagnostyka I2C dla ARIES-LITE v2.1
   // Skanuje magistrale I2C (A4/A5) i wypisuje znalezione adresy.
   // Oczekiwany wynik: DS1307 pod adresem 0x68 (DataLogger Shield)

   #include <Wire.h>

   void setup() {
       Serial.begin(115200);
       uint32_t start = millis();
       while (!Serial && millis() - start < 500) { delay(10); }

       Wire.begin();  // I2C master — BEZ argumentu (Pitfall 5: Wire.begin(addr) = slave!)
       Serial.println("=== ARIES-LITE I2C Scanner ===");
       Serial.println("Skanowanie adresow 0x01 - 0x7F...");

       uint8_t znalezione = 0;
       for (uint8_t addr = 1; addr < 127; addr++) {
           Wire.beginTransmission(addr);
           uint8_t wynik = Wire.endTransmission();
           if (wynik == 0) {
               Serial.print("I2C znaleziony: 0x");
               if (addr < 16) Serial.print("0");
               Serial.println(addr, HEX);
               znalezione++;
           }
       }

       Serial.print("Znalezione urzadzenia: ");
       Serial.println(znalezione);

       if (znalezione == 0) {
           Serial.println("BLAD: Brak urzadzen I2C! Sprawdz osadzenie DataLogger Shield.");
       }
       Serial.println("Skan zakonczony.");
   }

   void loop() {}
   ```

4. Zweryfikuj kompilacje na target Uno R4 WiFi:
   ```bash
   arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/i2c_scanner/i2c_scanner.ino
   ```
   Oczekiwany wynik: zero bledow kompilacji.
  </action>
  <verify>
    <automated>arduino-cli lib list | grep -i "rtclib" && arduino-cli lib list | grep -i "busio" && arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/i2c_scanner/i2c_scanner.ino 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `arduino-cli lib list` zawiera wiersz z "RTClib" i wersja >= 2.1
    - `arduino-cli lib list` zawiera wiersz z "Adafruit BusIO"
    - Plik `src/arduino/i2c_scanner/i2c_scanner.ino` istnieje
    - i2c_scanner.ino zawiera `#include <Wire.h>`
    - i2c_scanner.ino zawiera `Wire.begin()` BEZ argumentu (linia bez nawiasow z adresem)
    - i2c_scanner.ino zawiera `Wire.beginTransmission(addr)`
    - i2c_scanner.ino zawiera `Serial.println(addr, HEX)`
    - Kompilacja `arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi` konczy sie bez bledow
  </acceptance_criteria>
  <done>RTClib i BusIO zainstalowane, I2C scanner sketch kompiluje sie na Uno R4 WiFi target</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Flash I2C scanner i weryfikacja 0x68 na fizycznym hardware</name>
  <what-built>I2C scanner sketch gotowy do flashowania — weryfikuje fizyczne polaczenie DataLogger Shield z DS1307 na magistrali I2C (A4/A5)</what-built>
  <how-to-verify>
    1. Podlacz Arduino Uno R4 WiFi z DataLogger Shield (gold piny, per D-01/D-03) przez USB
    2. Flash sketch:
       ```bash
       arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:renesas_uno:unor4wifi src/arduino/i2c_scanner/i2c_scanner.ino
       ```
    3. Otworz Serial Monitor (115200 baud):
       ```bash
       arduino-cli monitor -p /dev/ttyACM0 --config baudrate=115200
       ```
    4. Oczekiwany wynik w Serial Monitor:
       ```
       === ARIES-LITE I2C Scanner ===
       Skanowanie adresow 0x01 - 0x7F...
       I2C znaleziony: 0x68
       Znalezione urzadzenia: 1
       Skan zakonczony.
       ```
    5. Jesli 0x68 NIE widoczny — sprawdzic osadzenie shielda na Arduino (docisnac gold header pins)
  </how-to-verify>
  <resume-signal>Wpisz "0x68 OK" jesli DS1307 wykryty, lub opisz problem jesli nie</resume-signal>
</task>

</tasks>

<verification>
- arduino-cli lib list zawiera RTClib i Adafruit BusIO
- i2c_scanner.ino kompiluje sie bez bledow na target arduino:renesas_uno:unor4wifi
- Serial Monitor po flashu pokazuje "I2C znaleziony: 0x68"
</verification>

<success_criteria>
DS1307 wykryty pod adresem 0x68 przez I2C scanner — fizyczne polaczenie DataLogger Shield potwierdzone, gotowe do integracji RTClib w firmware.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rtc-ds1307-izolowana-integracja/25-01-SUMMARY.md`
</output>
