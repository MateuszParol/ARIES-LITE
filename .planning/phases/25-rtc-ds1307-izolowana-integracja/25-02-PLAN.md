---
phase: 25-rtc-ds1307-izolowana-integracja
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: false
requirements: [RTC-01, RTC-02, RTC-03, INT-07]
user_setup: []

must_haves:
  truths:
    - "Firmware z ZegarRTC kompiluje sie na target arduino:renesas_uno:unor4wifi bez bledow"
    - "LCD bootscreen pokazuje 'v2.1  HH:MM:SS' z aktualnym czasem RTC po starcie"
    - "Serial Monitor wypisuje timestamp z RTC w formacie [RTC] YYYY-MM-DD HH:MM:SS"
    - "Przy braku RTC lub roku < 2025 system blokuje sie z LCD 'RTC ERROR' / 'Sprawdz baterie' i ciagly buzzer"
    - "Po 30s bez zasilania RTC nadal pokazuje poprawny czas (bateria CR1220 sprawna)"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Firmware v2.1 z klasa ZegarRTC i modyfikacja setup/HMI"
      contains: "class ZegarRTC"
  key_links:
    - from: "aries_controller.ino:setup()"
      to: "ZegarRTC::inicjalizuj()"
      via: "Wire.begin() przed zegar.inicjalizuj()"
      pattern: "Wire\\.begin\\(\\).*zegar\\.inicjalizuj"
    - from: "HMI::bootscreen_z_czasem()"
      to: "ZegarRTC::odczytaj_czas()"
      via: "DateTime parametr z odczytaj_czas()"
      pattern: "bootscreen_z_czasem"
    - from: "ZegarRTC::inicjalizuj()"
      to: "RTC_DS1307::begin()"
      via: "RTClib I2C komunikacja"
      pattern: "_rtc\\.begin\\(\\)"
---

<objective>
Dodac klase ZegarRTC do firmware aries_controller.ino, zmodyfikowac setup() z kolejnoscia Wire->RTC (per D-14/D-15), zaktualizowac HMI bootscreen aby wyswietlal czas RTC (per D-04), i zaimplementowac blokade startu przy braku RTC (per D-07/D-08/D-09).

Purpose: Izolowana integracja DS1307 — firmware odczytuje czas, wyswietla na LCD bootscreen, blokuje sie jesli RTC niedostepny. Interfejs ZegarRTC gotowy dla Phase 26 (DataLogger CSV).
Output: Firmware v2.1 z ZegarRTC kompilujacy sie i dzialajacy na Uno R4 WiFi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-rtc-ds1307-izolowana-integracja/25-CONTEXT.md
@.planning/phases/25-rtc-ds1307-izolowana-integracja/25-RESEARCH.md
@.planning/phases/25-rtc-ds1307-izolowana-integracja/25-01-SUMMARY.md

<interfaces>
<!-- Istniejace klasy w firmware — executor musi zachowac ich interfejsy -->

From src/arduino/aries_controller/aries_controller.ino:

class HMI — linie 76-169:
  public: inicjalizuj(), lcd_krok(), przycisk_krok(), buzzer_beep()
  private: _lcd (LiquidCrystal), lcd_begin(), lcd_bootscreen()
  Konstruktor: HMI() — bezargumentowy, lista inicjalizacyjna

class ServoPID — linie 175-309:
  public: inicjalizuj(), pid_krok(), skan_krok(), ustaw_serwa(), pid_reset(), resetuj_czas_skanu()
  Pola publiczne: kat_pan, kat_tilt, ostatni_blad_x, ostatni_blad_y

class MaszynaStanow — linie 315-440:
  Konstruktor: MaszynaStanow(ServoPID& serwa, HMI& hmi)
  public: przetwarzaj_bajt(), watchdog_krok(), wymus_skanowanie(), stan()

Globalne instancje (linia 445-447):
  ServoPID serwa;
  HMI hmi;
  MaszynaStanow maszyna(serwa, hmi);

setup() (linia 452-476): Serial.begin → pinMode A0/A1 → hmi.inicjalizuj() → delay(500) → serwa.inicjalizuj()
loop() (linia 481-503): parser serial → watchdog → pid → lcd → przycisk
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Klasa ZegarRTC + modyfikacja HMI + modyfikacja setup()</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (CALY plik — executor musi widziec aktualny stan przed edycja)
    - .planning/phases/25-rtc-ds1307-izolowana-integracja/25-RESEARCH.md (sekcja "Code Examples" — wzorce gotowe do uzycia)
    - .planning/phases/25-rtc-ds1307-izolowana-integracja/25-CONTEXT.md (decyzje D-04 do D-16)
  </read_first>
  <action>
Modyfikacja aries_controller.ino — 4 zmiany w jednym pliku:

**ZMIANA 1: Nowe #include na poczatku pliku (przed istniejacymi #include, po komentarzu naglowkowym)**

Dodac na poczatku pliku (linia ~5, przed `#include <QuickPID.h>`):
```cpp
#include <Wire.h>            // I2C master — magistrala dla DS1307 (A4/A5)
#include <RTClib.h>          // Adafruit RTClib 2.1.4 — DS1307 (D-16)
```

**ZMIANA 2: Nowa klasa ZegarRTC (po klasie MaszynaStanow, przed globalnymi instancjami)**

Wstawic klase ZegarRTC miedzy koncem klasy MaszynaStanow a sekcja "Globalne instancje":
```cpp
// ============================================================
// class ZegarRTC — Obsluga zegara DS1307 via I2C (D-10)
// Adapter RTClib z polskim interfejsem.
// Wire.begin() MUSI byc wywolane PRZED inicjalizuj() (D-15).
// ============================================================
class ZegarRTC {
public:
    ZegarRTC() : _dostepny(false) {}

    // Inicjalizacja DS1307 — Wire.begin() musi byc wczesniej (D-15)
    // Zwraca false jesli RTC nie odpowiada LUB rok < 2025 (D-07)
    // Przy pierwszym uruchomieniu (oscylator nie biegnie) ustawia czas kompilacji
    bool inicjalizuj() {
        if (!_rtc.begin()) {
            _dostepny = false;
            return false;
        }
        // Ustaw czas kompilacji TYLKO gdy oscylator nie biegnie (swiezutka bateria)
        // Pitfall 4: NIE bezwarunkowo — nadpisaloby czas przy kazdym resecie
        if (!_rtc.isrunning()) {
            _rtc.adjust(DateTime(F(__DATE__), F(__TIME__)));
        }
        DateTime teraz = _rtc.now();
        if (teraz.year() < 2025) {
            // Czas niepoprawny — bateria rozladowana lub DS1307 niezainicjowany
            _dostepny = false;
            return false;
        }
        _dostepny = true;
        return true;
    }

    // Odczyt aktualnego czasu z DS1307 (~0.3ms I2C)
    DateTime odczytaj_czas() {
        return _rtc.now();
    }

    // Czy RTC zainicjalizowany i czas poprawny
    bool czy_dostepny() const {
        return _dostepny;
    }

private:
    RTC_DS1307 _rtc;
    bool _dostepny;
};
```

**ZMIANA 3: Modyfikacja klasy HMI — dodanie 2 nowych metod publicznych**

Dodac 2 nowe metody publiczne do klasy HMI (po `buzzer_beep()`, przed `private:`):

```cpp
    // Aktualizacja bootscreen z czasem RTC (D-04)
    // Wywolywana po udanej inicjalizacji ZegarRTC
    void bootscreen_z_czasem(DateTime t) {
        char buf[17];
        snprintf(buf, sizeof(buf), "v2.1  %02d:%02d:%02d",
                 t.hour(), t.minute(), t.second());
        _lcd.setCursor(0, 0);
        _lcd.print(buf);
        _lcd.setCursor(0, 1);
        _lcd.print("RTC OK          ");
        delay(1500);  // Czas na przeczytanie — lacznie z bootscreen ~2s
    }

    // Blokada startu — RTC niedostepny (D-08, D-09)
    // LCD: "RTC ERROR" / "Sprawdz baterie" + ciagly buzzer
    // Wywolujacy MUSI umiescic while(true){} po tej metodzie
    void rtc_blokada() {
        _lcd.clear();
        _lcd.setCursor(0, 0);
        _lcd.print("RTC ERROR       ");
        _lcd.setCursor(0, 1);
        _lcd.print("Sprawdz baterie ");
        tone(BUZZER_PIN, 1000);  // ciagly ton 1kHz — bez duration (D-09)
    }
```

Rownoczesnie ZMNIEJSZYC delay w istniejacym `lcd_bootscreen()` z 2000 na 500:
```cpp
    // Bootscreen LCD przed inicjalizacja serw — uzytkownik widzi status (D-03)
    void lcd_bootscreen() {
        _lcd.setCursor(0, 0);
        _lcd.print("ARIES-LITE v2.1");
        _lcd.setCursor(0, 1);
        _lcd.print("Inicjalizacja...");
        delay(500);  // skrocone z 2000ms — reszta czasu na ekranie z czasem RTC
    }
```

**ZMIANA 4: Modyfikacja globalnych instancji i setup()**

Globalne instancje — dodac `ZegarRTC zegar;` PRZED `HMI hmi;`:
```cpp
// Globalne instancje — kolejnosc wazna (ZegarRTC i ServoPID i HMI przed MaszynaStanow)
ZegarRTC zegar;
ServoPID serwa;
HMI hmi;
MaszynaStanow maszyna(serwa, hmi);
```

setup() — wstawic Wire.begin() + zegar.inicjalizuj() MIEDZY hmi.inicjalizuj() a delay(500).
Nowy setup():
```cpp
void setup() {
    Serial.begin(115200);
    uint32_t start = millis();
    while (!Serial && millis() - start < 500) {
        delay(10);
    }

    pinMode(A0, OUTPUT);
    pinMode(A1, OUTPUT);

    // HMI: LCD bootscreen "Inicjalizacja..." + piny buzzer/przycisk
    hmi.inicjalizuj();

    // I2C + RTC po bootscreen (D-14: kolejnosc Wire -> RTC)
    Wire.begin();  // D-15: Wire PRZED rtc.begin()
    if (!zegar.inicjalizuj()) {
        // D-07/D-08/D-09: BLOKADA STARTU — RTC niedostepny lub czas < 2025
        Serial.println("[RTC] BLAD: RTC niedostepny lub czas niepoprawny!");
        hmi.rtc_blokada();
        while (true) { delay(100); }  // Wymaga fizycznego resetu Arduino
    }

    // RTC OK — wyswietl czas na bootscreen (D-04)
    DateTime teraz = zegar.odczytaj_czas();
    hmi.bootscreen_z_czasem(teraz);

    // Serial debug — timestamp RTC (RTC-03: interfejs gotowy dla Phase 26)
    Serial.print("[RTC] ");
    Serial.print(teraz.year()); Serial.print('-');
    Serial.print(teraz.month()); Serial.print('-');
    Serial.print(teraz.day()); Serial.print(' ');
    Serial.print(teraz.hour()); Serial.print(':');
    Serial.print(teraz.minute()); Serial.print(':');
    Serial.println(teraz.second());

    // Soft Start 500ms — stabilizacja napiecia zasilacza 6V PRZED ruchem serw (MIG-08)
    delay(500);

    // Inicjalizacja serw: attach + bezpieczny start (rampa 1000ms) + parametry PID
    serwa.inicjalizuj();
}
```

**WAZNE — czego NIE zmieniac:**
- Klasa ServoPID — BEZ ZMIAN
- Klasa MaszynaStanow — BEZ ZMIAN (poza przesunieciem w pliku jesli potrzebne)
- Funkcja loop() — BEZ ZMIAN
- lcd_krok() — BEZ ZMIAN (D-05)
- Wszystkie #define — BEZ ZMIAN
- Enumy StanParsera, StanSystemu — BEZ ZMIAN
  </action>
  <verify>
    <automated>arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - aries_controller.ino zawiera `#include <Wire.h>` i `#include <RTClib.h>` na poczatku pliku
    - aries_controller.ino zawiera `class ZegarRTC {` z metodami `inicjalizuj()`, `odczytaj_czas()`, `czy_dostepny()`
    - aries_controller.ino zawiera `RTC_DS1307 _rtc;` jako pole prywatne ZegarRTC
    - aries_controller.ino zawiera `if (!_rtc.isrunning())` przed `_rtc.adjust(` (Pitfall 4 protection)
    - aries_controller.ino zawiera `teraz.year() < 2025` w inicjalizuj() (D-07 walidacja)
    - aries_controller.ino zawiera `void bootscreen_z_czasem(DateTime t)` w klasie HMI
    - aries_controller.ino zawiera `void rtc_blokada()` w klasie HMI z `tone(BUZZER_PIN, 1000)`
    - lcd_bootscreen() zawiera `delay(500)` (zmniejszone z 2000)
    - setup() zawiera `Wire.begin()` PRZED `zegar.inicjalizuj()` (D-15)
    - setup() zawiera `hmi.rtc_blokada()` i `while (true)` w bloku if (!zegar.inicjalizuj())
    - setup() zawiera `hmi.bootscreen_z_czasem(teraz)` po udanej inicjalizacji
    - setup() zawiera `Serial.print("[RTC]")` z timestampem
    - Globalna instancja `ZegarRTC zegar;` istnieje PRZED `HMI hmi;`
    - lcd_krok() NIE zostal zmodyfikowany (D-05)
    - loop() NIE zostal zmodyfikowany
    - Kompilacja `arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi` konczy sie bez bledow
  </acceptance_criteria>
  <done>Firmware z ZegarRTC kompiluje sie na Uno R4 WiFi, setup() inicjalizuje Wire->RTC w poprawnej kolejnosci, HMI bootscreen wyswietla czas, blokada startu zaimplementowana</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Weryfikacja sprzetowa RTC na Uno R4 WiFi</name>
  <what-built>Firmware z klasa ZegarRTC — DS1307 odczytuje czas, LCD bootscreen wyswietla HH:MM:SS, blokada startu przy braku RTC</what-built>
  <how-to-verify>
    1. Podlacz Arduino Uno R4 WiFi z DataLogger Shield przez USB
    2. Flash firmware:
       ```bash
       arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino
       ```
    3. Otworz Serial Monitor (115200 baud):
       ```bash
       arduino-cli monitor -p /dev/ttyACM0 --config baudrate=115200
       ```

    **Test 1 — Bootscreen z czasem:**
    - LCD wiersz 0 powinien pokazac "v2.1  HH:MM:SS" z aktualnym czasem (rok 2026)
    - LCD wiersz 1 powinien pokazac "RTC OK"
    - Serial Monitor powinien wypisac: `[RTC] 2026-M-D H:M:S` z aktualnym czasem

    **Test 2 — Poprawnosc czasu:**
    - Porownaj czas na LCD z zegarem telefonu — roznica < 2 minuty jest akceptowalna
    - Jesli czas to 2000-01-01 — oscylator nie biegnie, firmware powinien pokazac "RTC ERROR" (blokada)

    **Test 3 — Blokada startu (opcjonalny):**
    - Jesli mozliwe: wyjmij baterie CR1220, poczekaj 30s, wloz z powrotem, zresetuj Arduino
    - Oczekiwany: firmware ustawi czas kompilacji (adjust) i uruchomi sie normalnie

    **Test 4 — Normalna praca po bootscreen:**
    - Po bootscreen (ok. 2s) LCD powinien przejsc do normalnego trybu (tryb+katy / bledy) — lcd_krok() niezmodyfikowany
    - PID, serial parser, przycisk — wszystko powinno dzialac jak wczesniej

    **Test 5 — Trwalosc czasu (bateria):**
    - Odlacz USB na 30 sekund, podlacz ponownie
    - Czas na bootscreen powinien byc aktualny (nie czas kompilacji) — bateria CR1220 podtrzymuje DS1307
  </how-to-verify>
  <resume-signal>Wpisz "RTC OK" jesli wszystkie testy pozytywne, lub opisz ktory test nie przeszedl</resume-signal>
</task>

</tasks>

<verification>
- Firmware kompiluje sie bez bledow na target arduino:renesas_uno:unor4wifi
- LCD bootscreen wyswietla aktualny czas HH:MM:SS z DS1307
- Serial Monitor wypisuje timestamp [RTC] z rokiem 2026
- Blokada startu dziala przy braku RTC (LCD "RTC ERROR" + buzzer)
- Po bootscreen normalna praca firmware (lcd_krok, PID, parser) — bez regresji
- Po 30s bez zasilania czas RTC poprawny (bateria podtrzymuje)
</verification>

<success_criteria>
DS1307 odczytuje poprawny czas, LCD bootscreen pokazuje HH:MM:SS, setup() inicjalizuje Wire->RTC w poprawnej kolejnosci, blokada startu dziala, normalna praca firmware bez regresji. Interfejs ZegarRTC (odczytaj_czas() -> DateTime) gotowy dla Phase 26.
</success_criteria>

<output>
After completion, create `.planning/phases/25-rtc-ds1307-izolowana-integracja/25-02-SUMMARY.md`
</output>
