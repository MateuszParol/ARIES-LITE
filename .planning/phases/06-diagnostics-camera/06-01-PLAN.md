---
phase: 06-diagnostics-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware.py
  - src/modes/test_tracker.py
autonomous: false
requirements:
  - DIAG-01
  - CAM-01
  - CAM-02

must_haves:
  truths:
    - "set_angles() emits WARNING in terminal for each axis whose value was clamped to the soft limit (pan ±60°, tilt ±30°)"
    - "Both axes are logged independently — only the axis that was clamped gets a WARNING line"
    - "AWB warm-up runs after camera start(); terminal shows INFO 'Czekam na stabilizację AWB (2s)...' then INFO 'ColourGains zablokowane: (R=X.XX, B=X.XX)'"
    - "Live video feed shows neutral skin tones within 3 seconds of startup — blue cast clears after warm-up"
    - "If ColourGains metadata returns None, AWB_FALLBACK_GAINS (2.5, 1.9) is applied with a WARNING log — no crash"
  artifacts:
    - path: "src/hardware.py"
      provides: "Clamp detection + per-axis WARNING logging in set_angles()"
      contains: "logger.warning.*Clamp pan|logger.warning.*Clamp tilt"
    - path: "src/modes/test_tracker.py"
      provides: "AWB warm-up lock in Picamera2Stream.start() + AWB_FALLBACK_GAINS constant"
      contains: "AWB_FALLBACK_GAINS"
  key_links:
    - from: "set_angles() raw input"
      to: "logger.warning"
      via: "per-axis comparison before assignment"
      pattern: "pan_clamped != pan"
    - from: "Picamera2Stream.start()"
      to: "self._picam2.set_controls"
      via: "capture_metadata() after time.sleep(2.0)"
      pattern: "set_controls.*ColourGains"
---

<objective>
Add clamp logging to hardware.py and AWB warm-up lock to test_tracker.py — two surgical, non-behavioral additions that establish observability and correct color rendering before Phase 7 motion tests.

Purpose: Silent servo limit saturation makes runaway PID impossible to diagnose; blue tint from un-locked AWB distorts visual verification of tracking quality. Both must be fixed before motion testing in Phase 7.
Output: src/hardware.py with WARNING-level clamp logging in set_angles(); src/modes/test_tracker.py with AWB_FALLBACK_GAINS constant and 2-second warm-up + ColourGains lock in Picamera2Stream.start().
</objective>

<execution_context>
@/Users/mattparol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattparol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/hardware.py
@src/config.py
@src/modes/test_tracker.py
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From src/hardware.py (current set_angles, lines 41-54):
```python
def set_angles(self, pan: float, tilt: float) -> None:
    # Obetnij wartości do zadeklarowanych bezpiecznych stref dla kamery RPi
    self.pan_angle = max(config.PAN_LIMIT_MIN, min(config.PAN_LIMIT_MAX, pan))
    self.tilt_angle = max(config.TILT_LIMIT_MIN, min(config.TILT_LIMIT_MAX, tilt))

    if not self._mock_mode and self.pan_servo and self.tilt_servo:
        self.pan_servo.angle = self.pan_angle
        self.tilt_servo.angle = self.tilt_angle
```

From src/config.py (limit constants):
```python
PAN_LIMIT_MIN = -60
PAN_LIMIT_MAX = 60
TILT_LIMIT_MIN = -30
TILT_LIMIT_MAX = 30
```

From src/hardware.py (logger, line 13):
```python
logger = logging.getLogger(__name__)
```

From src/modes/test_tracker.py (Picamera2Stream.start(), lines 63-75):
```python
def start(self) -> None:
    """Inicjalizuje kamere i uruchamia watek przechwytywania."""
    self._picam2 = Picamera2()
    video_config = self._picam2.create_video_configuration(
        lores={"size": (self._width, self._height), "format": "YUV420"}
    )
    self._picam2.configure(video_config)
    self._picam2.start()
    logger.info(f"Picamera2 uruchomiona: {self._width}x{self._height} YUV420 (konwersja do BGR)")

    self._running = True
    self._thread = threading.Thread(target=self._petla_przechwytywania, daemon=True)
    self._thread.start()
```

From src/modes/test_tracker.py (module constants, lines 24-34):
```python
LORES_WIDTH = 320
LORES_HEIGHT = 240
HAAR_MIN_NEIGHBORS = 8
HAAR_MIN_SIZE = (80, 80)
STREAK_REQUIRED = 3
SCAN_AMPLITUDE = 45.0
SCAN_FREQUENCY = 0.1
PID_OUTPUT_LIMIT = 10.0
CAMERA_MAX_RETRIES = 3
CAMERA_RETRY_DELAY = 1.0
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dodaj logowanie clamp w set_angles() (DIAG-01)</name>
  <files>src/hardware.py</files>
  <action>
    W metodzie set_angles() — zamiast obecnych dwóch linii przypisania, wprowadź osobne obliczenie clamped values z porównaniem i logowaniem WARNING przed przypisaniem do self.pan_angle / self.tilt_angle.

    Dokładna kolejność operacji:
    1. Oblicz pan_clamped = max(config.PAN_LIMIT_MIN, min(config.PAN_LIMIT_MAX, pan))
    2. Oblicz tilt_clamped = max(config.TILT_LIMIT_MIN, min(config.TILT_LIMIT_MAX, tilt))
    3. Jeśli pan_clamped != pan: logger.warning(f"Clamp pan: {pan:.1f} → {pan_clamped:.1f} (limit)")
    4. Jeśli tilt_clamped != tilt: logger.warning(f"Clamp tilt: {tilt:.1f} → {tilt_clamped:.1f} (limit)")
    5. self.pan_angle = pan_clamped
    6. self.tilt_angle = tilt_clamped
    7. Bez zmian: blok `if not self._mock_mode` pozostaje identyczny

    WAŻNE: Logowanie WARNING przed blokiem `if not self._mock_mode` — WARNING pojawia się zawsze (w trybie mock i hardware). Nie przenosić za ten blok.
    WAŻNE: Loguj TYLKO oś która faktycznie przekroczyła limit (osobne if, nie if/elif).
    WAŻNE: Nie dodawać rate-limitowania ani dodatkowego stanu — każde przekroczenie musi być zalogowane.
    WAŻNE: Nie zmieniać niczego poza blokiem clamp (inicjalizacja, smooth_move_to, detach_servos zostają bez zmian).

    Format logu per CONTEXT.md (locked decision):
    - "Clamp pan: {wartość_wejściowa:.1f} → {wartość_po_clampie:.1f} (limit)"
    - "Clamp tilt: {wartość_wejściowa:.1f} → {wartość_po_clampie:.1f} (limit)"
  </action>
  <verify>
    <automated>cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE && python3 -c "
import sys
sys.path.insert(0, '.')
import logging
logging.basicConfig(level=logging.WARNING)

# Patch config and gpiozero for test
import types
config_mod = types.ModuleType('src.config')
config_mod.PAN_LIMIT_MIN = -60
config_mod.PAN_LIMIT_MAX = 60
config_mod.TILT_LIMIT_MIN = -30
config_mod.TILT_LIMIT_MAX = 30
config_mod.SERVO_STEP = 1.0
sys.modules['src.config'] = config_mod
sys.modules['gpiozero'] = types.ModuleType('gpiozero')
sys.modules['gpiozero.pins'] = types.ModuleType('gpiozero.pins')
sys.modules['gpiozero.pins.pigpio'] = types.ModuleType('gpiozero.pins.pigpio')

import importlib, src.hardware as hw
importlib.reload(hw)

# Test: OOB value triggers WARNING
import io, logging
buf = io.StringIO()
handler = logging.StreamHandler(buf)
hw.logger.addHandler(handler)
hw.logger.setLevel(logging.WARNING)

pt = hw.PanTiltSystem()
pt.set_angles(75.0, -35.0)  # Both OOB

output = buf.getvalue()
assert 'Clamp pan' in output, f'Missing Clamp pan. Got: {output}'
assert 'Clamp tilt' in output, f'Missing Clamp tilt. Got: {output}'
assert '75.0' in output, f'Missing raw pan value. Got: {output}'
assert '60.0' in output, f'Missing clamped pan value. Got: {output}'
assert '-35.0' in output, f'Missing raw tilt value. Got: {output}'
assert '-30.0' in output, f'Missing clamped tilt value. Got: {output}'

# Test: in-limit value triggers NO WARNING
buf2 = io.StringIO()
handler2 = logging.StreamHandler(buf2)
hw.logger.addHandler(handler2)
pt.set_angles(30.0, 10.0)  # In range
assert 'Clamp' not in buf2.getvalue(), f'False positive clamp. Got: {buf2.getvalue()}'

print('PASS: clamp logging correct')
"
    </automated>
  </verify>
  <done>
    set_angles() emituje logger.warning z formatem "Clamp pan: X → Y (limit)" i "Clamp tilt: X → Y (limit)" gdy wartości wejściowe przekraczają soft-limity; nie emituje WARNING gdy wartości są w granicach; logowanie działa niezależnie od trybu mock/hardware.
  </done>
</task>

<task type="auto">
  <name>Task 2: AWB warm-up lock w Picamera2Stream.start() (CAM-01, CAM-02)</name>
  <files>src/modes/test_tracker.py</files>
  <action>
    Dwie zmiany w test_tracker.py:

    **Zmiana 1 — stała modułowa (góra pliku, po istniejących stałych):**
    Dodaj po linii `CAMERA_RETRY_DELAY = 1.0`:
    ```python
    AWB_FALLBACK_GAINS = (2.5, 1.9)  # (Red, Blue) — fallback gdy sensor nie zwróci gains
    ```

    **Zmiana 2 — Picamera2Stream.start() — dołącz po self._thread.start():**
    Dodaj na końcu metody start(), po `self._thread.start()`:
    ```python
    logger.info("Czekam na stabilizację AWB (2s)...")
    time.sleep(2.0)
    metadata = self._picam2.capture_metadata()
    gains = metadata.get("ColourGains")
    if gains is None:
        logger.warning("ColourGains niedostępne, używam fallback (2.5, 1.9)")
        gains = AWB_FALLBACK_GAINS
    self._picam2.set_controls({"ColourGains": gains})
    r, b = gains
    logger.info(f"ColourGains zablokowane: (R={r:.2f}, B={b:.2f})")
    ```

    WAŻNE: `set_controls({"ColourGains": gains})` wywołać TYLKO po `self._picam2.start()` + `time.sleep(2.0)`. Kolejność: start() → thread.start() → sleep(2.0) → capture_metadata() → set_controls(). Nie przenosić przed start().
    WAŻNE: NIE dodawać `AwbEnable: False` do set_controls() — powoduje konflikt sekwencjonowania ISP (locked decision z STATE.md).
    WAŻNE: Użyj `metadata.get("ColourGains")` (nie `metadata["ColourGains"]`) — klucz może nie istnieć lub zwrócić None.
    WAŻNE: Nie zmieniać cv2.COLOR_YUV420p2BGR ani żadnej innej logiki konwersji (locked decision — format jest poprawny).
    WAŻNE: Nie zmieniać żadnej innej metody klasy (stop, read, _petla_przechwytywania), ani stałych PID/HAAR/SCAN.
  </action>
  <verify>
    <automated>cd /Users/mattparol/Desktop/Projekty/ARIES-LITE/ARIES-LITE && python3 -c "
import ast, sys

with open('src/modes/test_tracker.py', 'r') as f:
    source = f.read()

# Verify module constant present
assert 'AWB_FALLBACK_GAINS = (2.5, 1.9)' in source, 'Missing AWB_FALLBACK_GAINS constant'

# Verify key AWB lines present in source
assert 'Czekam na stabilizację AWB (2s)' in source, 'Missing AWB wait INFO log'
assert 'ColourGains zablokowane' in source, 'Missing ColourGains lock INFO log'
assert 'ColourGains niedostępne, używam fallback' in source, 'Missing fallback WARNING log'
assert 'capture_metadata()' in source, 'Missing capture_metadata() call'
assert 'set_controls' in source, 'Missing set_controls call'
assert 'ColourGains' in source, 'Missing ColourGains in set_controls'

# Verify AwbEnable: False NOT present (locked anti-pattern)
assert 'AwbEnable' not in source, 'ERROR: AwbEnable must not be present (ISP conflict)'

# Verify syntax is valid
try:
    ast.parse(source)
    print('Syntax OK')
except SyntaxError as e:
    sys.exit(f'Syntax error: {e}')

print('PASS: AWB warm-up code present and valid')
"
    </automated>
  </verify>
  <done>
    AWB_FALLBACK_GAINS = (2.5, 1.9) istnieje jako stała modułowa; Picamera2Stream.start() blokuje ~2 sekundy po uruchomieniu wątku przechwytywania, odczytuje ColourGains z capture_metadata(), stosuje fallback z WARNING gdy gains=None, lockuje gains przez set_controls({"ColourGains": gains}), emituje INFO "ColourGains zablokowane: (R=X.XX, B=X.XX)"; AwbEnable nie pojawia się w kodzie.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Hardware verification na RPi4</name>
  <what-built>Clamp logging w src/hardware.py (DIAG-01) + AWB warm-up lock w src/modes/test_tracker.py (CAM-01, CAM-02)</what-built>
  <how-to-verify>
    Na hardware RPi4 z podłączoną kamerą:

    1. Uruchom: `python3 run_test_tracker.py`

    2. Sprawdź terminal podczas startu (pierwsze 5 sekund):
       - Musi pojawić się: `INFO ... Czekam na stabilizację AWB (2s)...`
       - Musi pojawić się: `INFO ... ColourGains zablokowane: (R=X.XX, B=X.XX)` (z faktycznymi wartościami)

    3. Sprawdź obraz wideo (okno cv2.imshow lub headless):
       - Przez pierwsze ~2 sekundy: akceptowalny błękitny odcień
       - Po 2-3 sekundach: neutralne odcienie skóry, bez dominanty niebieskiej

    4. Sprawdź logowanie clamp — przesuń twarz tak, by PID wysłał duże korekcje LUB tymczasowo zmień SCAN_AMPLITUDE na 65 w test_tracker.py i restartuj:
       - W terminalu szukaj linii: `WARNING ... Clamp pan: X.X → 60.0 (limit)`
       - Wróć SCAN_AMPLITUDE = 45.0 po teście jeśli była zmieniona

    SUCCESS CRITERIA fazy 6:
    - [ ] WARNING "Clamp pan/tilt" pojawia się gdy servo próbuje przekroczyć limit
    - [ ] INFO "Czekam na stabilizację AWB (2s)..." pojawia się przy starcie
    - [ ] INFO "ColourGains zablokowane: (R=X.XX, B=X.XX)" pojawia się po warm-up
    - [ ] Obraz neutralny kolorowo po ~3 sekundach
  </how-to-verify>
  <resume-signal>Wpisz "approved" po potwierdzeniu wszystkich punktów, albo opisz problem</resume-signal>
</task>

</tasks>

<verification>
Po wykonaniu wszystkich tasków:
- [ ] `grep "logger.warning.*Clamp" src/hardware.py` zwraca co najmniej 2 linie (pan i tilt)
- [ ] `grep "AWB_FALLBACK_GAINS" src/modes/test_tracker.py` zwraca linię stałej modułowej
- [ ] `grep "set_controls" src/modes/test_tracker.py` zwraca linię z ColourGains
- [ ] `grep "AwbEnable" src/modes/test_tracker.py` zwraca puste (MUST be empty)
- [ ] `python3 -c "import ast; ast.parse(open('src/hardware.py').read()); print('OK')"` przechodzi
- [ ] `python3 -c "import ast; ast.parse(open('src/modes/test_tracker.py').read()); print('OK')"` przechodzi
</verification>

<success_criteria>
- [ ] set_angles() loguje WARNING per oś gdy wartość jest clamped — grep "Clamp pan" / "Clamp tilt" działa
- [ ] Tylko przekroczone osie generują WARNING (nie obie gdy tylko jedna przekroczyła)
- [ ] Logowanie aktywne w trybie mock i hardware
- [ ] AWB warm-up blokuje ~2s w Picamera2Stream.start() po thread.start()
- [ ] ColourGains zablokowane przez set_controls() po warm-up
- [ ] Fallback (2.5, 1.9) użyty z WARNING gdy metadata zwraca None
- [ ] AwbEnable nie pojawia się nigdzie w test_tracker.py
- [ ] Oba pliki przechodzą ast.parse() (poprawna składnia Python)
- [ ] Hardware verification: neutralne kolory w ciągu 3s, WARNING clamp widoczny w terminalu
</success_criteria>

<output>
Po ukończeniu wszystkich tasków i checkpointu, utwórz `.planning/phases/06-diagnostics-camera/06-01-SUMMARY.md` z:
- Co zostało zaimplementowane (per-task)
- Faktyczne wartości ColourGains zablokowanych (z terminalu RPi)
- Czy fallback był potrzebny
- Wszelkie odchylenia od planu
</output>
