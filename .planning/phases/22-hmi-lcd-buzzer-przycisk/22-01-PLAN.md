---
phase: 22-hmi-lcd-buzzer-przycisk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: true
requirements:
  - HMI-01
  - HMI-04

must_haves:
  truths:
    - "LCD Row 0 pokazuje aktualny tryb (SKAN/SLEDZ/IDLE) i katy serw — aktualizacja co 200ms"
    - "LCD Row 1 pokazuje bledy X/Y z ostatniej ramki"
    - "LCD bootscreen ARIES-LITE v2.0 widoczny przez 2 sekundy po starcie"
    - "LCD odswiezanie nie powoduje migotania (brak LCD.clear() w lcd_tick)"
    - "LCD nie blokuje PID tick 100Hz — lcd_tick() wywolany poza pid_tick()"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "LCD defines, lcd global, lcd_tick(), bootscreen w setup(), lcd_tick() w loop()"
      contains: "LiquidCrystal lcd"
  key_links:
    - from: "loop()"
      to: "lcd_tick()"
      via: "wywolanie w loop() poza pid_tick()"
      pattern: "lcd_tick\\(\\)"
    - from: "setup()"
      to: "lcd.begin(16, 2)"
      via: "inicjalizacja LCD z bootscreen"
      pattern: "lcd\\.begin"
---

<objective>
Dodanie wyswietlacza LCD 1602 do firmware Arduino: konfiguracja pinow, inicjalizacja z bootscreen, cykliczne odswiezanie trybu i katow serw.

Purpose: Uzytkownik widzi stan systemu (tryb, katy, bledy) na fizycznym LCD bez potrzeby podlaczenia do komputera.
Output: Firmware z dzialajacym LCD — bootscreen przy starcie, odswiezanie co 200ms w loop().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-hmi-lcd-buzzer-przycisk/22-CONTEXT.md
@.planning/phases/22-hmi-lcd-buzzer-przycisk/22-RESEARCH.md

<interfaces>
<!-- Istniejacy firmware — kluczowe elementy do integracji -->

From src/arduino/aries_controller/aries_controller.ino:
```cpp
// Istniejace #include (LiquidCrystal juz dolaczone)
#include <LiquidCrystal.h>

// Istniejace stale
#define PAN_PIN         9
#define TILT_PIN        10

// Istniejace zmienne globalne
StanSystemu stan_systemu = IDLE;
float kat_pan  = 0.0f;
float kat_tilt = 0.0f;
int16_t ostatni_blad_x = 0;
int16_t ostatni_blad_y = 0;

// Istniejace funkcje (zachowac bez zmian)
void przetwarzaj_bajt(uint8_t bajt);
void przejdz_do(StanSystemu nowy_stan);
void dispatch_ramke();
void skan_tick(unsigned long teraz);
void pid_tick();
void safe_startup();
void init_pid();
void ustaw_serwa();

// Istniejacy setup() — trzeba dodac lcd.begin() + bootscreen PRZED serwo_pan.attach()
// Istniejacy loop() — trzeba dodac lcd_tick() po pid_tick()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LCD defines, globals, lcd_tick() i bootscreen w setup()</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino
    - .planning/phases/22-hmi-lcd-buzzer-przycisk/22-RESEARCH.md
  </read_first>
  <action>
Zmodyfikuj `aries_controller.ino` — dodaj LCD 1602 w trybie 4-bit:

**1. Nowe #define (po bloku "Skan Lissajous", przed enum StanParsera):**

```cpp
// --- HMI: LCD 1602 (D-04: piny 2,3,4,5,6,11) ---
#define LCD_RS          2
#define LCD_EN          3
#define LCD_D4          4
#define LCD_D5          5
#define LCD_D6          6
#define LCD_D7_PIN     11    // D7_PIN aby unikac konfliktu nazwy
#define LCD_INTERVAL_MS 200  // 5 Hz odswiezanie (D-02, HMI-01)
```

**2. Nowa zmienna globalna (po linii `StanParsera stan_parsera = WAIT_START;`, przed instancjami Servo):**

```cpp
// --- LCD 1602 (4-bit) ---
LiquidCrystal lcd(LCD_RS, LCD_EN, LCD_D4, LCD_D5, LCD_D6, LCD_D7_PIN);
unsigned long czas_ostatniego_lcd = 0;
```

**3. Nowa funkcja lcd_tick() (umieszczona po `ustaw_serwa()`, przed `setup()`):**

```cpp
// Odswiezanie LCD co 200ms — Row 0: tryb + katy, Row 1: bledy X/Y
// setCursor + overwrite zamiast LCD.clear() — brak migotania (Pitfall 3)
// dtostrf() zamiast sprintf("%f") — AVR nie obsluguje %f (Pitfall 1)
void lcd_tick() {
    unsigned long teraz = millis();
    if (teraz - czas_ostatniego_lcd < LCD_INTERVAL_MS) return;
    czas_ostatniego_lcd = teraz;

    // Row 0: tryb (5 znakow) + katy pan/tilt
    lcd.setCursor(0, 0);
    const char* tryb_str;
    switch (stan_systemu) {
        case TRACK: tryb_str = "SLEDZ"; break;
        case SCAN:  tryb_str = "SKAN "; break;
        default:    tryb_str = "IDLE "; break;
    }
    char pan_buf[5], tilt_buf[5];
    dtostrf(kat_pan,  4, 0, pan_buf);    // np. " +12" lub " -60"
    dtostrf(kat_tilt, 4, 0, tilt_buf);
    char linia0[17];
    snprintf(linia0, sizeof(linia0), "%sP:%-4sT:%-4s", tryb_str, pan_buf, tilt_buf);
    lcd.print(linia0);

    // Row 1: bledy X/Y z ostatniej ramki
    lcd.setCursor(0, 1);
    char linia1[17];
    snprintf(linia1, sizeof(linia1), "Bx:%-5dBy:%-5d", (int)ostatni_blad_x, (int)ostatni_blad_y);
    lcd.print(linia1);
}
```

**4. Zmiana w setup() — dodac lcd.begin() + bootscreen PRZED serwo_pan.attach():**

Po bloku USB CDC wait (`while (!Serial && millis() - start < 3000)`) a PRZED `serwo_pan.attach(PAN_PIN)` dodaj:

```cpp
    // --- LCD bootscreen (HMI-04, D-03) ---
    lcd.begin(16, 2);
    lcd.clear();
    lcd.setCursor(0, 0);
    lcd.print("ARIES-LITE v2.0");
    lcd.setCursor(0, 1);
    lcd.print("Inicjalizacja...");
    delay(2000);
```

**5. Zmiana w loop() — dodac lcd_tick() po pid_tick():**

Na koncu loop() (po `pid_tick();`) dodaj:

```cpp
    // --- LCD odswiezanie (HMI-01) ---
    lcd_tick();
```

**6. Zaktualizuj komentarz na poczatku pliku (linia 4):**

Zmien:
```cpp
// Implementacja LCD, HMI w fazach 22+
```
na:
```cpp
// Faza 22: HMI — LCD 1602, buzzer, przycisk
```

WAZNE:
- NIE uzywaj LCD.clear() w lcd_tick() — setCursor + overwrite (Pitfall 3 z RESEARCH.md)
- Uzyj dtostrf() do konwersji float — sprintf("%f") nie dziala na AVR (Pitfall 1)
- lcd_tick() w loop() POZA pid_tick() — per D-09
- Bootscreen delay(2000) tylko w setup(), NIGDY w loop()
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller/</automated>
  </verify>
  <acceptance_criteria>
    - aries_controller.ino contains `#define LCD_RS          2`
    - aries_controller.ino contains `#define LCD_EN          3`
    - aries_controller.ino contains `#define LCD_D4          4`
    - aries_controller.ino contains `#define LCD_D5          5`
    - aries_controller.ino contains `#define LCD_D6          6`
    - aries_controller.ino contains `#define LCD_D7_PIN     11`
    - aries_controller.ino contains `#define LCD_INTERVAL_MS 200`
    - aries_controller.ino contains `LiquidCrystal lcd(LCD_RS, LCD_EN, LCD_D4, LCD_D5, LCD_D6, LCD_D7_PIN)`
    - aries_controller.ino contains `void lcd_tick()`
    - aries_controller.ino contains `lcd.setCursor(0, 0)` (nie LCD.clear() w lcd_tick)
    - aries_controller.ino contains `dtostrf(kat_pan` (nie sprintf %f)
    - aries_controller.ino contains `lcd.begin(16, 2)` w setup()
    - aries_controller.ino contains `lcd.print("ARIES-LITE v2.0")` w setup()
    - aries_controller.ino contains `delay(2000)` w setup() (bootscreen)
    - aries_controller.ino contains `lcd_tick();` w loop()
    - lcd_tick() NIE zawiera `lcd.clear()`
    - arduino-cli compile exits 0
  </acceptance_criteria>
  <done>
    LCD 1602 zainicjalizowany z bootscreen "ARIES-LITE v2.0" przez 2s, lcd_tick() odswiezany co 200ms w loop() pokazujac tryb + katy + bledy, kompilacja bez bledow.
  </done>
</task>

</tasks>

<verification>
- `arduino-cli compile --fqbn arduino:avr:leonardo src/arduino/aries_controller/` zwraca exit 0
- `grep -c 'lcd_tick' src/arduino/aries_controller/aries_controller.ino` >= 2 (definicja + wywolanie)
- `grep -c 'lcd.clear' src/arduino/aries_controller/aries_controller.ino` == 1 (tylko w setup bootscreen, nie w lcd_tick)
- `grep 'ARIES-LITE' src/arduino/aries_controller/aries_controller.ino` zwraca wynik
</verification>

<success_criteria>
Firmware kompiluje sie bez bledow z LCD 1602: bootscreen przy starcie, cykliczne odswiezanie trybu/katow/bledow na LCD co 200ms, bez migotania (brak LCD.clear() w lcd_tick).
</success_criteria>

<output>
After completion, create `.planning/phases/22-hmi-lcd-buzzer-przycisk/22-01-SUMMARY.md`
</output>
