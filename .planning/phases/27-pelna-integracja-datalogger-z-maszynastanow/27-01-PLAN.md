---
phase: 27-pelna-integracja-datalogger-z-maszynastanow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: true
requirements:
  - INT-06
  - INT-08

must_haves:
  truths:
    - "Zmiana stanu SCAN<->TRACK logowana z RTC timestamp do CSV"
    - "face_size z ramki 8B zapisywany w CSV zamiast placeholder 0"
    - "latency_ms obliczany z millis() delta i zapisywany w CSV"
    - "Komenda 'D' na Serial zrzuca 10 ostatnich wpisow diagnostycznych"
    - "Guard w _przejdz_do() zapobiega wielokrotnemu logowaniu tego samego stanu"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Pelna integracja DataLogger z MaszynaStanow"
      contains: "loguj_zmiane_stanu"
  key_links:
    - from: "MaszynaStanow::_przejdz_do()"
      to: "DataLogger::loguj_zmiane_stanu()"
      via: "Bezposrednie wywolanie przez referencje _logger"
      pattern: "_logger\\.loguj_zmiane_stanu"
    - from: "MaszynaStanow::_przetworz_ramke()"
      to: "MaszynaStanow::ostatni_face_size"
      via: "Ekstrakcja bajtu 6 ramki binarnej"
      pattern: "ostatni_face_size = _ramka_buf\\[6\\]"
    - from: "loop() serial parser"
      to: "DataLogger::zrzuc_ostatnie()"
      via: "Intercept bajtu 'D' przed przetwarzaj_bajt()"
      pattern: "bajt == 'D'"
---

<objective>
Pelna integracja klasy DataLogger z MaszynaStanow: logowanie WSZYSTKICH zmian stanow z RTC timestamp (D-01), uzupelnienie placeholderow face_size i latency_ms rzeczywistymi wartosciami, komenda serialowa 'D' do zrzutu diagnostycznego, bufor 10 ostatnich wpisow.

Purpose: Domkniecie INT-06 i INT-08 — DataLogger loguje kompletna telemetrie z MaszynaStanow, system gotowy do E2E weryfikacji.
Output: Zmodyfikowany aries_controller.ino z pelna integracja DataLogger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pelna-integracja-datalogger-z-maszynastanow/27-CONTEXT.md
@.planning/phases/27-pelna-integracja-datalogger-z-maszynastanow/27-RESEARCH.md

<interfaces>
<!-- Klasa DataLogger (linie 543-657 aries_controller.ino) -->
class DataLogger {
public:
    DataLogger(ZegarRTC& zegar);
    bool inicjalizuj();
    void krok(StanSystemu stan, float pan, float tilt,
              int16_t bx, int16_t by, uint8_t fs, uint16_t latency_ms);
    bool czy_dostepne() const;
private:
    ZegarRTC& _zegar;
    File _plik;
    bool _sd_ok;
    uint8_t _licznik_klatek;
    uint8_t _wpisy_od_flush;
    uint8_t _ostatni_dzien;
    void _otworz_plik_dnia();
    void _zapisz_csv(StanSystemu stan, float pan, float tilt,
                     int16_t bx, int16_t by, uint8_t fs, uint16_t latency_ms);
    void _sprawdz_rotacje();
};

<!-- Klasa MaszynaStanow (linie 364-489 aries_controller.ino) -->
class MaszynaStanow {
public:
    MaszynaStanow(ServoPID& serwa, HMI& hmi);
    void przetwarzaj_bajt(uint8_t bajt);
    void watchdog_krok();
    void wymus_skanowanie();
    StanSystemu stan() const;
    unsigned long czas_ostatniej_ramki() const;
private:
    ServoPID& _serwa;
    HMI& _hmi;
    StanSystemu _stan_systemu;
    uint8_t _ramka_buf[FRAME_SIZE];
    uint8_t _ramka_idx;
    StanParsera _stan_parsera;
    unsigned long _czas_ostatniej_ramki;
    void _przetworz_ramke();
    void _przejdz_do(StanSystemu nowy);
};

<!-- Globalne instancje (linie 662-666) -->
ZegarRTC zegar;
ServoPID serwa;
HMI hmi;
MaszynaStanow maszyna(serwa, hmi);
DataLogger logger(zegar);

<!-- loop() (linie 731-758) -->
void loop() {
    while (Serial.available() > 0) {
        uint8_t bajt = (uint8_t)Serial.read();
        maszyna.przetwarzaj_bajt(bajt);
    }
    maszyna.watchdog_krok();
    serwa.pid_krok(maszyna.stan());
    hmi.lcd_krok(maszyna.stan(), serwa.kat_pan, serwa.kat_tilt,
                 serwa.ostatni_blad_x, serwa.ostatni_blad_y);
    if (hmi.przycisk_krok(maszyna.stan())) {
        maszyna.wymus_skanowanie();
    }
    logger.krok(maszyna.stan(), serwa.kat_pan, serwa.kat_tilt,
                serwa.ostatni_blad_x, serwa.ostatni_blad_y,
                0, 0);  // placeholder
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integracja DataLogger z MaszynaStanow — logowanie zmian stanow, face_size, latency_ms, komenda 'D'</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (caly plik — stan aktualny po Phase 26)
    - .planning/phases/27-pelna-integracja-datalogger-z-maszynastanow/27-RESEARCH.md (wzorce kodu i pitfalls)
    - .planning/phases/27-pelna-integracja-datalogger-z-maszynastanow/27-CONTEXT.md (decyzje D-01..D-07)
  </read_first>
  <action>
Zmodyfikowac aries_controller.ino w nastepujacych punktach. UWAGA: wszystkie zmiany w JEDNYM pliku .ino, kolejnosc modyfikacji jest wazna ze wzgledu na forward declarations.

**1. Klasa DataLogger — dodac nowe pola i metody (wewnatrz istniejacej klasy, linie ~543-657):**

a) Dodac prywatne pola do klasy DataLogger:
```cpp
char _bufor_diagnostyczny[10][64];  // krazacy bufor 10 ostatnich wpisow (640B RAM)
uint8_t _idx_diagnostyczny;         // wskaznik zapisu do bufora krazacego
```

b) Zainicjalizowac `_idx_diagnostyczny(0)` w liscie inicjalizacyjnej konstruktora DataLogger. Wyzerowac `_bufor_diagnostyczny` w konstruktorze: `memset(_bufor_diagnostyczny, 0, sizeof(_bufor_diagnostyczny));`

c) Dodac publiczna metode `loguj_zmiane_stanu()`:
```cpp
// Logowanie zmiany stanu — wywolywane z MaszynaStanow::_przejdz_do()
// Natychmiastowy flush — zdarzenie krytyczne, rzadkie (nie degraduje PID)
// Ten sam format CSV co _zapisz_csv() per D-05 — prosty parsing pandas
void loguj_zmiane_stanu(StanSystemu stary, StanSystemu nowy,
                        float pan, float tilt) {
    if (!_sd_ok || !_plik) return;
    _sprawdz_rotacje();
    DateTime teraz = _zegar.odczytaj_czas();
    char linia[64];
    snprintf(linia, sizeof(linia), "%lu,%d,%d,%d,0,0,0,0",
             (unsigned long)teraz.unixtime(),
             (int)nowy, (int)pan, (int)tilt);
    _plik.println(linia);
    _plik.flush();          // natychmiastowy flush — zdarzenie krytyczne
    _wpisy_od_flush = 0;    // reset licznika bufora
    // Kopiuj do bufora diagnostycznego
    strncpy(_bufor_diagnostyczny[_idx_diagnostyczny % 10], linia, 63);
    _bufor_diagnostyczny[_idx_diagnostyczny % 10][63] = '\0';
    _idx_diagnostyczny++;
}
```

d) Dodac publiczna metode `zrzuc_ostatnie()`:
```cpp
// Zrzut 10 ostatnich wpisow na Serial — diagnostyka bez wyjmowania karty SD (D-06)
void zrzuc_ostatnie() {
    Serial.println(F("[DUMP] Ostatnie 10 wpisow DataLogger:"));
    for (uint8_t i = 0; i < 10; i++) {
        uint8_t idx = (_idx_diagnostyczny + i) % 10;
        if (_bufor_diagnostyczny[idx][0] != '\0') {
            Serial.println(_bufor_diagnostyczny[idx]);
        }
    }
    Serial.println(F("[DUMP] Koniec."));
}
```

e) W istniejacej metodzie `_zapisz_csv()` dodac kopiowanie do bufora diagnostycznego PRZED `if (++_wpisy_od_flush >= 50)`:
```cpp
// Kopiuj do bufora diagnostycznego
strncpy(_bufor_diagnostyczny[_idx_diagnostyczny % 10], linia, 63);
_bufor_diagnostyczny[_idx_diagnostyczny % 10][63] = '\0';
_idx_diagnostyczny++;
```

**2. Klasa MaszynaStanow — dodac referencje DataLogger i pole face_size (linie ~364-489):**

a) Dodac publiczne pole: `uint8_t ostatni_face_size;`

b) Zmienic sygnature konstruktora z `MaszynaStanow(ServoPID& serwa, HMI& hmi)` na `MaszynaStanow(ServoPID& serwa, HMI& hmi, DataLogger& logger)`. W liscie inicjalizacyjnej dodac `_logger(logger)` i `ostatni_face_size(0)`.

c) Dodac prywatne pole: `DataLogger& _logger;`

d) W metodzie `_przetworz_ramke()` zamienic komentarz `// _ramka_buf[6] = face_size — zarezerwowane` na: `ostatni_face_size = _ramka_buf[6];  // Ekstrakcja face_size z bajtu 6 ramki 8B (INT-06)`

e) Zmodyfikowac metode `_przejdz_do(StanSystemu nowy)`:
```cpp
void _przejdz_do(StanSystemu nowy) {
    if (nowy == _stan_systemu) return;  // Guard — nie loguj ponownie tego samego stanu (Pitfall 2)
    StanSystemu stary = _stan_systemu;
    _stan_systemu = nowy;
    _logger.loguj_zmiane_stanu(stary, nowy, _serwa.kat_pan, _serwa.kat_tilt);  // INT-06: logowanie zmiany stanu
    if (nowy == SKANOWANIE) {
        _serwa.resetuj_czas_skanu();
        _serwa.pid_reset();
    } else if (nowy == SLEDZENIE) {
        _hmi.buzzer_beep();
        _serwa.pid_reset();
    }
}
```

**3. Kolejnosc globalnych instancji (linie 662-666) — KRYTYCZNE (Pitfall 1):**

Zmienic z:
```cpp
ZegarRTC zegar;
ServoPID serwa;
HMI hmi;
MaszynaStanow maszyna(serwa, hmi);
DataLogger logger(zegar);
```
Na:
```cpp
ZegarRTC zegar;
ServoPID serwa;
HMI hmi;
DataLogger logger(zegar);              // PRZED maszyna — referencja musi byc wazna (Pitfall 1)
MaszynaStanow maszyna(serwa, hmi, logger);  // Nowy parametr: DataLogger& (INT-06)
```

Zaktualizowac komentarz nad instancjami: `// Globalne instancje — kolejnosc wazna (DataLogger PRZED MaszynaStanow — referencja)`

**4. Modyfikacja loop() (linie 731-758):**

a) Zmienic serial parser — intercept bajtu 'D' PRZED przetwarzaj_bajt() (D-06, D-07):
```cpp
while (Serial.available() > 0) {
    uint8_t bajt = (uint8_t)Serial.read();
    if (bajt == 'D') {
        logger.zrzuc_ostatnie();  // Zrzut diagnostyczny (D-06)
    } else {
        maszyna.przetwarzaj_bajt(bajt);
    }
}
```

b) Zmienic wywolanie logger.krok() — zamienic placeholdery `0, 0` na rzeczywiste wartosci:
```cpp
uint16_t latency_ms = (uint16_t)(millis() - maszyna.czas_ostatniej_ramki());  // D-04: czas od ostatniej ramki
logger.krok(maszyna.stan(), serwa.kat_pan, serwa.kat_tilt,
            serwa.ostatni_blad_x, serwa.ostatni_blad_y,
            maszyna.ostatni_face_size,  // D-03: face_size z bajtu 6 ramki 8B
            latency_ms);               // D-04: latency od ostatniej ramki RPi
```

**5. Komentarz naglowkowy pliku (linia 1-5):**

Dodac linie: `// Faza 27: integracja DataLogger z MaszynaStanow — logowanie zmian stanow, face_size, latency_ms, komenda 'D'`

**ANTI-PATTERNS — NIE robic:**
- NIE uzywac klasy String — tylko char[] + snprintf()
- NIE zmieniac formatu CSV ani nazw kolumn (zamkniete w Phase 26)
- NIE zmieniac kolejnosci Wire.begin() -> RTC -> SD w setup()
- NIE dodawac flush co wpis w krok() — tylko w loguj_zmiane_stanu() (jednorazowe zdarzenie)
- NIE usuwac istniejacego filtra `stan != SLEDZENIE` w krok() — loguj_zmiane_stanu() jest NIEZALEZNA metoda
  </action>
  <verify>
    <automated>cd /home/parolisko/ARIES-LITE && arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - aries_controller.ino kompiluje sie bez bledow na target arduino:renesas_uno:unor4wifi
    - aries_controller.ino zawiera `void loguj_zmiane_stanu(StanSystemu stary, StanSystemu nowy,`
    - aries_controller.ino zawiera `void zrzuc_ostatnie()`
    - aries_controller.ino zawiera `char _bufor_diagnostyczny[10][64]`
    - aries_controller.ino zawiera `DataLogger logger(zegar);` PRZED `MaszynaStanow maszyna(serwa, hmi, logger);`
    - aries_controller.ino zawiera `MaszynaStanow(ServoPID& serwa, HMI& hmi, DataLogger& logger)`
    - aries_controller.ino zawiera `ostatni_face_size = _ramka_buf[6];`
    - aries_controller.ino zawiera `if (nowy == _stan_systemu) return;` w _przejdz_do()
    - aries_controller.ino zawiera `_logger.loguj_zmiane_stanu(stary, nowy,` w _przejdz_do()
    - aries_controller.ino zawiera `if (bajt == 'D')` w loop()
    - aries_controller.ino zawiera `maszyna.ostatni_face_size` w wywolaniu logger.krok()
    - aries_controller.ino zawiera `uint16_t latency_ms = (uint16_t)(millis() - maszyna.czas_ostatniej_ramki())` w loop()
    - aries_controller.ino NIE zawiera placeholder `0, 0);  // face_size i latency_ms`
  </acceptance_criteria>
  <done>Firmware kompiluje sie na Uno R4 WiFi, DataLogger zintegrowany z MaszynaStanow: logowanie zmian stanow, face_size z ramki 8B, latency_ms z millis() delta, komenda 'D' do zrzutu diagnostycznego, bufor krazacy 10 wpisow.</done>
</task>

</tasks>

<verification>
- `arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/` konczy sie bez bledow
- grep potwierdza obecnosc: loguj_zmiane_stanu, zrzuc_ostatnie, _bufor_diagnostyczny, ostatni_face_size
- Kolejnosc globalnych instancji: DataLogger PRZED MaszynaStanow
</verification>

<success_criteria>
- Firmware kompiluje sie bez bledow na Uno R4 WiFi
- Wszystkie 5 zmian (loguj_zmiane_stanu, face_size, latency_ms, komenda 'D', bufor diagnostyczny) sa obecne w kodzie
- Brak placeholderow `0, 0` w logger.krok()
- Guard `if (nowy == _stan_systemu) return;` chroni przed wielokrotnym logowaniem
</success_criteria>

<output>
After completion, create `.planning/phases/27-pelna-integracja-datalogger-z-maszynastanow/27-01-SUMMARY.md`
</output>
