---
phase: 14-pid-sign-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/kalibracja_serw.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Skrypt kalibracyjny odnosi sie do Arduino Uno R4 WiFi, nie Leonardo"
    - "Boot delay 5.0s zapewnia pelna inicjalizacje R4 WiFi przed wysylaniem ramek"
    - "Firmware kompiluje sie bez bledow na arduino:renesas_uno:unor4wifi"
  artifacts:
    - path: "scripts/kalibracja_serw.py"
      provides: "Zaktualizowany skrypt kalibracyjny dla R4 WiFi"
      contains: "Uno R4 WiFi"
  key_links:
    - from: "scripts/kalibracja_serw.py"
      to: "src/vision/serial_interface.py"
      via: "import SerialInterface"
      pattern: "from src.vision.serial_interface import SerialInterface"
---

<objective>
Przygotowanie skryptu kalibracyjnego i weryfikacja kompilacji firmware przed empiryczna kalibracja kierunkow serw.

Purpose: Skrypt kalibracyjny ma nieaktualne komentarze (Leonardo zamiast R4 WiFi) i zbyt krotki boot delay. Firmware musi kompilowac sie poprawnie przed kalibracja.
Output: Zaktualizowany skrypt kalibracyjny, potwierdzona kompilacja firmware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-pid-sign-fix/14-CONTEXT.md
@.planning/phases/14-pid-sign-fix/14-RESEARCH.md

<interfaces>
<!-- Skrypt kalibracyjny uzywa SerialInterface z src/vision/serial_interface.py -->
<!-- SerialInterface.open(), .send_frame(mode, error_x, error_y, face_size), .close() -->
<!-- Firmware: PAN_INVERT (linia 37), TILT_INVERT (linia 38) w aries_controller.ino -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aktualizacja skryptu kalibracyjnego dla R4 WiFi</name>
  <files>scripts/kalibracja_serw.py</files>
  <read_first>
    - scripts/kalibracja_serw.py
    - .planning/phases/14-pid-sign-fix/14-RESEARCH.md (sekcja "Stan skryptu kalibracyjnego")
  </read_first>
  <action>
Zaktualizuj `scripts/kalibracja_serw.py` — 3 zmiany (per D-01, Research sekcja "Stan skryptu"):

1. **Docstring naglowka (linia 13):** Zmien `"Wymaga: Arduino Leonardo z firmware Phase 22, polaczone przez USB Serial."` na `"Wymaga: Arduino Uno R4 WiFi z firmware v2.1+, polaczone przez USB Serial."`

2. **OPOZNIENIE_BOOT (linia 28):** Zmien `OPOZNIENIE_BOOT = 4.0` na `OPOZNIENIE_BOOT = 5.0` i zaktualizuj komentarz na: `# R4 WiFi boot delay: LCD bootscreen 500ms + Soft Start rampa 1000ms + CDC enum ~500ms + DataLogger/RTC init + margines`

3. **Komentarz PORT (linia 26):** Zmien `# Domyslny port Arduino Leonardo na RPi` na `# Domyslny port Arduino Uno R4 WiFi na RPi`

4. **Komentarz w main() linia 76:** Zmien `# Otworz port szeregowy (DTR=False zapobiega resetowi Leonardo per D-19)` na `# Otworz port szeregowy`

5. **Komentarz boot wait linia 79:** Zmien `# Czekaj na peny rozruch Arduino — LCD bootscreen 2s + safe_startup 1s + margines 1s` na `# Czekaj na pelny rozruch Arduino R4 WiFi — boot + LCD + Soft Start + DataLogger init`

6. **FAIL message linia 144:** Zmien `"  Plik: src/arduino/aries_controller/aries_controller.ino (linia 30-31)"` na `"  Plik: src/arduino/aries_controller/aries_controller.ino (linia 37-38)"`

NIE zmieniaj logiki skryptu — tylko komentarze, stale, numer linii. Logika 4 krokow, wysylaj_petla(), SerialInterface jest prawidlowa.
  </action>
  <verify>
    <automated>grep -n "Uno R4 WiFi" scripts/kalibracja_serw.py | head -5 && grep -n "OPOZNIENIE_BOOT = 5.0" scripts/kalibracja_serw.py && grep -n "linia 37-38" scripts/kalibracja_serw.py && python3 -c "import ast; ast.parse(open('scripts/kalibracja_serw.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/kalibracja_serw.py zawiera "Uno R4 WiFi" w docstringu (linia 13)
    - scripts/kalibracja_serw.py zawiera "OPOZNIENIE_BOOT = 5.0"
    - scripts/kalibracja_serw.py zawiera "linia 37-38" w FAIL message
    - scripts/kalibracja_serw.py NIE zawiera "Leonardo" nigdzie w pliku
    - python3 parsuje plik bez bledow skladniowych
  </acceptance_criteria>
  <done>Skrypt kalibracyjny zaktualizowany dla R4 WiFi — komentarze, boot delay 5.0s, poprawne numery linii</done>
</task>

<task type="auto">
  <name>Task 2: Weryfikacja kompilacji firmware na target R4 WiFi</name>
  <files></files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (linie 36-38 — PAN_INVERT, TILT_INVERT)
  </read_first>
  <action>
Zweryfikuj ze firmware kompiluje sie bez bledow na docelowej platformie R4 WiFi. Uruchom:

```bash
arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino
```

Oczekiwany wynik: kompilacja zakonczona sukcesem (exit code 0). Jesli bledy — napraw zanim przejdziesz do kalibracji (Plan 02).

Odczytaj i zaloguj aktualne wartosci PAN_INVERT i TILT_INVERT z firmware — beda punktem wyjscia dla kalibracji:
- PAN_INVERT = +1 (linia 37)
- TILT_INVERT = -1 (linia 38)
  </action>
  <verify>
    <automated>arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - arduino-cli compile exit code = 0
    - Output zawiera "Sketch uses" (potwierdzenie sukcesu kompilacji)
    - Brak linii zawierajacych "error:" w output kompilacji
  </acceptance_criteria>
  <done>Firmware kompiluje sie na arduino:renesas_uno:unor4wifi, aktualne INVERT values udokumentowane</done>
</task>

</tasks>

<verification>
- Skrypt kalibracyjny parsuje sie poprawnie (python3 syntax check)
- Firmware kompiluje sie na R4 WiFi target bez bledow
- Komentarze w skrypcie odwoluja sie do R4 WiFi, nie Leonardo
</verification>

<success_criteria>
- scripts/kalibracja_serw.py gotowy do uruchomienia na RPi z R4 WiFi (boot delay 5.0s, komentarze aktualne)
- Firmware potwierdzona kompilacja na arduino:renesas_uno:unor4wifi
</success_criteria>

<output>
After completion, create `.planning/phases/14-pid-sign-fix/14-01-SUMMARY.md`
</output>
