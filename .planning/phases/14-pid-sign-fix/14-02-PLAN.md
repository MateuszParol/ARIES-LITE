---
phase: 14-pid-sign-fix
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/arduino/aries_controller/aries_controller.ino
autonomous: false
requirements: []

must_haves:
  truths:
    - "Twarz po prawej stronie kadru powoduje ruch serwa pan W PRAWO (negative feedback)"
    - "Twarz ponizej srodka kadru powoduje ruch serwa tilt W DOL (negative feedback)"
    - "Serwa nie uciekaja do limitow katowych — konwergencja PID widoczna"
  artifacts:
    - path: "src/arduino/aries_controller/aries_controller.ino"
      provides: "Poprawne PAN_INVERT i TILT_INVERT po empirycznej kalibracji"
      contains: "PAN_INVERT"
  key_links:
    - from: "scripts/kalibracja_serw.py"
      to: "src/arduino/aries_controller/aries_controller.ino"
      via: "Wynik kalibracji (PASS/FAIL) determinuje wartosc #define INVERT"
      pattern: "PAN_INVERT|TILT_INVERT"
    - from: "src/vision/brain.py"
      to: "src/arduino/aries_controller/aries_controller.ino"
      via: "error_x/y through serial 8B protocol"
      pattern: "error_x|error_y"
---

<objective>
Empiryczna kalibracja PAN_INVERT/TILT_INVERT na Arduino Uno R4 WiFi i weryfikacja negative feedback.

Purpose: Wyeliminowac positive feedback (serwa uciekaja od twarzy) przez kalibracje kierunku na fizycznym hardware R4 WiFi.
Output: Firmware z poprawnymi wartosciami PAN_INVERT/TILT_INVERT, potwierdzone skryptem kalibracyjnym.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-pid-sign-fix/14-CONTEXT.md
@.planning/phases/14-pid-sign-fix/14-RESEARCH.md
@.planning/phases/14-pid-sign-fix/14-01-SUMMARY.md

<interfaces>
<!-- Firmware aries_controller.ino linie 36-38: -->
<!-- #define PAN_INVERT  (1)    // +1 lub -1 -->
<!-- #define TILT_INVERT (-1)   // +1 lub -1 -->
<!-- Zmiana wartosci: zamien (1) na (-1) lub odwrotnie -->

<!-- Kompilacja + upload: -->
<!-- arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino -->
<!-- arduino-cli upload --fqbn arduino:renesas_uno:unor4wifi --port /dev/ttyACM0 src/arduino/aries_controller/aries_controller.ino -->

<!-- Skrypt kalibracyjny: python3 scripts/kalibracja_serw.py -->
<!-- 4 kroki: PAN prawo, PAN lewo, TILT dol, TILT gora -->
<!-- Uzytkownik odpowiada t/n per krok -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Kalibracja empiryczna — uruchomienie skryptu na hardware R4 WiFi</name>
  <files>scripts/kalibracja_serw.py</files>
  <read_first>
    - scripts/kalibracja_serw.py
    - src/arduino/aries_controller/aries_controller.ino (linie 36-38)
    - .planning/phases/14-pid-sign-fix/14-RESEARCH.md (sekcja "Procedura kalibracji")
  </read_first>
  <action>
Checkpoint: uzytkownik uruchamia skrypt kalibracyjny na RPi z podlaczonym Arduino Uno R4 WiFi.

**Prereqs fizyczne:**
- Arduino Uno R4 WiFi podlaczone do RPi przez USB (/dev/ttyACM0)
- Zasilacz 6V podlaczony do serw MG-90S
- Firmware zaflashowany na R4 WiFi (z Phase 28)

**Procedura (na RPi):**
```bash
cd /home/parolisko/ARIES-LITE
source venv/bin/activate
python3 scripts/kalibracja_serw.py
```

Uzytkownik odpowiada `t` (tak) lub `n` (nie) per krok:
- **Krok 1 (PAN prawo):** error_x=+50 — czy serwo PAN poruszylo sie w PRAWO? (per D-02: pan jako pierwszy)
- **Krok 2 (PAN lewo):** error_x=-50 — czy serwo PAN poruszylo sie w LEWO?
- **Krok 3 (TILT dol):** error_y=+30 — czy serwo TILT pochylilo kamere w DOL?
- **Krok 4 (TILT gora):** error_y=-30 — czy serwo TILT unioslo kamere w GORE?

**Oczekiwane wyniki (jesli montaz per D-03: pan+=prawo, tilt+=dol):**
- PAN_INVERT=+1: Kroki 1,2 powinny byc PASS
- TILT_INVERT=-1: Research sugeruje ze Kroki 3,4 MOGA byc FAIL (patrz Pitfall 4/5 w Research)

Resume signal: uzytkownik wkleja wynik PASS/FAIL per krok lub "all pass".
  </action>
  <verify>
    <automated>echo "Checkpoint — weryfikacja wizualna przez uzytkownika na fizycznym hardware"</automated>
  </verify>
  <acceptance_criteria>
    - Uzytkownik uruchomil python3 scripts/kalibracja_serw.py na RPi z R4 WiFi
    - Uzytkownik dostarczyl wynik PASS/FAIL dla kazdego z 4 krokow
    - Wynik jednoznacznie okresla czy PAN_INVERT i TILT_INVERT sa poprawne
  </acceptance_criteria>
  <done>Wynik kalibracji znany — PASS/FAIL per os (PAN, TILT) — gotowe do korekty firmware jesli potrzebna</done>
</task>

<task type="auto">
  <name>Task 2: Korekta PAN_INVERT/TILT_INVERT i reflash firmware (jesli FAIL)</name>
  <files>src/arduino/aries_controller/aries_controller.ino</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (linie 36-38 — aktualne wartosci INVERT)
    - .planning/phases/14-pid-sign-fix/14-RESEARCH.md (sekcja "Lancuch znaku" i "Gdzie naprawic D-09")
  </read_first>
  <action>
Na podstawie wyniku kalibracji (Task 1 checkpoint):

**Scenariusz A — All PASS (exit code 0):**
- Nie zmieniaj firmware. PAN_INVERT=+1, TILT_INVERT=-1 sa poprawne.
- Zaktualizuj komentarz w liniach 37-38 z "zmien empirycznie" na "SKALIBROWANY — poprawny dla montazu R4 WiFi v2.1.1"

**Scenariusz B — PAN FAIL (Kroki 1 lub 2):**
- Zmien linie 37: `#define PAN_INVERT (1)` na `#define PAN_INVERT (-1)`
- Zaktualizuj komentarz: `// -1 skalibrowany empirycznie R4 WiFi v2.1.1`

**Scenariusz C — TILT FAIL (Kroki 3 lub 4):**
- Zmien linie 38: `#define TILT_INVERT (-1)` na `#define TILT_INVERT (1)`
- Zaktualizuj komentarz: `// +1 skalibrowany empirycznie R4 WiFi v2.1.1`

**Scenariusz D — Oba FAIL:**
- Zastosuj zmiany B i C jednoczesnie.

**Po kazdej zmianie (B/C/D):**
1. Kompiluj: `arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino`
2. Wgraj: `arduino-cli upload --fqbn arduino:renesas_uno:unor4wifi --port /dev/ttyACM0 src/arduino/aries_controller/aries_controller.ino`
3. Poczekaj 5s na boot Arduino

Per D-05: wynik kalibracji utrwalony jako #define w firmware. Per D-09: fix TYLKO w Arduino INVERT, NIE w brain.py.
  </action>
  <verify>
    <automated>grep -n "PAN_INVERT\|TILT_INVERT" src/arduino/aries_controller/aries_controller.ino | head -5 && arduino-cli compile --fqbn arduino:renesas_uno:unor4wifi src/arduino/aries_controller/aries_controller.ino 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - aries_controller.ino linia 37 zawiera "#define PAN_INVERT" z wartoscia odpowiadajaca wynikom kalibracji
    - aries_controller.ino linia 38 zawiera "#define TILT_INVERT" z wartoscia odpowiadajaca wynikom kalibracji
    - Komentarze przy INVERT zawieraja "skalibrowany" lub "SKALIBROWANY" (nie "zmien empirycznie")
    - arduino-cli compile exit code = 0
    - Jesli zmieniono INVERT: firmware wgrane na R4 WiFi przez arduino-cli upload
  </acceptance_criteria>
  <done>PAN_INVERT i TILT_INVERT ustawione na wartosci potwierdzone empiryczna kalibracja, firmware skompilowane i zaflashowane</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Weryfikacja negative feedback po kalibracji</name>
  <files>scripts/kalibracja_serw.py</files>
  <read_first>
    - src/arduino/aries_controller/aries_controller.ino (linie 36-38 — finalne wartosci INVERT)
  </read_first>
  <action>
Checkpoint: uzytkownik ponownie uruchamia skrypt kalibracyjny (lub pelny system) aby potwierdzic negative feedback po ewentualnej korekcie INVERT.

**Opcja A — Ponowna kalibracja skryptem (szybsza):**
```bash
python3 scripts/kalibracja_serw.py
```
Wszystkie 4 kroki powinny byc PASS. Exit code = 0.

**Opcja B — Pelny system z kamera (per Success Criteria SC-3, SC-4):**
```bash
source venv/bin/activate
python3 src/vision/run_pi_brain.py
```
1. Staw twarz po PRAWEJ stronie kadru — serwo pan obraca kamere w prawo (SC-1)
2. Staw twarz PONIZEJ srodka kadru — serwo tilt pochyla kamere w dol (SC-2)
3. Serwa nie docieraja do limitow katowych w ciagu 2s od wykrycia twarzy (SC-3)
4. Twarz wycentrowana w obu osiach w ciagu 1-3s — widoczna konwergencja (SC-4)

**Uwaga:** Opcja B wymaga dzialajacego pi_brain.py i kamery. Jezeli niedostepne, Opcja A jest wystarczajaca dla tej fazy.

Resume signal: "kalibracja pass", "e2e pass", lub opis problemow.
  </action>
  <verify>
    <automated>echo "Checkpoint — weryfikacja empiryczna negative feedback na fizycznym hardware"</automated>
  </verify>
  <acceptance_criteria>
    - Skrypt kalibracyjny zwraca exit code 0 (all 4 steps PASS) LUB
    - Pelny system pi_brain.py potwierdza negative feedback w obu osiach
    - Serwa nie uciekaja do limitow katowych podczas SLEDZENIE
  </acceptance_criteria>
  <done>Negative feedback potwierdzony empirycznie — PAN_INVERT i TILT_INVERT poprawne na R4 WiFi</done>
</task>

</tasks>

<verification>
- PAN_INVERT i TILT_INVERT w firmware odpowiadaja fizycznemu montazowi R4 WiFi
- Skrypt kalibracyjny potwierdza 4/4 PASS
- Firmware skompilowane i zaflashowane bez bledow
- Negative feedback: error_x=+50 — pan w prawo, error_y=+30 — tilt w dol
</verification>

<success_criteria>
- Kalibracja: 4/4 krokow PASS (PAN prawo, PAN lewo, TILT dol, TILT gora)
- Firmware: PAN_INVERT i TILT_INVERT ustawione na skalibrowane wartosci, komentarz zaktualizowany
- Kompilacja: arduino-cli compile exit code 0
- Negative feedback potwierdzony empirycznie na R4 WiFi
</success_criteria>

<output>
After completion, create `.planning/phases/14-pid-sign-fix/14-02-SUMMARY.md`
</output>
