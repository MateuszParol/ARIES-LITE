---
phase: 10-detection-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/modes/test_tracker.py]
autonomous: false
requirements: [DET-01, DET-02]

must_haves:
  truths:
    - "HAAR cascade wykrywa twarz przy odleglosci 40-100cm od kamery"
    - "Detekcja dziala przy odchyleniu glowy do ±30° od frontalnej pozycji"
    - "Stan TRACKING utrzymywany przez co najmniej 3 sekundy bez przerwy"
    - "Zielony prostokat widoczny na HUD przy normalnym uzytkowaniu"
  artifacts:
    - path: "src/modes/test_tracker.py"
      provides: "HAAR parameters tuned for real-world detection"
      contains: "HAAR_MIN_NEIGHBORS = 4"
    - path: "src/modes/test_tracker.py"
      provides: "Reduced minimum face size for 40-100cm range"
      contains: "HAAR_MIN_SIZE = (40, 40)"
  key_links:
    - from: "src/modes/test_tracker.py line 27-28 (constants)"
      to: "DetekcjaTwarzy.wykryj() detectMultiScale()"
      via: "module-level constants referenced by name"
      pattern: "minNeighbors=HAAR_MIN_NEIGHBORS"
---

<objective>
Zmiana dwoch stalych HAAR w test_tracker.py zeby detekcja twarzy dzialala w realnych warunkach na RPi4.

Purpose: Root cause braku detekcji to zbyt restrykcyjne parametry HAAR — minSize=(80,80) wymaga twarzy zajmujacej 25% kadru, minNeighbors=8 eliminuje detekcje pod katem. Zmiana na (40,40) i 4 rozwiazuje oba problemy.
Output: Zmodyfikowany src/modes/test_tracker.py z parametrami umozliwiajacymi detekcje przy 40-100cm i odchyleniu do ±30°.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-detection-fix/10-CONTEXT.md
@.planning/phases/10-detection-fix/10-RESEARCH.md

<interfaces>
<!-- Stale modulowe w test_tracker.py linie 27-28 — jedyne miejsce zmiany -->
From src/modes/test_tracker.py (lines 27-28):
```python
HAAR_MIN_NEIGHBORS = 8
HAAR_MIN_SIZE = (80, 80)
```

From src/modes/test_tracker.py (lines 189-194 — NIE ZMIENIAC, referencjonuje stale przez nazwe):
```python
twarze = self._klasyfikator.detectMultiScale(
    szara,
    scaleFactor=1.1,
    minNeighbors=HAAR_MIN_NEIGHBORS,
    minSize=HAAR_MIN_SIZE,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zmiana parametrow HAAR w test_tracker.py</name>
  <files>src/modes/test_tracker.py</files>
  <read_first>
    - src/modes/test_tracker.py (caly plik — sprawdzic aktualne wartosci linii 27-28 i potwierdzic ze detectMultiScale referencjonuje stale przez nazwe)
    - .planning/phases/10-detection-fix/10-CONTEXT.md (locked decisions D-01 do D-05)
  </read_first>
  <action>
Zmienic DOKLADNIE dwie stale w src/modes/test_tracker.py:

Linia 27 — zmienic z:
  HAAR_MIN_NEIGHBORS = 8
na:
  HAAR_MIN_NEIGHBORS = 4
(per D-02: blizej default OpenCV (3), streak filter zapewnia dodatkowa filtracje)

Linia 28 — zmienic z:
  HAAR_MIN_SIZE = (80, 80)
na:
  HAAR_MIN_SIZE = (40, 40)
(per D-01: pozwala na detekcje twarzy od 40cm do ~100cm przy FOV kamery RPi)

NIE ZMIENIAC:
- STREAK_REQUIRED = 3 (per D-03: zostaje bez zmian)
- scaleFactor=1.1 w detectMultiScale (research: 5ms na klatke, miesci sie w budzecie)
- Zadnych innych plikow, w szczegolnosci NIE src/vision.py (per D-04)
- NIE wyciagac parametrow do config.py (per D-05)
  </action>
  <verify>
    <automated>python3 -c "import ast; tree = ast.parse(open('src/modes/test_tracker.py').read()); print('Syntax OK')" && grep -n "HAAR_MIN_NEIGHBORS = 4" src/modes/test_tracker.py && grep -n "HAAR_MIN_SIZE = (40, 40)" src/modes/test_tracker.py && echo "Constants verified"</automated>
  </verify>
  <acceptance_criteria>
    - grep "HAAR_MIN_NEIGHBORS = 4" src/modes/test_tracker.py zwraca dokladnie jedna linie
    - grep "HAAR_MIN_SIZE = (40, 40)" src/modes/test_tracker.py zwraca dokladnie jedna linie
    - grep "STREAK_REQUIRED = 3" src/modes/test_tracker.py zwraca dokladnie jedna linie (nie zmienione)
    - grep "HAAR_MIN_NEIGHBORS = 8" src/modes/test_tracker.py NIE zwraca wynikow (stara wartosc usunieta)
    - grep "HAAR_MIN_SIZE = (80, 80)" src/modes/test_tracker.py NIE zwraca wynikow (stara wartosc usunieta)
    - python3 -c "import ast; ast.parse(open('src/modes/test_tracker.py').read())" konczy sie bez bledu
    - git diff --name-only pokazuje TYLKO src/modes/test_tracker.py — zaden inny plik nie zostal zmodyfikowany
  </acceptance_criteria>
  <done>HAAR_MIN_NEIGHBORS=4 i HAAR_MIN_SIZE=(40,40) w test_tracker.py, plik przechodzi syntax check, STREAK_REQUIRED=3 nie zmienione, zadne inne pliki nie zmodyfikowane</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Weryfikacja empiryczna na RPi4</name>
  <files>src/modes/test_tracker.py</files>
  <read_first>
    - src/modes/test_tracker.py (potwierdzic ze stale sa zmienione przed weryfikacja)
  </read_first>
  <action>
Checkpoint — weryfikacja empiryczna. Claude NIE wykonuje zadnych zmian w kodzie.
Uzytkownik uruchamia test tracker na RPi4 i potwierdza ze detekcja dziala w realnych warunkach.
  </action>
  <verify>
    <automated>grep "HAAR_MIN_NEIGHBORS = 4" src/modes/test_tracker.py && grep "HAAR_MIN_SIZE = (40, 40)" src/modes/test_tracker.py && echo "Ready for empirical verification"</automated>
  </verify>
  <acceptance_criteria>
    - Uzytkownik potwierdza: zielony prostokat na HUD przy 40-100cm od kamery (DET-01)
    - Uzytkownik potwierdza: detekcja przy odchyleniu glowy ~30° (DET-02)
    - Uzytkownik potwierdza: TRACKING utrzymywany >= 3 sekundy
    - Uzytkownik potwierdza: brak false positives na pustym pokoju
  </acceptance_criteria>
  <what-built>Zmienione parametry HAAR w test_tracker.py: minSize=(40,40) i minNeighbors=4 zamiast (80,80) i 8. Streak filter (STREAK_REQUIRED=3) bez zmian.</what-built>
  <how-to-verify>
    1. Uruchom: `sudo pigpiod && python3 run_test_tracker.py`
    2. Test DET-01 (odleglosc): Stan 40-100cm od kamery — zielony prostokat powinien pojawic sie na HUD
    3. Test DET-01 (porownanie): Sprobuj tez z ~30cm — powinno dzialac rowniez (regresja)
    4. Test DET-02 (kat): Odchyl glowe ~30° w lewo lub prawo — zielony prostokat powinien pozostac na HUD
    5. Test stabilnosci: Po wejsciu w TRACKING — sprawdz czy stan utrzymuje sie przez minimum 3 sekundy bez migotania
    6. Test false positives: Skieruj kamere na pusty pokoj (bez twarzy) — NIE powinien wejsc w TRACKING
  </how-to-verify>
  <resume-signal>Wpisz "approved" jesli detekcja dziala poprawnie, lub opisz problemy</resume-signal>
  <done>Uzytkownik zatwierdzil empiryczna weryfikacje detekcji na RPi4</done>
</task>

</tasks>

<verification>
- python3 -c "import ast; ast.parse(open('src/modes/test_tracker.py').read())" — syntax OK
- grep "HAAR_MIN_NEIGHBORS = 4" src/modes/test_tracker.py — nowa wartosc obecna
- grep "HAAR_MIN_SIZE = (40, 40)" src/modes/test_tracker.py — nowa wartosc obecna
- grep "STREAK_REQUIRED = 3" src/modes/test_tracker.py — niezmienione
- Empiryczna weryfikacja na RPi4: zielone prostokaty przy 40-100cm, detekcja pod katem, TRACKING >= 3s
</verification>

<success_criteria>
1. HAAR_MIN_NEIGHBORS=4 i HAAR_MIN_SIZE=(40,40) w test_tracker.py
2. Zielony prostokat na HUD przy odleglosci 40-100cm od kamery (DET-01)
3. Detekcja przy odchyleniu glowy do ±30° (DET-02)
4. TRACKING utrzymywany >= 3 sekundy bez przerwy
5. Brak regresji — detekcja nadal dziala z bliska (~30cm)
</success_criteria>

<output>
After completion, create `.planning/phases/10-detection-fix/10-01-SUMMARY.md`
</output>
